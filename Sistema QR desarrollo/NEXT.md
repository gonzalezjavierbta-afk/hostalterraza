# 🚀 NEXT.md — Estado de Relevo y Próximos Pasos (v1.19-FIX)

Este documento define el punto de control estratégico y las directrices para la continuación inmediata del desarrollo. Es la hoja de ruta obligatoria para cualquier IA o agente que retome el proyecto para asegurar la fidelidad de 1px y la integridad del núcleo.

---

#### -19. Hito Mas Reciente — Silo F12 "Kande": ajuste del HERO en MOVIL, ciclo express v1.13.0 -> v1.13.3 (v1.13.0-1.13.2 desplegado en `main`/Vercel; v1.13.3 en working tree) (2026-09-23)
*   **Que se estaba haciendo:** cierre documental (Mandato 12 / Reglas de Oro #12; Regla 8: el historial de chat nunca es fuente de verdad) del ajuste del HERO en MOVIL del silo f12 "Kande" (`css/templates/fiesta/f12.css`, template_id `f12`, categoria `fiesta`, theme `kande`), ejecutado en **MODO EXPRESS** con el subagente `@frontend-tpl` (pro). **UNICO archivo tocado.** Todo verificado contra el archivo real (ADR-006, prevalece el archivo).
*   **Estado:** `f12.css` **v1.13.2** desplegado en `main` (produccion via Vercel): commits `f45f651` (v1.13.0), `daa1755` (v1.13.1), `1fbd8a6` (v1.13.2 = HEAD), cada uno toca UNICAMENTE `css/templates/fiesta/f12.css`. **Ronda 4 = v1.13.3 aplicada en el WORKING TREE, SIN commit ni deploy** (2026-09-23): `git status` = `css/templates/fiesta/f12.css` (M) + `Sistema QR desarrollo/TASKS.md` (M) + `Sistema QR desarrollo/NEXT.md` (M). El repo ya NO esta limpio (el commit de v1.13.3 + el cierre documental quedan a cargo de Direccion).
*   **Cambios por version (4 rondas, registro interno del CSS):** **v1.13.0** titulo movil mas grande (~75% del ancho de pantalla): `--f12-title-size` en `@media (max-width:767px)` de `clamp(3rem,16vw,5.6rem)` a `clamp(3.4rem,24vw,11.5rem)` y en `@media (max-width:480px)` de `clamp(2.6rem,17vw,4.2rem)` a `clamp(3rem,24vw,7.6rem)`; cinta de subtitulo (`#mod-hero::before`) mas alta (`bottom:54%` -> `58%`, `height:2rem` -> `2.15rem`) con letra mayor (`.55rem` -> `.62rem` en <=767 y `.5rem` -> `.58rem` en <=480). **v1.13.1** cinta a `bottom:62%` + badge de ceja (`#org-name-badge`) con `margin-top:2.5vh` en movil (baja ceja + titulo). **v1.13.2** cinta a `bottom:67%` + badge `margin-top:5vh`. **v1.13.3** salvaguarda anti-solape en anchos de tablet/landscape (481-767px): bloque NUEVO a nivel raiz entre `@media (max-width:767px)` y `@media (max-width:480px)` (`f12.css` L2217-2231) que limita el titulo por ALTURA (`--f12-title-size: clamp(3rem, min(24vw, calc(20vh - 4rem)), 11.5rem)`) y recorta el badge de ceja (`#org-name-badge` `margin-top:3vh !important`); NO afecta a telefonos (<=480px).
*   **Naturaleza:** SOLO CSS de silo — sin datos, sin seguridad, sin runtime, sin cambios de JS/HTML, sin tocar IDs del Contrato de Datos (Cero Borrado respetado).
*   **Verificacion real (ADR-006):** HEAD `1fbd8a6` (v1.13.2) = `f12.css` 2536 lineas / 92222 bytes / 0 bytes > 127 / CRLF=0 / llaves 265/265 (diff 0). **Working tree (v1.13.3):** `f12.css` = **2558 lineas / 93290 bytes / 0 bytes > 127 (ASCII-safe) / CRLF=0 / llaves 268/268 (diff 0)**; `git diff --stat` del working tree = 3 files changed (`f12.css` +23/-1, `TASKS.md`, `NEXT.md`). Escudo GOLD proporcional al riesgo (Modo Express): `node scripts/express_check.js` NO soporta `.css` (solo `.js`) -> se aplico validacion CSS equivalente (llaves balanceadas + ASCII-safety + fin de linea); verificacion visual de v1.13.0-1.13.2 confirmada por el usuario en su movil ("quedo bien").
*   **Deuda registrada (`[DEUDA-EXPRESS]`):** (1) posible solape titulo/cinta en anchos ~600-767px con altura grande (tipo tablet vertical) — **MITIGADA en v1.13.3** (2026-09-23) con la salvaguarda por altura (bloque NUEVO `f12.css` L2217-2231: titulo `min(24vw, calc(20vh - 4rem))` + badge 3vh en 481-767px); ya NO es deuda abierta. (2) **PREEXISTENTE (no tocado en el ciclo express):** el hero de f12 usa `min-height:100vh/100svh` en movil (L1974-1975 en <=767px y L2237-2238 en <=480px), en contra de la norma documentada (72svh base / 68svh <=767) — registrado como **TSK-063**.
*   **Que sigue:** (1) commitear y desplegar v1.13.3 (`f12.css` en working tree SIN commit) — accion de Direccion; (2) corregir el `min-height:100vh/100svh` preexistente del hero f12 movil para alinearlo con la norma (72svh/68svh) — **TSK-063**; (3) registrar retroactivamente en el Core las versiones v1.10.0 -> v1.12.0 (ya hecho en este cierre: TSK-064/065/066); (4) deudas previas que siguen abiertas: TSK-060/061 (ADR-051), TSK-059 (commit+deploy ADR-050), TSK-051..TSK-056 (ciclo F12 D1-D6), TSK-035/039/044..049.
*   **Riesgos activos:** (1) el solape titulo/cinta en tablets verticales (~600-767px) queda **MITIGADO** por la salvaguarda de altura v1.13.3 — vigilar en anchos 481-600px y landscape extremo; (2) el `min-height:100vh/100svh` preexistente del hero f12 movil contradice la norma documentada y puede reproducir el bug de "hero no cabe en pantalla" ya visto en otros silos — **TSK-063**; (3) v1.13.3 esta SIN commit ni deploy (working tree) — no visible en produccion hasta que Direccion commitee; (4) los riesgos de hitos previos siguen vigentes (TSK-026/TSK-035/TSK-017 y drift de docs que citan `evento.html`).

#### -18. Hito Mas Reciente — ADR-051: Fase de landing PROXIMAMENTE (teaser de expectativa) — implementacion COMPLETA en working tree (kernel + OG + admin + cartelera + i18n + express_check + migracion), migracion pendiente de ejecutar (2026-09-22)
*   **Que se estaba haciendo:** cierre documental (Mandato 12 / Reglas de Oro #12; Regla 8: el historial de chat nunca es fuente de verdad) del ciclo ADR-051 — una fase de presentacion "proximamente" (teaser de expectativa) para eventos: la MISMA URL muestra SOLO hero/titulo/subtitulo/contador/meta + boton WhatsApp, y al pasar a `'completa'` la misma pagina se amplia con el resto de modulos **sin recrear la pagina ni cambiar la URL**. La fase vive como estado de primera clase en `eventos.fase_landing` (NO en el gate comercial `eventos.estado` de adr037, ni en el JSONB `config_landing` que `_buildConfigLanding()` reconstruye completo en cada guardado). Todo verificado contra el archivo real del working tree (ADR-006, prevalece el archivo).
*   **Estado (implementacion COMPLETA en el working tree; migracion PENDIENTE de Direccion):** los 7 puntos del alcance ADR-051 estan implementados y verificados contra el archivo real (ADR-006), no solo declarados: (1) `evento-app.html` kernel (atomo `#event-subtitle` L64 + render subtitulo L891-904; gate de fase L1824-1867 que lee `ev.fase_landing`, setea `body[data-fase]` y oculta por whitelist con `display:none !important` + `data-fase-hidden`, neutralizando el grid `#main-content-flow` para evitar filas fantasma de los silos f6/f8/b4; balance de divs 92/92 = 0); (2) `api/evento-og.js` (`select=*` tolerante L143 — nunca rompe el OG si la columna no existe —, `isProx` L228, titulo/meta OG "Proximamente" L97/L117; `node --check` OK, ASCII 0 bytes>127); (3) `admin.html` (control de fase `#ev-fase-landing-wrap` L518-533; feature-detection `HAS_EVENTO_FASE` L2512-2514; `_detectarFaseLanding()` + `_aplicarEstadoFaseLanding()` L2568-2592; `_evFaseLanding` + `setFaseLanding()` + `_validarFaseLandingProximamente()` L2877-2906; preload L5213-5217; payload condicional en UPDATE L5500 e INSERT L6587; deteccion en login L9774; balance de divs = 3, baseline intacto); (4) `index.html` (badge `.htz-badge-prox` CSS L187-189 + badge en tarjeta L690-692; balance de divs = 1, baseline intacto); (5) `js/i18n.js` (keys `landing.hero_wa_prox` y `landing.badge_proxima` en ES L212-213, FR L584-585, EN L956-957); (6) `scripts/express_check.js` (`evento-app.html` agregado a `HTML_SET` L30); (7) migracion NUEVA `migrations/adr051_fase_landing.sql` (aditiva e idempotente; `fase_landing text NOT NULL DEFAULT 'completa'` + `CHECK (fase_landing IN ('completa','proximamente'))`; ASCII puro; NO toca `eventos.estado`; untracked) — **pendiente de ejecutar manualmente en el SQL Editor de Supabase**. Hasta aplicarla el front degrada **fail-open** (todos los eventos se comportan como `'completa'`, 0 regresion).
*   **Verificacion real (ADR-006, working tree al 2026-09-22):** `git status` = `evento-app.html` (M), `api/evento-og.js` (M), `admin.html` (M), `index.html` (M), `js/i18n.js` (M), `scripts/express_check.js` (M), `migrations/adr051_fase_landing.sql` (untracked), `Sistema QR desarrollo/DECISIONS.md`/`TASKS.md`/`NEXT.md` (M, cierre documental). **`node scripts/express_check.js` => PASS 10, FAIL 0** (sin regresiones; baseline divs reconocido: index.html +1, admin.html +3). **Nota de reconciliacion (Regla 8 - el historial no es fuente de verdad):** una version previa de este hito se redacto sobre un working tree capturado **mientras otros agentes editaban en paralelo (race condition)** — declaraba `admin.html`/`index.html`/`js/i18n.js`/`scripts/express_check.js` como "NO implementados aun" (0 ocurrencias); eso quedo **superseded y corregido por esta verificacion**: los 4 archivos SI contienen la implementacion hoy, con la evidencia de lineas del punto "Estado" inmediato anterior.
*   **Dictamen QA (`@qa-auditor`) APTO CON OBSERVACIONES (2026-09-22) + desviacion intencional OBS-2 (aceptada):** (1) **correccion de nombre real (ADR-006):** la funcion de validacion en `admin.html` es `_validarFaseLandingProximamente()` (L2896-2906) — este hito la citaba por error con un identificador incompleto (faltaba el segmento `Landing`); corregido en la linea "Estado" de este mismo hito. (2) **OBS-2 — desviacion INTENCIONAL y ACEPTADA (observacion QA menor):** la validacion de fecha de `admin.html` usa `f < _hoyISO` (L2901: rechaza solo fechas estrictamente anteriores a HOY), por lo que un evento que ocurre HOY **si** puede activar la fase "proximamente"; el ADR-051 redacto `diff <= 0`. La desviacion es deliberada y se acepta: un evento que ocurre hoy sigue siendo un teaser valido; lo que se bloquea es la fecha vacia, el centinela `2099-12-31` y las fechas ya pasadas. (3) **refuerzo post-dictamen del gate del kernel (OBS-1):** `evento-app.html` (L1830-1841) degrada a `'completa'` cuando el anio de la fecha es centinela (>= 2090) o la fecha ya paso (`__tFase <= Date.now()`); con fecha no parseable (`NaN`) conserva `'proximamente'` (validacion conservadora, no rompe la feature). (4) **i18n del rotulo del teaser:** el boton WhatsApp del teaser usa la key `landing.hero_wa_prox` con traduccion real ES/FR/EN (`js/i18n.js` L212/L584/L956) en vez de un literal — el kernel setea `data-i18n="landing.hero_wa_prox"` y `textContent = T('landing.hero_wa_prox')` (`evento-app.html` L1871-1877), sin tocar el `href`.
*   **Deuda registrada (tareas en `TASKS.md`):** **TSK-060** = ejecutar `migrations/adr051_fase_landing.sql` en Supabase SQL Editor (Direccion, no ejecutable por la IA); **TSK-061** = verificacion end-to-end del teaser (activar `'proximamente'` con fecha real futura, confirmar whitelist sin huecos de grid, pasar a `'completa'` y validar la landing ampliada + OG con "Proximamente").
*   **Que sigue:** (1) Direccion ejecuta la migracion adr051 en Supabase (TSK-060) y commitea el working tree; (2) verificacion end-to-end (TSK-061); (3) **alcance ADR-051 CERRADO en codigo** — los 7 puntos (kernel + OG + admin + cartelera + i18n + express_check + migracion) estan implementados y verificados en el working tree (`express_check` PASS 10/0); no queda codigo por escribir, solo ejecutar la migracion y el E2E; (4) deudas previas abiertas: TSK-059 (commit+deploy ADR-050), TSK-051..TSK-056 (ciclo F12), TSK-035/TSK-039/TSK-044..TSK-049.
*   **Riesgos activos:** (1) **cache EDGE hasta ~10 min:** los cambios de `api/evento-og.js` y del kernel pueden tardar en propagarse tras el deploy — la verificacion visual debe considerar la ventana de cache (no confundir cache con bug); (2) **fail-open:** sin la columna `eventos.fase_landing` el teaser NO se activa (0 regresion, pero la feature no se ve hasta ejecutar la migracion); (3) **silo b4:** `#mod-hero-meta` esta oculta por CSS propio de ese silo; el teaser respeta el Aislamiento Atomico (no fuerza su visibilidad global) y b4 muestra el teaser sin meta — aceptado y documentado, no se edita su CSS; (4) **i18n de rotulos:** las cadenas nuevas del teaser ("Proximamente", badge, sufijo) deben existir en `js/i18n.js` para todos los idiomas activos; un idioma sin traduccion cae al texto base (Espanol) — deuda de traduccion, no bug de runtime.
*   **Fuera de alcance (declarado):** `f7.html`, `serie.html` y `registroaforo.html` NO consumen la fase PROXIMAMENTE; la propagacion del teaser a sesiones (replicarlo en el resto de superficies) queda fuera de este ciclo.

#### -17. Hito Mas Reciente — ADR-050: Analitica del landing AMPLIADA — telemetria 8 tipos + 4 vistas `security_invoker` + sub-tab Landing + port b5 (2026-09-22)
*   **Que se estaba haciendo:** cierre documental (Mandato 12 / Reglas de Oro #12; Regla 8: el historial de chat nunca es fuente de verdad) del ciclo ADR-050 sobre `page_events` (ADR-032): la tabla se habia creado y aplicado a produccion (TSK-057) pero NADIE la leia — el panel no mostraba navegabilidad del landing. Este ciclo AMPLIA el contrato de telemetria: CHECK de event_type 6→8 (`section_view`, `lightbox`), 4 vistas nuevas con `WITH (security_invoker = true)`, sub-tab "Landing" (5.º pill) en `admin.html` y telemetria portada a `eventovenezuela.html` (silo b5 que antes no media nada). Todo verificado contra el archivo real del working tree (ADR-006, prevalece el archivo).
*   **Estado (implementado y QA APTO CON OBSERVACIONES; pendiente de commit + deploy por Direccion):** migracion NUEVA `migrations/adr050_landing_analytics_ampliado.sql` (274 lineas, 100% idempotente, 100% ASCII) **APLICADA y VERIFICADA en produccion `ctgyvydzshueemlelkzv.supabase.co`** (TSK-058): CHECK 8 tipos confirmado por `pg_constraint`, indice `idx_page_events_org_slug_created` (org_id, evento_slug, created_at DESC) en `pg_indexes`, **`security_invoker=true` en pg_class para las 4 vistas** (v_landing_analytics 15 columnas con org_id en GROUP BY + form_ok/dup/error + section_views + lightbox_events + scroll_50pct; v_landing_clicks_rank; v_landing_serie_diaria; v_landing_funnel), GRANT SELECT TO authenticated. **Front en working tree SIN commit ni deploy** (`git status`): `admin.html` (M), `evento-app.html` (M), `eventovenezuela.html` (M), migracion untracked; HEAD = `7836e3f` (cierre ADR-049). **TSK-059** (implementacion front + QA): **dictamen `@qa-auditor-free` APTO CON OBSERVACIONES (2026-09-22)** — paso de "implementado, QA en curso" a QA dictaminado con ambas observaciones resueltas: **MEDIO-1** (26 lineas bytes>127 en `admin.html`, emojis/tildes del sub-tab Landing) **ACEPTADA por decision del build** (`admin.html` = HTML cliente charset utf-8, baseline ya 21171 bytes>127; ASCII-safety cero aplica a `api/*.js`/serverless); **MEDIO-2** (`eventovenezuela.html` emitia la galeria como `click` en vez de `lightbox`) **CORREGIDA y VERIFICADA** (emision de `.gallery-thumb` movida de CLICK_RULES al handler del lightbox nativo: `emit('lightbox','lb:open','gallery',1)` + `lb:close` origenes x/backdrop/esc; express_check PASS 9/0, divs 120/120, ASCII 0/0, sin doble emision); deuda BAJO-1 (documental ADR-050) ya cerrada. **Pendiente de Direccion: commit del working tree y despliegue del front a Vercel — desplegar front (`evento-app.html`, `eventovenezuela.html`, `admin.html`) + migraciones adr032/adr050 YA aplicadas en produccion.**
*   **Verificacion real (ADR-006, working tree):** `admin.html` (10362 lineas) — `data-pnl-pill="landing"` L1439 (5.º pill tras resumen/tendencias/rankings/asistentes), `data-pnl-tab="landing"` L1659-1698 (6 KPIs pageviews/unique/clicks/forms/form_ok/tiempo activo L1662-1667, chart serie diaria, embudo `pn-ld-funnel`, ranking con chips all/click/lightbox/section_view, tabla eventos recientes, boton `exportarLandingXLSX()` L1696), `renderLanding()` L8151 consulta las 4 vistas con `.eq('org_id', ORG_ID)` (+slug y periodo opcionales), empty state fail-open "Sin datos de navegabilidad todavia" L8161; NO hay boton PDF en el sub-tab Landing (solo export XLSX). `evento-app.html` (1828 lineas) — `CLICK_RULES` L415-428 con `btn:spotify` + `link:externo` (a[target=_blank] y .video-placeholder), comentario L409-414 (lightbox nativo emite `lightbox` sin duplicar clics), `emitLb` L561-613 (`lb:open`/`lb:next`/`lb:prev`/`lb:close`), `section_view`/`sec:visible` L461-480, scroll umbrales 25/50/75/95 L444-459, `btn:tab_*` L674. `eventovenezuela.html` — port b5: `.gallery-thumb`→`lb:open` 'gallery' L643, `link:externo` L645-646, `section_view` L695; formulario campana con `comunidad_ok`/`form_error` (sin `comunidad_dup`).
*   **Deuda registrada (tareas en `TASKS.md`):** **TSK-059** = implementacion del ESCUDO ampliado en el front (evento-app + port b5 + sub-tab Landing) — **QA APTO CON OBSERVACIONES (2026-09-22), ambas resueltas (MEDIO-1 aceptada, MEDIO-2 corregida y verificada); cierre real pendiente de commit del working tree + despliegue del front (accion de Direccion)**. **Riesgo de tenancy pendiente (ver §4):** `page_events_auth_read` queda con `USING true` → restriccion por `org_id` (patron `get_org_id()` + org master, ADR-025/036) en el barrido de tenancy (referencia TSK-026) — impacta las 4 vistas via `security_invoker`. **Diferidos/opcionales:** boton "compartir" (atomo kernel nuevo, ADR aparte, precedente TSK-047); medicion de eventos internos del iframe de Spotify (cross-origin, no capturable sin SDK); deteccion de duplicados de comunidad en `eventovenezuela.html` (hoy solo `form_error` en catch, sin `comunidad_dup`).
*   **Que sigue:** (1) **desplegar el front a Vercel** (`admin.html` + `evento-app.html` + `eventovenezuela.html`) — **migraciones adr032+adr050 YA aplicadas en produccion**; los datos de telemetria acumulan desde el 2026-09-22 sin historico previo; (2) QA `@qa-auditor` del sub-tab Landing + telemetria ampliada + port b5 **YA DICTAMINADO y CERRADO (APTO CON OBSERVACIONES 2026-09-22, ambas resueltas)** para cerrar TSK-059 — el unico pendiente del ciclo es el **commit del working tree + deploy del front a Vercel (Direccion)**; (3) barrido de tenancy TSK-026 con el ajuste de `page_events_auth_read` por `org_id`; (4) deudas previas abiertas: TSK-025/026/028/035/039, TSK-044..056 (ciclo F12), TSK-048/049; (5) el **commit del cierre** (patron vigente: Direccion commitea — working tree con 3 HTML + migracion + 3 docs del Core).
*   **Riesgos activos:** (1) **RLS `page_events_auth_read` con `USING true`** — cualquier authenticated puede leer las filas de `page_events` de TODAS las organizaciones; no es fuga total porque el panel filtra por ORG_ID en las consultas y las 4 vistas usan `security_invoker`, pero la politica subyacente es permisiva → ajuste por `org_id` pendiente (TSK-026); (2) **front sin desplegar**: migraciones aplicadas en produccion pero `admin.html`/`evento-app.html`/`eventovenezuela.html` sin commit ni deploy — el sub-tab Landing NO se vera en produccion hasta desplegar; (3) `v_landing_analytics` se recreo con DROP+CREATE (no CREATE OR REPLACE: cambia el orden de columnas con org_id primero) — cualquier lector externo de la vista v1 (ADR-032) queda invalidado hasta ajustarse (no hay lectores conocidos fuera de `admin.html`); (4) **sin historico**: la tabla nace vacia (0 filas) — las decisiones de conversion requieren acumular datos desde el 2026-09-22; (5) los riesgos de hitos previos siguen vigentes (deudas TSK-026/TSK-035/TSK-017 y drift de docs que citan `evento.html`).

#### -16. Hito Mas Reciente — Silo F12 "Kande": ciclo visual CSS-only v1.3.0 -> v1.9.0 desplegado en `main`/Vercel (ADR-049, 2026-09-22)
*   **Que se estaba haciendo:** cierre documental (Mandato 12 / Reglas de Oro #12; Regla 8: el historial de chat nunca es fuente de verdad) del ciclo de iteracion visual del silo f12 "Kande" (`css/templates/fiesta/f12.css`, template_id `f12`, categoria `fiesta`, theme `kande`) del evento `kande-musica-tradicional-del-caribe-colombiano-kk4c` (org "Klandestino bar", plan `pro_mensual`; fecha 2026-09-25; lugar "Kr 4 # 19-56 cuarto piso"; boletos: taquilla `$20.000`, preventa `null`). **100% CSS en un solo archivo**, sin tocar kernel, SQL ni admin. Todo verificado contra el archivo real (ADR-006).
*   **Estado (desplegado):** `f12.css` **v1.9.0** en `main` (ya en produccion via Vercel). Baseline `5d3676c` (v1.3.0). Commits del ciclo: `6960cc6` (v1.4.0+v1.5.0 - hero titulo/subtitulo + cinta "Klandestino Bar", boleteria solo taquilla, meta 2 lineas), `17f8cb4` (v1.6.0), `de790a0` (v1.7.0), `f6f13ed` (v1.8.0), `e528464` (v1.9.0 = HEAD); commit de Direccion `a45174f` (asset `--f12-asset-flora`) entre medio. `HEAD == origin/main == e528464`. **Repo limpio y sincronizado** (working tree sin cambios pendientes). Preview: `https://hostalterraza.vercel.app/evento.html?slug=kande-musica-tradicional-del-caribe-colombiano-kk4c`.
*   **Verificacion real (ADR-006, HEAD):** `f12.css` = 2423 lineas / 86600 bytes / 0 bytes > 127 (ASCII-safe) / llaves 257/257 (diff 0) / **22 IDs `#mod-*` unicos intactos** (Cero Borrado, Regla de Oro #3). Los 6 commits del ciclo tocan UNICAMENTE `css/templates/fiesta/f12.css`. Escudo GOLD local PASS (llaves + ASCII + Cero Borrado).
*   **Cambios por version (resumen):** v1.4.0 hero en 2 lineas visuales (`#event-title` `::before` "KANDE" / `::after` subtitulo; texto real intacto en el DOM con `font-size:0`), ceja "KLANDESTINO BAR", boleteria solo taquilla (`#db-boletos-preventa` oculta SIEMPRE - en prod renderiza "POR DEFINIR"); v1.5.0 META 2 lineas por item (patron f9) con contenido fijo Kande; v1.6.0 cinta con subtitulo, banda "BOLETERIA" neutralizada, fondo amarillo WhatsApp, `#event-title::after` neutralizado; v1.7.0 cinta mas cerca del titulo, hero mobile compacto (100svh con overlay), badge cyan, CTA mobile menor; v1.8.0 mobile con menos hueco + mas aire, CTA "COMPRAR ENTRADAS" 100% CSS, cinta mostaza; v1.9.0 mobile +35% de subida, `.boleto-trust` sin resalte, footer "Klandestino Bar" via CSS, ceja con transparencia (`rgba(47,224,208,0.68)` + `backdrop-filter: blur(4px)`).
*   **Deuda registrada (tareas en `TASKS.md`):** **D1** meta data-driven - extender el kernel `evento-app.html` (~L817, gate `__esF9Meta`) a `.tpl-f12` + cargar `content.meta` del evento Kande (**TSK-051**); **D2** copyright real por i18n/dato (hoy solo visual; los lectores de pantalla leen "Chongotronik", `js/i18n.js` L250) (**TSK-052**); **D3** migrar a datos los textos fijos del hero/CTA (**TSK-053**); **D4** assets - `--f12-asset-papel` = `url("")` y revision del contrato `assets/templates/f12/` (**TSK-054**); **D5** deudas heredadas - namespacing `ld-mod-historia-fiesta` (TSK-055; `@media-reader-free` = TSK-048 y barrido `evento.html` = TSK-049, ya existentes, no duplicar); **D6** verificacion visual pendiente mobile (overlay con margenes negativos `-50svh`, contrastes, `:has()`) (**TSK-056**). Patron del ciclo documentado en **ADR-049**.
*   **Que sigue:** (1) D1 + D2/D3/D4/D5/D6 segun prioridad (TSK-051..TSK-056); (2) **deudas previas que siguen abiertas:** TSK-044 (T6 smoke post-D1), TSK-045 (D1/D2 Supabase - asignar plantilla `f12` al evento + tarifa real), TSK-046 (assets Supabase), TSK-047 (nav superior = ADR futuro), TSK-048 (`@media-reader-free`), TSK-049 (barrido citas `evento.html`), TSK-035 (ejecutar migracion `config_global`), TSK-039; (3) el **commit del cierre documental** (TASKS/NEXT/DECISIONS/PROJECT) queda a cargo de Direccion segun el patron vigente del repo.
*   **Riesgos activos:** (1) el silo f12 hoy es "de un solo evento": el contenido fijo Kande en CSS lo hace NO reutilizable hasta ejecutar D1/D3; (2) el overlay mobile con margenes negativos (`-50svh` + `calc` con guard `max(0px,...)`) es fragil - sin la validacion D6 puede romper en 360/390/414px o landscape; (3) el copyright real sigue siendo "Chongotronik" para lectores de pantalla (D2) - deuda de accesibilidad; (4) `--f12-asset-papel` vacio y assets en URLs externas (`i.ibb.co`/`gstatic.com`), no en Supabase Storage (D4); (5) los riesgos del hito -15 siguen vigentes (D1 de ADR-048 sin ejecutar: sin `template_id='f12'` en Supabase el silo NO se inyecta; drift de docs que citan `evento.html`).

#### -15. Hito Mas Reciente — Silo F12 "Kande" (Tropical Noir Brutalista) entregado + Registro del theme en el Wizard + Reconciliacion estructural `evento.html` -> `evento-app.html` (ADR-048, 2026-09-22)
*   **Que se estaba haciendo:** cierre documental completo (Mandato 12 / Reglas de Oro #12, Regla 8: el historial de chat nunca es fuente de verdad) de la entrega del silo NUEVO `css/templates/fiesta/f12.css` v1.0.0 (theme "Kande", template_id `f12`, categoria `fiesta`) + registro del theme `kande` en el Wizard (`admin.html`) + correccion de un defecto MAYOR del preset ADN Visual + **hallazgo estructural critico** (la fuente mas importante de este ciclo): `evento.html` **NO existe como archivo en el repositorio**; el motor publico real es **`evento-app.html`**. Todo verificado contra los archivos reales (ADR-006).
*   **Evidencia real verificada (silo f12):** 1563 lineas / 50294 bytes / 0 bytes > 127 / llaves 193/193 / 0 `@import` reales / 0 selectores fuera de scope (unico archivo de silo 100% scopeado bajo `.tpl-f12`/`.Tpl-F12`) / 28 tokens `--f12-*` unicos + puente `--tpl-accent: var(--master-accent, #ff2f8e)` y `--gold` / media queries 767 (L1247), 480 (L1439), 992-1279 (L1478), min-1280 (L1518), reduced-motion (L1534) / **14 modulos encendidos** (`#mod-hero`, `#unified-frame` countdown, `#mod-hero-meta`, `#mod-hero-ctas`, `#mod-historia`, `#mod-cartel`, `#mod-video`, `#mod-playlist`, `#mod-boletos`, `#mod-form`, `#mod-faq`, `#mod-ubicacion`, `#mod-whatsapp`, `#mod-footer`) y **9 apagados con IDs intactos** (`#mod-lineup`, `#mod-experiencias`, `#mod-objetivo`, `#mod-impacto`, `#mod-info-tecnica`, `#mod-sponsors`, `#mod-equipo`, `#mod-cta-final`, `#mod-descripcion`) / hero opcion A aprobada por Direccion (countdown tira compacta DENTRO del hero; orden `badge -> titulo -> cinta -> meta -> countdown -> CTA`; alturas 72svh base, 68svh <=767px, 72svh 992-1279px, 78svh >=1280px; **NUNCA 100vh/100svh en el hero**) / datos sin duplicar (solo `#meta-lugar` visible; `#meta-fecha`/`#meta-hora` ocultos por CSS con `:has()`, IDs intactos; el countdown asume fecha/hora) / ancla `.hero-cta-btn--lineup` oculta por CSS (apunta a `#mod-lineup` apagado) / 100% CSS (CERO cambios a JS, CERO subconmutaciones, CERO atomos nuevos). Estetica: "Tropical Noir brutalista Kande" — negro carbon texturizado, flora fucsia/turquesa, bloques mostaza tipo papel rasgado, display 900 en mayusculas con `-webkit-text-stroke`, cintas/laterales por pseudoelementos con escapes unicode ASCII-safe.
*   **Evidencia real verificada (registro en `admin.html`, 10077 lineas — 6 puntos):** tarjeta `.ld-theme-card` con `data-theme="kande"` + `onclick="abrirFichaTheme('kande')"` **L606** (grupo Fiesta, tras `fosforescente`) · `_THEME_TYPE` **L2830** (`'kande':'fiesta'`) · `_THEME_TPL` **L2844** (`'kande':'f12'`; el derivado `_TPL_THEME` sale del `reduce`, no se edita) · `_THEME_MODULES` **L2875** · `_THEME_FICHA` **L3103-3104** · `names` de `seleccionarTheme()` **L3235**. Tarjetas totales: **19** (18 previas intactas = Cero Borrado). **Balance de divs: delta 0** (la diferencia +3 es baseline preexistente en `scripts/express_check.js`). **Reconciliacion de conteo:** `_THEME_MODULES` paso de **18 a 19 themes** — el hito -11 de este documento (L39) dice 18; quedo superseded por este hito (Cero Borrado: el hito -11 NO se edito).
*   **Defecto MAYOR detectado por QA y corregido (CONTRATO ADN Visual):** el preset `_THEME_MODULES['kande']` incluia `ld-mod-historia` en `requeridos`, pero ese checkbox solo existe en `#mods-campana` (L684) y NO en `#mods-fiesta` (L651-665). Efecto: marcaba/bloqueaba un checkbox de campaña invisible, persistia `modules.historia=true` en un evento de fiesta y `hideForeignModulesByType()` (L5154; loop L5189-5196) lo desmarcaba en edicion. **Correccion aplicada:** `requeridos` de `kande` = `['ld-mod-boletos','ld-mod-playlist','ld-mod-ubicacion']`; `ld-mod-historia` eliminado por completo (no a recomendados ni selectivos). NO se duplico el id en `#mods-fiesta` (familia de `ERRORES_HISTORICOS.md` seccion 13). Limite heredado: `#mod-historia` del render publico lo enciende el CSS del silo, pero el Wizard de fiesta NO puede expresarlo (falta namespacing por categoria tipo `ld-mod-historia-fiesta`) — deuda.
*   **HALLAZGO ESTRUCTURAL CRITICO (corrige documentacion previa):** `evento.html` **NO existe como archivo en el repo** (`Test-Path` = False). El motor publico real es **`evento-app.html`** (1724 lineas, "Master Orchestrator v214"): `vercel.json` reescribe `/evento.html` hacia `/api/evento-og` y ese endpoint sirve `evento-app.html`; `injectAtomicCSS` (L598-638) construye `css/templates/{categoria}/{template_id}.css` (L600-601) y L691-693 aplica la doble clase `tpl-{template_id}` + `tpl-{lowercase}` al body. Otros hechos del kernel real: `#db-gallery-grid` (L148) vive DENTRO de `#mod-video` (L147) — apagar `#mod-video` oculta la galeria; `#mod-hero-ctas` nace con `display:none !important` (L33) y exige encendido explicito via CSS del silo; NO existe override `?tpl=`/`?cat=` en el kernel real (solo en `evento2.html`, legado); el array `MODULE_IDS` ya no existe en v214 (solo en legados). Reconciliado SIN borrar texto original (patron ADR-006) en `TEMPLATES.md` v1.4.0, `PROJECT.md` Seccion 6 y `DECISIONS.md` ADR-048.
*   **Deudas del cierre (como tareas en `TASKS.md`):** T6 smoke post-commit+D1 (**TSK-044**); D1 asignar plantilla "Kande" al evento `kande-musica-tradicional-del-caribe-colombiano-kk4c` + D2 cargar tarifa real "COVER 20K" (**TSK-045**, Direccion/Supabase); assets de `assets/templates/f12/` — `f12-hero-bg.v1.webp` (2560x1440, <=350KB), `f12-hero-bg-mobile.v1.webp` (1080x1620, <=250KB), `f12-papel-rasgado.v1.png` (1600x180 con alfa, <=80KB), `f12-flora-esquina.v1.png` (900x900 con alfa, <=150KB) — el CSS ya los referencia (L45-48) con fallback visible ADR-008, la v1.0.0 es funcional sin ellos (**TSK-046**); barra de navegacion superior del diseno de referencia = ADR futuro (exige atomo nuevo en `evento-app.html`, cambio de kernel) (**TSK-047**); `@media-reader-free` roto porque su frontmatter declara `opencode/mimo-v2.5-free`, modelo YA RETIRADO del catalogo (invocacion = "Model not found"; el catalogo ofrece `mimo-v2.6-flash-free`, `ling-3.0-flash-fin-free`, `muse-spark-1.2-contributor-free`) — actualizar `.opencode/agent/media-reader-free.md` Y la tabla de modelos de `AGENTS.md` (**TSK-048**); hub `TEMPLATES.md` Paso 8 desactualizado (decia 3 puntos + `seleccionarTheme(...)`; el real son 6 puntos + `abrirFichaTheme(...)` + `_THEME_MODULES` y `_THEME_FICHA`) — corregido en v1.4.0 en este cierre, queda el barrido residual de citas de `evento.html` en otros docs (**TSK-049**). **NOTA DE NUMERACION:** los IDs `TSK-040`/`TSK-041`/`TSK-042` ya existian (Modo Express / Analisis AI-DOS / Esquema Tripartito); por unicidad de ID este ciclo se numera desde **TSK-043** (el silo f12 es `TSK-043`, no `TSK-040`).
*   **Que sigue:** (1) **commit de la entrega por Direccion** — working tree sin commit: `css/templates/fiesta/f12.css` (untracked), `admin.html` (M), `kande contexto.txt` (untracked), mas la acumulada del ciclo tripartito (`AGENTS.md`, `free-plan.md`, 4 agentes nuevos, skills, scripts, `DOCUMENTOS/`, `DECISIONS.md`, `NEXT.md`, `TASKS.md`); (2) ejecutar D1 + D2 en Supabase (TSK-045); (3) T6 smoke post-commit (TSK-044); (4) subir assets (TSK-046); (5) decidir los ADRs pendientes: esquema tripartito (deuda del hito -14) y barra nav superior (TSK-047); (6) reparar `@media-reader-free` (TSK-048); (7) barrido residual de citas `evento.html` en el Core (TSK-049).
*   **Riesgos activos:** (1) sin D1 (plantilla asignada en Supabase) el silo f12 NO se inyecta — el kernel real no tiene override de plantilla; (2) sin tarifa real el kernel auto-oculta `#mod-boletos` (`__hideEmptyModule`) — D2; (3) los assets de f12 aun no existen en Supabase Storage — el CSS referencia la capa de fallback visible (ADR-008), la v1.0.0 es funcional pero el hero usara el respaldo de gradiente; (4) **drift de docs que citan `evento.html` como unico motor** (`BLUEPRINT.md`, `AGENTS.md` — su seccion "Discrepancia conocida" auditaba `evento.html`, hito -14, `PROJECT.md` Seccion 9, `TASKS.md` TSK-016/017/018): este ciclo documenta `evento-app.html` como motor real y reconcilia TEMPLATES/PROJECT/ADR-048, pero el barrido completo de citas queda abierto (TSK-049); (5) el gate de commit de las entregas acumuladas (TSK-029..043) sigue abierto — Direccion debe commitear.
*   **Complemento de cierre (2026-09-22, misma fecha del hito):** materializado el entregable documental faltante del ciclo — subseccion 13.3 nueva en `ERRORES_HISTORICOS.md` (colision de namespace Fiesta/Campana del preset `_THEME_MODULES['kande']`, defecto MAYOR de ADR-048, con problema/causa raiz/correccion/blindaje/verificacion + TRACE al pie). Reconciliaciones ADR-006 verificadas contra `admin.html` real (10077 lineas): `_THEME_FICHA['kande']` = **L3104-3105** (docs previos citaban L3103-3104 — desfase de 1 linea); `_aplicarPresetTheme` = **L3247-3293**; `_buildConfigLanding` = **L3894** (persistencia `historia` en L3961); `hideForeignModulesByType` = **L5154** (loop L5189); `#mods-fiesta` = **L650-678** (grid L652-665); 19 tarjetas `.ld-theme-card` confirmadas. Registrado en `TASKS.md` (TRACE complemento).

#### -14. Hito Más Reciente — Esquema TRIPARTITO Free/Hybrid/Pro + Fix Gatekeeper Tier Gratuito (2026-09-21, sesion xpress)
*   **Qué se estaba haciendo:** cierre documental del ciclo que adopta el **esquema TRIPARTITO de rutas (Free / Hybrid / Pro)** replicando el ejemplo de ExploraCO, y del **fix del gatekeeper del tier gratuito de OpenCode Zen**. Verificado contra los archivos reales (ADR-006).
*   **Hallazgo y fix (evidencia real):** los modelos gratis (`opencode/*`, p.ej. `opencode/big-pickle`) fallaban con el 403 `"OpenCode's free tier can only be used from within OpenCode"`. Causa raíz: OpenCode Zen agregó un gatekeeper (aprox. 16-19 sep 2026) que exige, **simultáneamente**: (1) `stream=true`, (2) declarar las tools `bash`/`shell` y `read` en el body del request, (3) cabeceras de cliente OpenCode. El agente `free-plan` tenía `bash: deny` → OpenCode no declaraba la tool `bash` → 403. FIX: `.opencode/agent/free-plan.md` cambió `edit: deny`/`bash: deny` → `edit: ask`/`bash: ask` (verificado en el archivo real: frontmatter vigente `edit: ask` · `bash: ask`). Verificación en vivo: `opencode run --agent free-plan "responde solo OK"` → OK; `opencode run --agent free-build ...` → OK; `opencode --version` = **1.18.32**; `node scripts/express_check.js` → `Resumen: PASS 9, FAIL 0` con baseline divs reconocido (index.html +1, admin.html +3).
*   **Cambio de gobernanza (esquema TRIPARTITO):** se crearon **4 agentes nuevos** en `.opencode/agent/` — `hybrid-plan.md` (primario, `opencode-go/deepseek-v4.1-flash`, edit/bash deny, matriz de ruteo PRO/FREE por riesgo), `hybrid-build.md` (primario, edit/bash allow), `exp-pickle-free.md` (subagente, `temperature: 0.3`, soporte mecánico gratuito) y `qa-auditor-free.md` (subagente auditor FREE, Escudo GOLD, solo reporta). **`AGENTS.md` reescrito** al esquema tripartito (39 agentes reales en el roster: 15 pares pro/free + 9 singulares; sección 1.1 Ruta GRATUITA / 1.2 Ruta HYBRID / 1.3 Ruta de PAGO + sección nueva "Modelos gratis: gatekeeper del tier gratuito"). **`opencode.json` NO se tocó** (verificado: `default_agent: free-plan` sigue vigente). Revisión `@architect-review-free`: **APTO CON OBSERVACIONES**; se corrigieron 2 (free-plan.md tabla → `@qa-auditor-free`; exp-pickle-free.md → `architect-review-free`), ambas verificadas en los archivos reales. Regla práctica del gatekeeper: un agente con modelo gratis NO debe poner `bash: deny` (omite la tool `bash` del request → 403); usar `bash: ask` o `bash: allow`.
*   **Deuda del cierre documental (3 ítems, `[DEUDA-EXPRESS]`, 2026-09-21):**
    *   `[DEUDA-EXPRESS]` `AGENTS.md` cita `DOCUMENTOS/MODO_EXPRESS_ANALISIS.md` que **NO existe** en el repo (ruta rota pre-existente; verificado con `Test-Path` = False). Además, `DOCUMENTOS/` real en disco solo contiene 3 archivos (`agents.md`, `opencode.json`, `orquestacion agentes.md`) — los entregables citados en hitos -12/-13 (`SKILL_MODO_EXPRESS.md`, `Analisis_AI-DOS_HostalTerraza.md`) **no están en el working tree**: reconciliar físicamente el estado declarado vs. el disco (ADR-006).
    *   `[DEUDA-EXPRESS]` Referencias a `Reglas de Oro QR.md` con emoji `🛡️` en agentes viejos vs. el nombre real del archivo **sin emoji** (`Sistema QR desarrollo/Reglas de Oro QR.md`, verificado = existe): 16 referencias con `🛡️` en `.opencode/agent/` (13 → `🛡️ Reglas de Oro QR.md` en admin-dev/free, architect/free, architect-review-free, backend-dev/free, docs-keeper/free; 3 → `🛡️ Reglas de Oro ExploraCO — v5.md` en content-loader/free, seo-dev/free, renderer-dev/free, qa-auditor). Normalizar el nombre real.
    *   `[DEUDA-EXPRESS]` Decidir si se crea un ADR en `DECISIONS.md` formalizando el esquema TRIPARTITO (hoy rige solo por `AGENTS.md` reescrito y los archivos de `.opencode/agent/`; `DECISIONS.md` NO se tocó en esta sesión — pendiente de decisión de Dirección).
*   **Qué sigue:** (1) Dirección decide si crear el ADR del esquema TRIPARTITO en `DECISIONS.md` (deuda 3) y si quiere migrar el default a `hybrid-plan` en `opencode.json` (hoy sigue `free-plan`, NO se cambió); (2) normalizar referencias con `🛡️` a `Reglas de Oro QR.md` (16 archivos de `.opencode/agent/`); (3) reconciliar `DOCUMENTOS/` (deuda 1); (4) **commitear la entrega** — working tree sin commit: `AGENTS.md` (199 líneas), 4 agentes nuevos (untracked), `free-plan.md` (M), `DECISIONS.md` (M de sesión previa), `NEXT.md` (M), `TASKS.md` (M), `.opencode/skills/express-mode/` (untracked), `scripts/` (untracked), `DOCUMENTOS/` (untracked), siguiendo el patrón vigente del repo (Dirección commitea).
*   **Riesgos activos:** (1) **gatekeeper del tier gratuito** — cualquier agente FREE futuro con `bash: deny` en el frontmatter repetirá el 403; la norma es `bash: ask`/`bash: allow` para modelos `opencode/*`; `hybrid-plan`/`hybrid-build` usan `deepseek-v4.1-flash` (PRO) donde `edit: deny`/`bash: deny` sí es válido para un planificador; (2) la deuda del hito -13 sigue parcial — la matriz TRIPARTITO ya está implementada en `AGENTS.md`, pero persisten: índices de `DECISIONS.md`/`ERRORES_HISTORICOS.md` (ADR-032..038), smokes versionados (`npm run test` inexistente), TSK-025/TSK-026/TSK-028/TSK-035/TSK-039 y el gate de commit de las entregas acumuladas (TSK-029..042); (3) el **drift de `AGENTS.md`** (cita `evento.html` y `pagina-destino.js` inexistentes en el repo con ese estado) sigue abierto.

#### -13. Hito Mas Reciente - Analisis AI-DOS v1.1 + Orquestacion Hybrid por Proyecto (2026-09-21, sesion xpress)
*   **Que se estaba haciendo:** analisis del AI-DOS v1.1 y de las Reglas de Oro QR v127 frente a la operacion real del proyecto, aplicando el metodo del analisis de la etapa ExploraCO a HostalTerraza. Entregado `DOCUMENTOS/Analisis_AI-DOS_HostalTerraza.md` (NUEVO, 319 lineas, ASCII-safe 0 bytes > 127): (1) diagnostico del AI-DOS v1.1 con brechas G1-G12 y tamanos reales de los docs del AI-DOS Core (~374 KB total; ADR-032..038 sin entradas en `DECISIONS.md`, declarados como deuda en ADR-039); (2) diagnostico de las Reglas de Oro QR v127 (16 mandatos); (3) **Seccion 5 central de orquestacion de agentes por proyecto** - proposicion del sistema HYBRID (esquema C tripartito sobre el esquema DUAL actual): matriz agente -> modelo -> ruta PRO/FREE sobre el roster real de 35 archivos en `.opencode/agent/`, regla de ruteo por RIESGO (PRO obligatorio si toca runtime de produccion `admin.html`/`api/*.js`/motor de render/`evento.html`/CSS de silo compartido, datos RLS/esquema, ADR o certificacion QA; FREE por defecto en exploracion/lectura, docs del Core, seeds/smokes no criticos, JS rutinario no compartido, SEO no critico, investigacion y media; ante duda subir a PRO; gate de cuota con etiqueta `[DEUDA-EXPRESS]`), primarios nuevos `hybrid-build`/`hybrid-plan` (cambio propuesto de `default_agent` -> `hybrid-plan` en `opencode.json`) y plantilla de parametrizacion por proyecto. Numeros clave del analisis (evidencia E3 de `logs/uso/`, mes 2026-09-07): 151 sesiones costo **$3.2078**; `deepseek-v4-pro` = **72.8% del gasto con solo 3 sesiones** ($2.3338); ruta free $0.0000; task 92 = 0.76% de 12.081 calls; ratio de delegacion **35.8%** (target >= 50%); explore = 62% de las 92 tareas delegadas; sin evidencia de auditoria KPI posterior al 2026-09-14 (regla 9 de `AGENTS.md` sin ciclo ejecutado).
*   **Que sigue:** decisiones pendientes de Direccion derivadas del analisis: (1) aprobar la instanciacion del sistema HYBRID - a partir de ahi implementar la matriz hybrid en `AGENTS.md`; (2) crear `hybrid-build`/`hybrid-plan` y fijar `default_agent` = `hybrid-plan` en `opencode.json`; (3) crear `qa-auditor-free` para resolver el hueco detectado del roster (hoy solo existe `qa-auditor` pro, Seccion 5.5 del analisis); (4) smokes versionados (hoy el unico smoke ejecutable es `node scripts/express_check.js`; `npm run test` no existe por falta de `package.json`); (5) indices de `DECISIONS.md`/`ERRORES_HISTORICOS.md` (sin TOC; ADR-032..038 pendientes); (6) gate de commit para las entregas acumuladas en el working tree sin commit (TSK-029..040). Ademas sigue pendiente lo ya registrado de hitos previos: TSK-025, TSK-026, TSK-028, TSK-035 y TSK-039 (Direccion/Lead Developer).
*   **Riesgos activos:** (1) **deuda `[DEUDA-EXPRESS]` del cierre documental del analisis** (2026-09-21) - la matriz hybrid NO esta implementada en `AGENTS.md` (el esquema DUAL pro/free de ADR-031 sigue siendo el unico vigente), `hybrid-build`/`hybrid-plan` y `qa-auditor-free` no existen, los docs del Core siguen sin indices y las entregas TSK-029..040 estan **sin commit** en el working tree (patron vigente del repo: Direccion debe commitear); (2) el gasto PRO se concentra en pocas sesiones no criticas (3 sesiones = 72.8% del mes) - cualquier sesion pro sin brief quirurgico repite el patron; (3) el **drift de `AGENTS.md`** (cita `evento.html` y `pagina-destino.js`, inexistentes en el repo con ese estado) sigue abierto y se agrava si se implementa la matriz hybrid sin reconciliar antes los contratos reales (ADR-006).

#### -12. Hito Más Reciente — Modo Express (xpress): skill transversal + comando único de verificación (2026-09-21)
*   **Qué se estaba haciendo:** implementación del **Modo Express (xpress)** como skill operativa transversal de HostalTerraza, verificado sobre los archivos reales (ADR-006). (1) **Nueva skill** `.opencode/skills/express-mode/SKILL.md` (116 líneas, ASCII-safe, 0 bytes > 127): se activa con "express"/"xpress"/"rápido"; incluye gate de escalado con los archivos críticos reales del repo (siempre `@sql-security`, nunca `sql-security-free`, en esquema/RLS/seguridad), ruteo de delegación FREE/PRO, plantilla de brief express (un subagente por dominio con rutas exactas), checklist de 6 puntos y cierre documental con deuda etiquetada. (2) **Nuevo comando único de verificación mínima** `scripts/express_check.js` (362 líneas, Node CommonJS sin dependencias, READ-ONLY, ASCII-safety 0/0/0, `node --check` PASS): escanea `api/*.js` + `js/*.js` + `scripts/*.js` + `*.js` de raíz + el set HTML clave (`index.html`, `admin.html`, `scanner.html`, `eventovenezuela.html`); incluye paso 0 de existencia (faltante = SKIP, nunca FAIL) y `BASELINE_DIV_DIFF = { 'index.html': 1, 'admin.html': 3 }` para no reportar como regresión el desbalance preexistente. (3) **`DOCUMENTOS/SKILL_MODO_EXPRESS.md` reescrito** (120 líneas): copia de registro sincronizada con la skill, sin referencias a ExploraCO. (4) **`AGENTS.md`** (169 → 182 líneas, +13 inserciones, 0 borrados): nueva sección `## Modo Express (xpress)`, nuevo ítem 7 en Reglas transversales y línea de ruteo en el flujo de delegación. (5) **`DECISIONS.md`** (764 → 785 líneas, append al final): "Nota de práctica operativa (**NO es un ADR**) sin número", fecha 2026-09-21, estado VIGENTE. **Verificación ejecutada:** `node --check scripts/express_check.js` → PASS; `node scripts/express_check.js` → `Resumen: PASS 9, FAIL 0 (omitidos 0)` con exit code 0 y baseline reconocido; ASCII-safety del script 0/0/0; 0 residuos "ExploraCO" en la skill; `git diff --stat` → `AGENTS.md +13`, `DECISIONS.md +23/-1` (los -1 son el ajuste de fin de línea, no pérdida de contenido).
*   **Qué sigue:** (1) **commitear la entrega** — todo el working tree (skill nueva, `scripts/`, `DOCUMENTOS/`, `AGENTS.md`, `DECISIONS.md`) quedó **sin commit**, siguiendo el patrón vigente del repo (ver el ítem 6 de la deuda al final de este hito); (2) **ejecutar el primer caso de uso real del modo express** para validar la skill en operación: brief quirúrgico por dominio + `node scripts/express_check.js` como verificación mínima proporcional al riesgo, con cierre documental diferido a un solo pase.
*   **Riesgos activos:** (1) el **baseline de divs** (`index.html` +1, `admin.html` +3) reconocido por `express_check.js` puede **enmascarar una regresión nueva** si alguien toca esos HTML — comparar su balance contra el baseline, no contra cero al ejecutar el comando; (2) el **drift de `AGENTS.md`** (cita `evento.html` y `pagina-destino.js`, inexistentes en el repo) sigue abierto — ver el ítem 1 de la deuda al final de este hito.
*   **Deuda técnica del cierre documental (6 ítems, 2026-09-21):**
    *   `[DEUDA-EXPRESS]` `AGENTS.md` declara vigentes `evento.html` y `pagina-destino.js`, que NO existen en el repo (drift con `TASKS.md`, `NEXT.md` y `vercel.json`, que reescribe `/evento.html` → `/api/evento-og`). Requiere un pase dedicado de reconciliación documental.
    *   `[DEUDA-EXPRESS]` `admin.html` (+3) e `index.html` (+1) tienen el balance de `<div>` desviado de forma preexistente; se registró como baseline en `scripts/express_check.js`. Requiere pase dedicado de balanceo.
    *   `[DEUDA-EXPRESS]` `DOCUMENTOS/MODO_EXPRESS_ANALISIS.md` conserva referencias a ExploraCO y rutas heredadas (`exploraco desarrollo/ampliacion desarrollo/`); no se adaptó en esta sesión.
    *   `[DEUDA-EXPRESS]` No existe `package.json` en el repo → el `npm run test` que exige `AGENTS.md` (regla transversal 4) no es ejecutable; el smoke real disponible es `node scripts/express_check.js`.
    *   `[DEUDA-EXPRESS]` La carpeta `DOCUMENTOS/` no está trackeada por git (`?? DOCUMENTOS/`); decidir si se versiona o se excluye.
    *   `[DEUDA-EXPRESS]` Los nuevos assets (`.opencode/skills/express-mode/`, `scripts/`, `AGENTS.md`, `DECISIONS.md`) quedaron en working tree SIN commit, siguiendo el patrón vigente del repo.

#### -11. Hito Más Reciente — Contratos de Configuración del Wizard/Landing (ADR-047 — TSK-036 ✅ / TSK-037 ✅ / TSK-038 ✅ / TSK-039 ⏳)
*   **Qué se entregó (working tree modificado, SIN commit) — QA APTO (`@qa-auditor` sobre `admin.html` y `evento.html`):** cierre documental de TRES features de configuración del Wizard/landing que ya estaban implementadas y certificadas APTO, pero sin registro en la memoria de ingeniería. Ninguna de las tres estaba documentada en `DECISIONS.md`, `TASKS.md` ni `NEXT.md` antes de este cierre.
    *   **(1) Redirect dinámico del formulario del landing (`form_redirect`, TSK-036 / ADR-047):** contrato nuevo `config_landing.content.form_redirect` (string, URL genérica: WhatsApp / grupo / comunidad), **distinto** de `content.whatsapp` (contacto/ventas) y de `content.comunidad_whatsapp`. `evento.html` lo consume con retrocompatibilidad: `effContent.form_redirect || effContent.comunidad_whatsapp || ''`; si hay link y el usuario marcó "unirse a comunidad", redirige en el **MISMO tab** (`window.location.href`) tras insertar en `inscritos`; si está vacío/ausente, mantiene el mensaje de éxito (Silent Fallback, ADR-008). `admin.html`: input `#ld-form-redirect` (`type="url"`, junto a `#ld-whatsapp-tel`), persistido en `_buildConfigLanding` **solo cuando es truthy** (`delete` si vacío, para no pisar el valor previo); precarga en `_poblarContenidoWizard`; limpieza en `_limpiarWizardCompleto`. La rama "solo formulario" (ADR-033) **NO lo persiste**.
    *   **(2) Sección de idioma (`#ld-i18n-config`) solo en paso 4 (TSK-037 / ADR-047):** se movió dentro del **único** `.wiz-step[data-wiz-step="4"]` (Blindaje), antes de `#btn-crear-ev`; dejó de mostrarse en los pasos 2 y 3. Sigue gateado por `toggleLandingConfig` (landing activa). Regresión evitada: un solo contenedor de paso 4 (verificado: `data-wiz-step="4"`=1, `id="ld-i18n-config"`=1).
    *   **(3) ADN Visual: preselección de módulos por template (`_THEME_MODULES`, TSK-038 / ADR-047):** mapa nuevo en `admin.html` junto a `_THEME_TPL` con **18 themes** (`requeridos` / `recomendados` / `selectivos` / `metaDosLineas`). Al seleccionar un template se **SOBRESCRIBE**: requeridos marcados+BLOQUEADOS (`disabled`), recomendados marcados, selectivos desmarcados; reset cross-categoría de TODOS los `.mods-tipo [id^="ld-mod-"]`; `metaDosLineas` gobierna `#ld-meta-hero-wrap` (solo `mistico`/f9 `true`). Modo edición con `{preserveModules:true}`: no se pisa `cfg.modules`; se re-bloquean requeridos y se recalcula el meta wrap.
*   **Limitaciones conocidas (documentadas, NO bugs):** (a) `registroaforo.html` (ruta solo-formulario) **NO consume `form_redirect`** por decisión de Dirección — el redirect solo aplica al landing de evento; (b) `evento.html` **NO lee `config_landing.modules`** (verificado: 0 ocurrencias de `modules` en el archivo): el preset ADN Visual es UX del Wizard, **no gobierna el render público** — no prometer lo contrario.
*   **Deuda MENOR derivada (TSK-039):** `#ld-form-redirect` usa `type="url"` **sin normalización de esquema** (normalizar/prefijar `https://` o validar antes de persistir); decidir si `registroaforo.html` consumirá `form_redirect`. Ver `DECISIONS.md` ADR-047 (Limitaciones + Pendientes).
*   **⚠️ Pendiente inmediato:** la entrega está en el working tree **sin commit** — Dirección debe commitearla. No hay migración SQL asociada (todo vive en el JSONB `config_landing.content`): `form_redirect` es opcional y aditivo.

#### -10. Hito Más Reciente — Ciclo Entrega 1+2+3 (ADR-044 / ADR-045 / ADR-046 — TSK-030 ✅ / TSK-034 ✅ / TSK-035 ⏳)
*   **Qué se entregó (working tree modificado, SIN commit) — QA APTO (Escudo GOLD PASS):**
    *   **(1) Catálogo de plantillas sin numeración + Registro del silo F11 (ADR-044):** se quitó la numeración `P#` de las 18 tarjetas `.ld-theme-card` de `admin.html` y del mapa `names` de `seleccionarTheme()` (solo NOMBRE + DESCRIPCIÓN; la numeración P9-P13 estaba duplicada entre Fiesta y Campaña). Se registró el silo `f11` como theme slug `fosforescente` (nombre "Fosforescente", desc "Neon fucsia · Hero 100svh · Mobile-first", categoría Fiesta): tarjeta nueva + `_THEME_TYPE['fosforescente']='fiesta'` + `_THEME_TPL['fosforescente']='f11'` + derivado `_TPL_THEME['f11']='fosforescente'` + `names`. `rico`/`f10` NO se registró (pendiente opcional heredado de ADR-042). **Reconciliación (ADR-006):** `f11.css` real es **v3.3.3 / 1595 líneas** (los docs lo tenían como v3.2.0 / 1554) — fila del silo y docs actualizados. NO se tocaron `evento.html`, `f7.html`, `f11.css` ni `f11gemini.css`.
    *   **(2) Implementación de ADR-040 — catálogo curado por la cuenta master (ADR-045):** ADR-040 pasa de "solo diseño / NO implementado" a **IMPLEMENTADO**. Migración nueva `migrations/adr040_config_global_templates.sql` (idempotente, ASCII 0 bytes>127, aditiva, 0 tablas existentes alteradas): tabla `public.config_global` (fila única `id='default'`, `templates jsonb`, `updated_at`, `updated_by`), helper `public.is_master_org_user()` (SECURITY DEFINER, espejo de `_IS_SYSTEM_ADMIN()`), RLS (SELECT authenticated; escritura solo master), GRANTs, `NOTIFY pgrst 'reload schema'`, rollback comentado, y la RPC nueva `public.fn_config_global_merge(p_templates jsonb)` SECURITY DEFINER que valida la cuenta master (RAISE EXCEPTION ERRCODE 42501), hace merge JSONB server-side (`COALESCE(templates,'{}') || p_templates`), sella `updated_at=now()`/`updated_by=auth.uid()` y devuelve la fila. Contrato JSONB canónico: `{ "version": 2, "hidden": [slugs], "order": [slugs] }` (ADR-040 documentaba v1 sin `order`; la Dirección pidió reordenar → enmienda registrada). `admin.html` implementa: CSS `.ld-theme-oculto`/`.ld-theme-legacy-forzado`, `_cargarConfigGlobal()` (read-through cache `htz_tpl_visibilidad`, fail-open, `console.warn` si `res.error`), `_guardarConfigGlobal(hidden, order)` vía la RPC (SIN comparación de timestamps), `_aplicarTemplatesOcultos()` (classList.toggle, JAMÁS `card.remove()`, + reorden `appendChild`), `_primerThemeVisible(tipo)`, guard `data-cat-empty`, sub-tab "Plantillas" en `pg-globaladmin` (extiende `gaTab()`) con MOSTRAR/OCULTAR/REORDENAR, bloqueo duro de catálogo en cero y aviso suave por categoría, revelación legacy no persistente al editar un evento con template oculto, y fallback de `toggleLandingConfig()` con `_primerThemeVisible('cinematografia')`. Caso borde: la página pública NO consulta `hidden` — ocultar es curación de catálogo/UX, NO seguridad, NO allowlist. **QA Escudo GOLD PASS:** `node --check` 6/6, ASCII 0 en 358 líneas nuevas, balance `<div>` 854/854, Cero Borrado intacto. Defectos A-H corregidos antes del cierre (detalle en `ERRORES_HISTORICOS.md` §13): A) bloqueo optimista roto por formato `timestamptz` (Z vs +00:00); B) éxito silencioso si RLS negaba; C) `updated_by` nunca se escribía; D) faltaba merge JSONB; E) lectura sin observabilidad; F) colisión de namespace `data-cat` (panel Plantillas vs Wizard → `data-tplcat`); G) mensaje de categoría vacía no filtrado; H) revelación legacy no limpiada al cancelar/crear.
    *   **(3) Hub normativo de templates + skill puente (ADR-046):** nuevo `Sistema QR desarrollo/AMPLIACION/TEMPLATES.md` (v1.3.0, 438 líneas): hub normativo con 10 capítulos + Brief obligatorio (9-bis) + Metodología de creación en 8 pasos (9-ter) + Anexo A (versiones), Anexo B (mapa de referencia) y Anexo C (inventario del DOM real ~81 IDs). Nueva skill `.opencode/skills/templates/SKILL.md` (skill puente que remite al hub). Incluye la advertencia Contrato v112 (21 átomos) vs DOM real (~81 IDs): solo 8 de 21 existen — no reportar los 12 restantes como bug.
*   **⚠️ Pendiente inmediato (TSK-035, Dirección/Lead Developer):** ejecutar manualmente `migrations/adr040_config_global_templates.sql` en Supabase (SQL Editor) y correr la **Prueba de Carga Dual** post-migración (ocultar/reordenar desde la cuenta master y confirmar persistencia en otra org; editar un evento con theme oculto y confirmar que conserva su `template_id`). Sin la tabla el front cae en fail-open (todo visible) — no bloquea. Las entregas de este ciclo están en el working tree **sin commit** — Dirección debe commitearlas.
*   **⚠️ ADR-040 ya NO está en "solo diseño":** la mención histórica del hito -7 queda superseded — ver hito -10 y `DECISIONS.md` ADR-045 / Enmienda I de ADR-040.

#### -9. Hito Más Reciente — Fix Mobile del Hero del Silo F11 "CYBERPUNK FOSFORESCENTE" (TSK-033 / ADR-043)
*   **Qué se entregó (working tree modificado, SIN commit):** corrección del bug visual del hero en mobile del silo **f11** (`css/templates/fiesta/f11.css` → v3.2.0, UNICO archivo tocado, 1544 → 1554 líneas). Bug reportado por el usuario: en mobile el hero mostraba arriba los elementos (countdown, meta, botón) y abajo el título. (1) **T1 — orden explícito en la media query 767px** con doble notación `.tpl-f11` + `.Tpl-F11` (aislamiento atómico): `#mod-hero`=1, `#unified-frame`=2, `#mod-hero-meta`=3, `#mod-hero-ctas`=4, resto de bloques re-numerado 5-15 — resultado visual aprobado: título → countdown → meta → botón. (2) **T2 — hero en UNA sola pantalla (proporción 60/40 aprobada):** `#mod-hero` con `min-height: 60svh` (fallback `60vh`), título `clamp(2.6rem, 11vw, 4.2rem)` en 767px y `clamp(2.2rem, 10vw, 3.4rem)` en 480px, `background-position: center center` en mobile (conservando `background-size: cover`), paddings/gaps compactados (countdown + meta + botón comprimidos en el ~40% restante del viewport, sin scroll vertical en el hero).
*   **QA ejecutado (Escudo GOLD — PASS):** balance de llaves 213/213 (diff 0), ASCII-safety (bytes >127 solo en comentarios históricos, no en reglas activas), desktop `min-height: 85vh !important` intacto, breakpoint 992-1279px intacto, sin media queries nuevas, Cero Borrado (sin IDs del Contrato v112 eliminados). `f11gemini.css` NO fue tocado (decisión explícita del usuario). Ver `ERRORES_HISTORICOS.md` §12 y `DECISIONS.md` ADR-043.
*   **⚠️ Pendiente inmediato:** la entrega está en el working tree **sin commit** — Dirección debe commitearla. Verificación visual final (Prueba de Carga Dual) del hero 60/40 en dispositivos reales (mobile y desktop), ya que la proporción es nueva y no hay referencia fotográfica en este ciclo.

#### -8. Cierre de Silo F10 "Rico" v2.0.0 (TSK-032 / ADR-042)
*   **Que se entrego (working tree modificado, SIN commit):** reescritura TOTAL solo-CSS del silo **f10** en UN SOLO archivo - `css/templates/fiesta/f10.css` (v2.0.0, 1801 lineas, 0 bytes > 127). Theme "Rico" (Cyberpunk Fosforescente), evento `rico-5mw5` en produccion (template f10, categoria fiesta - dato de Supabase, fuera del repo). NINGUN otro archivo del repo fue tocado. (1) **Lineup sobre selectores kernel reales:** `.artist-card*` (incl. `.headliner`), `.artist-photo`, `.artist-overlay`, `.artist-name`, `.artist-hora`, `.artist-resena` (line-clamp 2), `.artist-social`, `.artist-social-icon`, `.artist-social a` - el kernel JAMAS genera los `.f10-artist*`/`.f10-lc-*` de v1.0.0. (2) **Cero selectores muertos** (Cero Borrado verificado). (3) **Galeria `#db-gallery-grid`:** maqueta de espacios con grid de N abierto `repeat(auto-fill, minmax(...))` y placeholders glow (celda figure con marco fosforescente + gradiente neon, ADR-008). (4) **ASCII-safety estricto.** (5) **IoC conservada** + `prefers-reduced-motion` + tipografia Geist 900. (6) **Breakpoints reales** 640/767/768/992/1279.
*   **QA ejecutado (Escudo GOLD - PASS):** ASCII-safety 0 bytes > 127 (escaneo de bytes), balance de llaves, cero selectores muertos, Cero Borrado, Aislamiento Atomico `.tpl-f10`/`.Tpl-F10`. Cierre documental: ADR-042, plan `.opencode/plans/template-f10-rico.md`, log de versiones del pie actualizado (fila v2.0.0 canonica), PROJECT.md Seccion 6, TASKS.md TSK-032.
*   **Pendiente inmediato:** la entrega esta en el working tree **sin commit** - Direccion debe commitearla. Ademas, el theme **`rico` NO esta registrado en `admin.html`** (`_THEME_TYPE`/`_THEME_TPL` llegan hasta `mistico`->`f9`): hoy el silo aplica solo por `template_id='f10'` directo en Supabase; si Direccion quiere crearlo/editarlo por el Wizard, registrar `rico` -> `f10` (patron de ADR-041). Cargar fotos de artistas en `config_landing.content.dj_lineup` (pendiente heredado de F9).

#### -7. Hito Más Reciente — Entrega F9 "Místico" (TSK-029 / ADR-041)
*   **Qué se entregó (working tree modificado, SIN commit):** segunda iteración del silo **f9 "Místico / Rastro MC"** en 3 archivos — `css/templates/fiesta/f9.css` (log v1.1.0), `evento.html` y `admin.html`. (1) **Átomo nuevo `#mod-descripcion`** (HTML + render) que nace oculto por el kernel IoC y **solo lo enciende F9**; su fuente prioritaria es `effContent.descripcion || ev.descripcion`, con texto editorial de ejemplo del prelanzamiento del disco Místico de octubre como último fallback (fix H-1). (2) **Silo reorganizado:** se apagan `#mod-historia`, `#mod-objetivo`, `#mod-impacto` y `#mod-info-tecnica` (IDs intactos en el HTML, Cero Borrado) y se enciende `#mod-descripcion`; hero meta a 2 líneas (`.meta-line`); lineup desktop = headliner a la izquierda + 6 artistas en 2 filas de 3 (1 columna en mobile); grid desktop con `cartel|playlist` pareados; footer minimal (base f8) en dorado con sello `- rastro mc`; `.form-tabs` centrado; breakpoints portados de f8 (1279/991/767/640). (3) **Hero meta en 2 líneas solo para `tpl-f9`** vía `__metaSet`, con fallback a 1 línea en los demás silos (fix H-2). (4) **Boletería F9:** solo taquilla con precio real; preventa solo con precio real (oculta si vacía); módulo oculto si no hay ninguna tarifa — nunca pinta "POR DEFINIR". (5) **FAQ fallback** de 5 preguntas editoriales, exclusivo de `tpl-f9`. (6) **WhatsApp sin número en el texto** (cambio GLOBAL aprobado por el usuario; el `href` `wa.me` no cambia). (7) **Registro del theme `mistico` → `f9`** en `admin.html`: card `data-theme="mistico"` (P13) en Fiesta, `_THEME_TYPE`, `_THEME_TPL`, label en `names` e inputs de `content.meta` (fecha/hora/lugar `l1`/`l2`) con persistencia en `_buildConfigLanding()`, precarga y limpieza.
*   **Contrato de datos nuevo (opcional y aditivo):** `config_landing.content.meta = { fecha:{l1,l2}, hora:{l1,l2}, lugar:{l1,l2} }`; vacío o sin `l1` = línea única histórica.
*   **QA ejecutado (Escudo GOLD — PASS):** sintaxis JS (`node --check`), ASCII-safety de código, balance de divs/llaves/comentarios, Cero Borrado y grid. **H-1/H-2 corregidos**; **H-3** (FAQ de ejemplo hardcodeado para cualquier evento f9) y **H-4** (`formatWaPhone()` sin uso = código muerto) quedan **aceptados como deuda técnica (TSK-031)**. Ver `DECISIONS.md` ADR-041 y `.opencode/plans/template-f9-mistico.md` (v2).
*   **⚠️ Pendiente inmediato:** la entrega está en el working tree **sin commit** — Dirección debe commitearla. El **ADR-040 ya está IMPLEMENTADO** (hito -10 / ADR-045): la migración `adr040_config_global_templates.sql` existe y el front ya está escrito — queda únicamente **ejecutar la migración en Supabase** (TSK-035, Dirección) y la Prueba de Carga Dual; hasta entonces el front degrada a fail-open (todo visible). El theme `mistico` es forward-compatible (nace visible por defecto bajo la curacion de ADR-040/045).
*   **⚠️ Pendiente de datos (heredado de la iteración v1):** cargar fotos de cada artista en `config_landing.content.dj_lineup` (hoy solo `nombre`; sin foto se pierde la predominancia fotográfica del lineup de F9).

#### -6. Hito Más Reciente — Exportación de "Lista de Invitados" en Excel/PDF (TSK-027 / ADR-030)
*   **Qué se entregó:** dentro del tab **Asistentes** de `admin.html` se agregó la exportación de la **Lista de Invitados** en `.xlsx` (SheetJS) y `.pdf` (ventana auxiliar + `window.print()`), a partir de `prompt lista.txt`. El requerimiento original asumía columnas inexistentes (`tipo_asistente`, `invitador_codigo`); Dirección cerró el alcance a las columnas realmente necesarias: **#**, **Nombre** y **Cédula**.
*   **Alcance respetado:** sin consultas nuevas a Supabase — reutiliza la cache `_pnAllList` (cargada por `renderTabla()` según la vista evento/serie/general y el período activo) y aplica encima los filtros visibles del panel (búsqueda/tipo/estado/categoría) mediante `_filtrarInscritosPanel()`. Se añadió el filtro adicional "Solo asistencia" (`used=true`).
*   **Anti-duplicidad (Regla de No-Duplicidad):** se extrajeron `_leerFiltrosPanel()` y `_filtrarInscritosPanel()` (compartidos por la tabla y las exportaciones), se refactorizó `_renderTablaPagina()` para consumirlos, y `_xlsxInscritos()` reutiliza `_applyXlsxRowStyle()`. Nuevas funciones: `_etiquetaContextoPanel()`, `_buildInvitadosRows()`, `_generarXLSXInvitados()`, `_generarPDFInvitados()` y `exportarListaInvitados()`. UI: 4 ítems en `#export-menu` y controles (select de alcance + botones Excel/PDF) en el toolbar del tab Asistentes.
*   **Correcciones post-auditoría (qa-auditor):** marca correcta "Hostal Terraza" en el PDF; fallback de etiqueta de contexto para el rol `admin_evento` (M-1); tilde unificada "Cédula" (M-2); TRACE del Escudo GOLD en el PDF (R-1); `.filter(Boolean)` null-safety (R-2).
*   **QA ejecutado (Escudo GOLD — LIMPIA):** `node --check` exit 0 en ambos `<script>` inline; balance de `<div>` HTML puro 831/831 (baseline 830/830); funciones sin duplicados; smoke 25/25 con entradas `NULL` (ADR-008/ADR-024/ADR-029); scope global y multi-tenant correctos; Cero Borrado verificado contra `git show HEAD:admin.html` (ningún ID/función eliminado). Ver `DECISIONS.md` ADR-030.
*   **⚠️ Riesgo preexistente detectado (fuera de alcance, NO corregido):** `generarInforme()` (~L7900 de `admin.html`) consulta `SB.from('inscritos').select('*')` **sin** `.eq('org_id', ORG_ID)` (sus consultas vecinas de `clientes`/`logs` sí filtran por organización) → posible fuga cross-tenant del "Informe gráfico (PDF)". Queda como pendiente en TSK-028 y en Riesgos Activos (§4); relacionado con TSK-026.

#### -5. Hito Más Reciente — Auditoría Null Pointer Completa de admin.html (TSK-023 / ADR-029)
*   **Qué se hizo:** se extendió el barrido de ADR-024 (que se limitó a `_renderTablaPagina`/`renderPanel`) a **todo** `admin.html` buscando métodos de string sin blindar sobre campos de Supabase (`.replace`, `.split`, `.toUpperCase`, `.length`, `.includes`, `.slice`). Resultado: **11 puntos de crash reales corregidos** con el patrón Silent Fallback `(campo||'')` — ninguno había sido reportado todavía por un usuario, pero todos truenan con `TypeError` si el campo llega `null` (caso "Barrio R10" con inscritos incompletos).
*   **Dónde:** `renderEventos()` (2), `renderInvitadoresPerfil()` (1) + `abrirPerfilInvitador()` (2), `renderDirectorio()` (1), duplicados (2), `showVerify()` de Admin (1), `descargarInvitacion()` (1), export de eventos (1), `filtrarGlobalOrgs()`/gráficas globales (1), búsqueda global (1). Se blindaron además interpolaciones que renderizaban el texto literal `"null"` en las dos funciones del mandato (historial de eventos y perfil de invitador).
*   **QA ejecutado:** `node --check` sobre el JS inline (OK), prueba funcional aislada con casos `null` para los 13 patrones parcheados (13/13 OK) y balance de `<div>` 0 (760/760). Cero Borrado intacto — no se eliminó ningún ID/elemento, solo blindaje de interpolaciones.
*   **⚠️ Alcance restante señalado (no cubierto, fuera de mandato):** los archivos `index.html`, `qr.html`, `evento.html`, `evento3.html`, `registro.html`, `serie.html` no fueron auditados con este patrón. `scanner.html` ya quedó blindado parcialmente en TSK-005 (feed de últimos ingresos y `cnt-last`). Si Dirección prioriza, replicar el mismo barrido en `evento3.html` (motor público "El Cerebro", ~6K líneas) sería el siguiente candidato.

#### -4. Hito Anterior — Dashboard contextual portero (TSK-005)
*   **Qué se entregó:** se cerró el gap pendiente de "últimos 5 ingresos" en `scanner.html`. El contador vs aforo (número de ingresos, `de X inscritos · aforo Y`, barra de ocupación con estados verde/ámbar/rojo y badges ★/◎) ya existía en `updateCounter()`; la sección "Últimas entradas" solo reflejaba eventos de la sesión en curso vía `addLog()` y se resetaba en cada recarga. Se agregaron dos funciones: `seedUltimosIngresos()` (consulta `inscritos` con `used=true`, `order('hora_ingreso', desc)`, `limit 5`; fallback offline derivando desde `qrCache` filtrando `used=true`) y `renderUltimosIngresos()` (puebla `#sc-log` con los 5 ingresos más recientes reutilizando el markup `log-row` existente). La semilla se ejecuta una sola vez en `init()` tras `cargarCache()` para no pisar los logs de sesión (denegados/duplicados) en cada escaneo.
*   **Blindaje null (patrón ADR-024):** tanto `renderUltimosIngresos()` como `cnt-last` (que mostraba `null` literal si `nombre`/`hora_ingreso` estaban en `NULL`) usan Silent Fallback `(campo||'—')`. Relevante para el caso "Barrio R10" de inscritos incompletos.
*   **QA ejecutado:** `node --check` sobre el JS inline (sintaxis OK), prueba funcional aislada con casos null/vacío/top-5 de 7/orden offline (4/4 OK) y balance de `<div>` 0 (43 opens / 43 closes). No se modificó HTML estructural ni esquema SQL (Cero Borrado y Contrato v112 intactos).
*   **Nota de alcance (spec BLUEPRINT):** el "botón directo al scanner" descrito en la especificación no aplica en `scanner.html` — este archivo *es* el scanner; el portero llega vía link-token, no vía un login previo. Si en el futuro hay pantalla de login de porteros, esa parte de la spec se evaluaría aparte.

#### -3. Hito Anterior — Separación Master Admin vs. Barrio R10 (ADR-025)
*   **Qué se corrigió:** Barrio R10 (slug `hostal-terraza`) operaba a la vez como cliente regular y como identidad de System Admin global del SaaS, porque `_IS_SYSTEM_ADMIN()` y `applyRoleNav()` comparaban el slug directamente. Se reemplazó por una columna `organizaciones.is_master_org boolean`, se creó una org maestra nueva ("SaaS Master Admin", cáscara sin eventos propios) y Barrio R10 pasa a ser cliente regular (mismo `org_id`, solo cambia `slug` → `barrio-r10`). Corte directo, sin fallback temporal (mandato de Dirección). Ver `DECISIONS.md` ADR-025.
*   **Auditoría previa (antes de tocar código o SQL):** grep completo de `admin.html`, `index.html`, `qr.html`, `evento.html`, `evento3.html`, `registro.html`, `scanner.html`, `serie.html` — solo 3 referencias reales al slug hardcodeado, las 3 en `admin.html`. Se validó contra la exportación real de `pg_policies` (no contra suposición) que solo `insert_organizaciones`/`delete_organizaciones` dependían del slug a nivel de Postgres — `eventos`/`inscritos`/`perfiles` se aíslan por `org_id`, no por slug.
*   **⚠️ Hallazgo colateral, NO corregido en esta sesión (TSK-026):** la misma auditoría de `pg_policies` reveló políticas legacy permisivas en `eventos`, `clientes` y `perfiles` que conceden acceso cross-tenant a *cualquier* cliente autenticado (no solo a Barrio R10) porque conviven sin excluirse con las políticas correctas `org_id = get_org_id()`. Es un riesgo de seguridad más amplio que el problema original de Barrio R10, y requiere su propio Context Package — no se tocó porque estaba fuera del mandato explícito de esta sesión. Ver `DECISIONS.md` ADR-025, diagnóstico punto 3.
*   **Pendiente de ejecución (no ejecutable por la IA):** correr `migrations/adr025_master_admin_separation.sql` contra Supabase real (incluye pasos manuales de Dashboard para crear el nuevo login operativo de Barrio R10). Hasta que esto se ejecute, `admin.html` en producción seguiría funcionando igual que antes solo si la columna `is_master_org` no existe todavía — **desplegar el código de `admin.html` y ejecutar el SQL deben ir juntos**, no por separado, o la org maestra quedará sin acceso al Panel de Sistema.

#### -2. Hito Anterior — Fix "Null Pointer Regression" (ADR-024)
*   **Qué se corrigió:** el panel se bloqueaba por completo para organizaciones con registros de `inscritos` incompletos (caso "Barrio R10": `nombre`/`evento_nombre` en `NULL`), porque `_renderTablaPagina()` usaba `.replace()` sin blindar sobre esos campos. **Confirmado que no es regresión de ADR-023** — bug preexistente que esa organización disparó primero. Se corrigió con el patrón Silent Fallback (`(campo||'')`) y, preventivamente, se blindó también `evStats` en `renderPanel()` (mismo riesgo, aún no disparado). Log TRACE agregado. Ver `DECISIONS.md` ADR-024.
*   **Entregado además:** `migrations/adr024_audit_nulls_inscritos.sql` (solo lectura) para ubicar qué otros registros/organizaciones tienen campos incompletos — incluye nota de que algunos `NULL` en `inscritos` son legítimos por diseño (campos de Fase 2 de `b5`), no corregir a ciegas con `UPDATE` masivo.
*   **⚠️ Alcance no cubierto, pendiente de priorización (TSK-023):** el mismo patrón de riesgo podría existir en `renderEventos()`/`renderInvitadoresPerfil()` (Admin) y en el resto del archivo no auditado — el mandato de esta sesión se limitó explícitamente a `_renderTablaPagina` y subfunciones de `renderPanel`.

#### -1. Hito Anterior — Reorganización de Navegación de Panel (ADR-023)
*   **Qué se entregó:** Igual que ADR-022 pero para "Panel" — 4 pestañas internas (**Resumen** por defecto, Tendencias, Rankings, Asistentes) agrupando las 14 secciones de analítica que antes eran un único scroll. Controles globales (vista/período/exportar) siguen siempre visibles fuera de las pestañas. Ver `DECISIONS.md` ADR-023.
*   **Bug real detectado y corregido en la misma sesión:** la regla CSS que oculta pestañas por defecto solo cubría `[data-adm-tab]`; al reutilizar el patrón con el nuevo atributo `data-pnl-tab` (namespace separado a propósito para no interferir con Admin), las 4 pestañas de Panel se habrían mostrado todas a la vez. Corregido antes de entregar. Ver `ERRORES_HISTORICOS.md` §10 — lección: al reutilizar un patrón de "atributo + clase de estado compartida" para un segundo módulo, verificar que el selector CSS cubra el nuevo namespace de atributo, no solo el nombre de la clase.
*   **Nota para la siguiente sesión:** igual que con Admin, el orden visual de las 4 pestañas de Panel no corresponde al orden físico de los bloques en el archivo — ubicar por `id` o `data-pnl-tab`, no por posición aproximada.

#### 0. Hito Anterior — Reorganización de Navegación de Admin (ADR-022)
*   **Qué se entregó:** El módulo "Admin" (antes un único scroll continuo) ahora tiene 3 pestañas internas — **Historial** (vista por defecto), **+ Nuevo evento** (el Wizard de ADR-021), **Invitadores** — con contadores en cada pestaña. El Historial ganó título de sección (antes no tenía), buscador por nombre, filtro por categoría (Cine/Fiesta/Campaña) y separación Próximos/Pasados/Todos. "Nuevo asistente" (rol `admin_evento`) se reubicó dentro de Historial. Ver `DECISIONS.md` ADR-022.
*   **Regresión real detectada y corregida en la misma sesión:** `aplicarRestriccionesAdminEvento()` ocultaba la card de creación con un selector posicional (`#pg-admin > .wrap > .card`) que la reestructuración en pestañas rompió silenciosamente — corregido antes de entregar. Ver `ERRORES_HISTORICOS.md` §9. Lección para próximas sesiones: antes de envolver/reordenar HTML existente, grepear el archivo por selectores posicionales (`>`, `:nth-child`, `:first-child`) que apunten a esa región.
*   **Confirmación de Dirección:** el desarrollo activo del motor público se está haciendo desde `evento3.html` — dato relevante para TSK-017 (ver abajo), aunque no cierra formalmente la reconciliación con el link `evento.html` que sigue generando `admin.html`.

#### 0b. Hito Anterior — Wizard Inteligente de Creación (ADR-021)
*   **Qué se entregó:** `admin.html` reestructurado en un Wizard de 4 pasos (Cimiento, ADN Visual, Contenido, Blindaje) con Orquestador de Intención Cine/Fiesta/Campaña, modo "Captura Pura" (sin QR), armonías cromáticas vía TinyColor2 y fix de persistencia de `template_id`/`categoria_slug`. Ver `DECISIONS.md` ADR-021 y `ERRORES_HISTORICOS.md` §8 para el detalle completo (incluye dos hallazgos reales: columnas documentadas-pero-no-escritas y un bug de anidación `<div>` en el campo Aforo, ambos corregidos).
*   **⚠️ Nota para la siguiente sesión sobre `admin.html`:** el Wizard se implementó envolviendo el HTML existente **in situ** con `data-wiz-step="N"` en vez de reordenar físicamente el archivo — el orden visual de los 4 pasos en pantalla **no** corresponde al orden físico de los bloques en el archivo. Antes de editar cualquier campo del formulario de creación de eventos, ubicarlo por su `id` o por su `data-wiz-step`, no por su posición aproximada en el archivo.
*   **⚠️ No cerrado todavía:** TSK-016 (`evento.html` debe consumir `captura_pura`), TSK-017 (reconciliar `evento.html` vs. `evento3.html` como "El Cerebro" — ver Riesgos §4 abajo) y TSK-018 (ejecutar la migración SQL) siguen pendientes y son prioridad inmediata.

#### 1. Resumen del Punto de Control (Evolución v110 y Silo b5)
*   **Hito Sellado:** Integración exitosa del silo **b5 ("Emergencia Crítica")** y evolución formal al **Contrato de Datos v110**, expandiendo la capacidad a 20 átomos soberanos interactivos.
*   **Estado del Sistema:** **v1.6.1-GOLD / Contrato v112**. Certificación de la lógica **Dual-Phase** en `eventovenezuela.html` (Registro de Fase 1 + Perfilamiento in-place de Fase 2) como estándar de alta conversión.
*   **Arquitectura:** Validación operativa del modelo de silos atómicos y blindaje de datos mediante el control estricto de esquemas en Supabase.

#### 2. Instrucciones para la Siguiente IA / Sesión
Para mantener el control del proyecto y la trazabilidad, se debe seguir esta secuencia inquebrantable:
1.  **Carga de Contexto:** Leer obligatoriamente en este orden: `PROJECT.md` -> `BLUEPRINT.md` -> `TASKS.md` -> `DECISIONS.md` -> `NEXT.md`.
2.  **Soberanía de Silos:** Al operar sobre plantillas de campaña o emergencia, asegurar la correcta separación de estilos bajo el selector atómico correspondiente.
3.  **Invariabilidad del Cerebro:** Queda prohibido modificar el HTML base de orquestación. Toda reordenación visual debe gestionarse vía `grid-template-areas` en el CSS del silo.
4.  **Asunción de Roles:** Asumir estrictamente el rol asignado según la **Matriz de Capacidades de AI-DOS v1.2**.

#### 3. Próximos Objetivos Operativos (Sincronía con TASKS.md)
*   **Prioridad -1 (TSK-025, ⚠️ bloqueante — desplegar junto con el código, no por separado):** Ejecutar `migrations/adr025_master_admin_separation.sql` en Supabase. El código de `admin.html` ya asume que `is_master_org` existe — si se despliega el HTML sin correr el SQL primero, la org maestra se queda sin acceso al Panel de Sistema.
*   **Prioridad -1b (TSK-026, hallazgo de auditoría de ADR-025):** Evaluar y retirar las políticas RLS legacy permisivas en `eventos`/`clientes`/`perfiles` que permiten acceso cross-tenant a cualquier cliente autenticado — ver `DECISIONS.md` ADR-025, diagnóstico punto 3.
*   **Prioridad -1c (TSK-028, hallazgo de TSK-027):** Corregir `generarInforme()` (~L7900 de `admin.html`) para filtrar `inscritos` por `org_id` — mismo frente multi-tenant que TSK-026, detectado como riesgo preexistente durante el módulo de exportación y NO corregido. Ver `DECISIONS.md` ADR-030.
*   **Prioridad -1d (TSK-035 — ADR-040 IMPLEMENTADO, ejecución pendiente):** ejecutar `migrations/adr040_config_global_templates.sql` en Supabase + Prueba de Carga Dual. El código de `admin.html` y la migración **ya existen** (ADR-045, QA APTO): ocultar sin borrar (`.ld-theme-oculto`), reordenar (`order`), sub-tab Plantillas en `pg-globaladmin` con bloqueo duro en cero catálogo, RPC `fn_config_global_merge` de merge JSONB server-side. Sin la tabla, el front cae en fail-open (todo visible) — desplegar HTML y ejecutar SQL es seguro en cualquier orden, pero la feature solo se ve tras ejecutar la migración. Los themes `mistico`/`f9` y `fosforescente`/`f11` ya quedaron registrados (TSK-029/ADR-044) y nacen visibles por defecto. Ver `DECISIONS.md` ADR-040 (Enmienda I) y ADR-045; `ERRORES_HISTORICOS.md` §13.
*   **Prioridad -1e (TSK-031, deuda técnica de F9):** (H-3) acotar o configurar el FAQ de ejemplo hardcodeado para cualquier evento `tpl-f9`; (H-4) retirar o reutilizar `formatWaPhone()`, que quedó sin uso tras el cambio global de WhatsApp. Ver `DECISIONS.md` ADR-041.
*   **Prioridad -1f (TSK-039, deuda MENOR derivada de ADR-047):** (1) `#ld-form-redirect` usa `type="url"` sin normalización de esquema — normalizar (prefijar `https://` si falta) o validar antes de persistir; (2) decisión de Dirección sobre si `registroaforo.html` (ruta solo-formulario) debe consumir `form_redirect` (hoy NO lo hace). Ver `DECISIONS.md` ADR-047.
*   **Prioridad 0 (TSK-017, ⚠️ bloqueante para dar por cerrado ADR-021):** Reconciliar si `evento.html` (al que apunta el link público de `admin.html`) es el mismo motor que `evento3.html` ("El Cerebro" según `BLUEPRINT.md` §1) — de esto depende si `template_id`/`categoria_slug` tienen efecto visual real.
*   **Prioridad 0b (TSK-016, TSK-018):** Consumir `captura_pura` en `evento.html` y ejecutar `migrations/adr021_eventos_columns.sql` en Supabase.
*   **Prioridad 1 (TSK-010):** Verificación y despliegue del esquema de base de datos en Supabase para la tabla inscritos (incorporando las columnas críticas: `whatsapp`, `tipo_ayuda`, `autorizacion` y el contenedor `respuestas_custom` en JSONB).
*   **Prioridad 2 (TSK-011):** Despliegue físico del archivo `b5.css` en la ruta estandarizada `css/templates/campana/`.
*   **Prioridad 3:** Retomar las tareas estratégicas de monetización e infraestructura (`TSK-001` Integración Wompi y `TSK-002` Edge Function de webhook).

#### 4. Riesgos Activos y Restricciones a Preservar (STRICT)
*   **Identidad de System Admin (ADR-025):** desde este ADR, `organizaciones.is_master_org = true` es la única fuente de verdad para permisos de System Admin global — no un slug. No reintroducir comparaciones de slug hardcodeado en filtros RLS ni guardas de navegación. Barrio R10 (`slug: barrio-r10`) es cliente regular; la org maestra es `slug: master-admin`.
*   **RLS Legacy Cross-Tenant (TSK-026, nuevo):** hasta que se resuelva, cualquier cliente autenticado (no solo la org maestra) puede leer/escribir `eventos`/`clientes`/`perfiles` de otras organizaciones vía políticas permisivas legacy que no fueron retiradas al añadir las políticas `org_id = get_org_id()`. No asumir que el aislamiento multi-tenant está garantizado solo porque existen las políticas `_org` correctas — coexisten con las legacy.
*   **`generarInforme()` sin filtro `org_id` (TSK-028, nuevo):** riesgo preexistente detectado durante TSK-027 (fuera de alcance, NO corregido). El "Informe gráfico (PDF)" de `admin.html` (~L7900) consulta `SB.from('inscritos').select('*')` sin `.eq('org_id', ORG_ID)`, a diferencia de las consultas vecinas de `clientes`/`logs` que sí filtran por organización. Hasta sanearlo, el informe podría incluir inscritos de otras organizaciones. Mismo frente multi-tenant que TSK-026, pero a nivel de código (no de RLS). Ver `DECISIONS.md` ADR-030 (Pendientes).
*   **Contratos de configuración Wizard/Landing (ADR-047, nuevo):** `config_landing.content.form_redirect` es un contrato **aditivo y opcional** — sin él, `evento.html` cae a `comunidad_whatsapp` y si tampoco existe no redirige (Silent Fallback). No confundirlo con `content.whatsapp` (contacto/ventas). Dos límites a preservar mientras no se decida lo contrario: (a) `registroaforo.html` NO consume `form_redirect`; (b) `evento.html` NO lee `config_landing.modules` — el preset `_THEME_MODULES` es UX del Wizard y **no** gobierna la página pública. No prometer lo contrario ni "arreglarlo" sin ADR propio (ADR-006).
*   **Cumplimiento CSP de Vercel:** Prohibido introducir librerías que ejecuten `eval()` o `new Function()`. El estándar de referencia para QRs es `qr-creator`.
*   **Estabilidad de Supabase:** Mantener los websockets de **Realtime deshabilitados** para prevenir bloqueos en la interfaz; usar polling o reconsultas manuales.
*   **Asunciones de Base de Datos:** El núcleo de `eventovenezuela.html` asume la existencia de las nuevas columnas de la v110 en la tabla `inscritos`; cualquier desajuste interrumpirá la Fase 2 del flujo de conversión.
*   **Regla de Bloque (ADR-007):** Los módulos multimedia (#mod-video, #mod-lineup) deben conservar `display: block !important` para evitar el colapso visual tras la inyección JS[cite: 2].
*   **Tratamiento Geist 900:** Obligatoriedad de tipografía **Geist 900 de 3.2rem** con efectos Afterglow en todas las tarjetas de datos técnicos para el estándar de $10,000[cite: 6].
*   **Motor Público Ambiguo (ADR-021 / TSK-017):** `BLUEPRINT.md` declara `evento3.html` como "El Cerebro", pero `admin.html` enlaza a `evento.html` tras crear un evento. No modificar la lógica de generación de `pubLink` en `admin.html` hasta confirmar cuál de los dos es el motor vigente — un cambio a ciegas podría romper enlaces ya distribuidos.
*   **Dashboard portero (TSK-005):** la semilla de "últimos 5 ingresos" se ejecuta una sola vez en `init()`. No debe re-ejecutarse en cada `updateCounter()` (borraría los logs de sesión: denegados/duplicados). Los nuevos ingresos se reflejan arriba vía `addLog()`. Si se desea un feed auto-refrescante, debe evaluarse contra la Regla "Realtime deshabilitados" (usar polling puntual, no websockets).
*   **RLS `page_events_auth_read` permisiva sin `org_id` (ADR-050, nuevo):** la politica SELECT de `page_events` (ADR-032) queda con `USING true` (verificada por `pg_policies` en TSK-057). Las 4 vistas del panel (v_landing_analytics, v_landing_clicks_rank, v_landing_serie_diaria, v_landing_funnel) usan `WITH (security_invoker = true)` y `admin.html` filtra por `.eq('org_id', ORG_ID)` en las consultas (mitigacion a nivel de consulta), pero la politica subyacente permite a CUALQUIER authenticated leer filas de telemetria de TODAS las organizaciones. **Ajustar a restriccion por `org_id`** (patron `get_org_id()` + org master, ADR-025/036) en el barrido de tenancy (referencia **TSK-026**) — el cambio impacta directamente a las 4 vistas via `security_invoker` (se beneficiaran del filtro tras el cambio, sin re-crearlas). Hasta entonces: no exponer `page_events` a endpoints publicos, no agregar RPCs que devuelvan filas sin filtrar, y no dar por garantizado el aislamiento multi-tenant de la telemetria. Ver `DECISIONS.md` ADR-050 (Pendientes 2) y `TASKS.md` TSK-059.

---
*Este documento se actualiza bajo la Regla v127, garantizando la preservación total de la información histórica.*