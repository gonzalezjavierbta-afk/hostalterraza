# 🚀 NEXT.md — Estado de Relevo y Próximos Pasos (v1.19-FIX)

Este documento define el punto de control estratégico y las directrices para la continuación inmediata del desarrollo. Es la hoja de ruta obligatoria para cualquier IA o agente que retome el proyecto para asegurar la fidelidad de 1px y la integridad del núcleo.

---

#### -11. Hito Más Reciente — Contratos de Configuración del Wizard/Landing (ADR-047 — TSK-036 ✅ / TSK-037 ✅ / TSK-038 ✅ / TSK-039 ⏳)
*   **Qué se entregó (working tree modificado, SIN commit) — QA APTO (`@qa-auditor` sobre `admin.html` y `evento.html`):** cierre documental de TRES features de configuración del Wizard/landing que ya estaban implementadas y certificadas APTO, pero sin registro en la memoria de ingeniería. Ninguna de las tres estaba documentada en `DECISIONS.md`, `TASKS.md` ni `NEXT.md` antes de este cierre.
    *   **(1) Redirect dinámico del formulario del landing (`form_redirect`, TSK-036 / ADR-047):** contrato nuevo `config_landing.content.form_redirect` (string, URL genérica: WhatsApp / grupo / comunidad), **distinto** de `content.whatsapp` (contacto/ventas) y de `content.comunidad_whatsapp`. `evento.html` lo consume con retrocompatibilidad: `effContent.form_redirect || effContent.comunidad_whatsapp || ''`; si hay link y el usuario marcó "unirse a comunidad", redirige en el **MISMO tab** (`window.location.href`) tras insertar en `inscritos`; si está vacío/ausente, mantiene el mensaje de éxito (Silent Fallback, ADR-008). `admin.html`: input `#ld-form-redirect` (`type="url"`, junto a `#ld-whatsapp-tel`), persistido en `_buildConfigLanding` **solo cuando es truthy** (`delete` si vacío, para no pisar el valor previo); precarga en `_poblarContenidoWizard`; limpieza en `_limpiarWizardCompleto`. La rama "solo formulario" (ADR-033) **NO lo persiste**.
    *   **(2) Sección de idioma (`#ld-i18n-config`) solo en paso 4 (TSK-037 / ADR-047):** se movió dentro del **único** `.wiz-step[data-wiz-step="4"]` (Blindaje), antes de `#btn-crear-ev`; dejó de mostrarse en los pasos 2 y 3. Sigue gateado por `toggleLandingConfig` (landing activa). Regresión evitada: un solo contenedor de paso 4 (verificado: `data-wiz-step="4"`=1, `id="ld-i18n-config"`=1).
    *   **(3) ADN Visual: preselección de módulos por template (`_THEME_MODULES`, TSK-038 / ADR-047):** mapa nuevo en `admin.html` junto a `_THEME_TPL` con **18 themes** (`requeridos` / `recomendados` / `selectivos` / `metaDosLineas`). Al seleccionar un template se **SOBRESCRIBE**: requeridos marcados+BLOQUEADOS (`disabled`), recomendados marcados, selectivos desmarcados; reset cross-categoría de TODOS los `.mods-tipo [id^="ld-mod-"]`; `metaDosLineas` gobierna `#ld-meta-hero-wrap` (solo `mistico`/f9 `true`). Modo edición con `{preserveModules:true}`: no se pisa `cfg.modules`; se re-bloquean requeridos y se recalcula el meta wrap.
*   **Limitaciones conocidas (documentadas, NO bugs):** (a) `registroaforo.html` (ruta solo-formulario) **NO consume `form_redirect`** por decisión de Dirección — el redirect solo aplica al landing de evento; (b) `evento.html` **NO lee `config_landing.modules`** (verificado: 0 ocurrencias de `modules` en el archivo): el preset ADN Visual es UX del Wizard, **no gobierna el render público** — no prometer lo contrario.
*   **Deuda MENOR derivada (TSK-039):** `#ld-form-redirect` usa `type="url"` **sin normalización de esquema** (normalizar/prefijar `https://` o validar antes de persistir); decidir si `registroaforo.html` consumirá `form_redirect`. Ver `DECISIONS.md` ADR-047 (Limitaciones + Pendientes).
*   **⚠️ Pendiente inmediato:** la entrega está en el working tree **sin commit** — Dirección debe commitearla. No hay migración SQL asociada (todo vive en el JSONB `config_landing.content`): `form_redirect` es opcional y aditivo.

#### -10. Hito Más Reciente — Ciclo Entrega 1+2+3 (ADR-044 / ADR-045 / ADR-046 — TSK-030 ✅ / TSK-034 ✅ / TSK-035 ⏳)
*   **Qué se entregó (working tree modificado, SIN commit) — QA APTO (Escudo GOLD PASS):**
    *   **(1) Catálogo de plantillas sin numeración + Registro del silo F11 (ADR-044):** se quitó la numeración `P#` de las 18 tarjetas `.ld-theme-card` de `admin.html` y del mapa `names` de `seleccionarTheme()` (solo NOMBRE + DESCRIPCIÓN; la numeración P9-P13 estaba duplicada entre Fiesta y Campaña). Se registró el silo `f11` como theme slug `fosforescente` (nombre "Fosforescente", desc "Neon fucsia · Hero 100svh · Mobile-first", categoría Fiesta): tarjeta nueva + `_THEME_TYPE['fosforescente']='fiesta'` + `_THEME_TPL['fosforescente']='f11'` + derivado `_TPL_THEME['f11']='fosforescente'` + `names`. `rico`/`f10` NO se registró (pendiente opcional heredado de ADR-042). **Reconciliación (ADR-006):** `f11.css` real es **v3.3.3 / 1595 líneas** (los docs lo tenían como v3.2.0 / 1554) — fila del silo y docs actualizados. NO se tocaron `evento.html`, `f7.html`, `f11.css` ni `f11gemini.css`.
    *   **(2) Implementación de ADR-040 — catálogo curado por la cuenta master (ADR-045):** ADR-040 pasa de "solo diseño / NO implementado" a **IMPLEMENTADO**. Migración nueva `migrations/adr040_config_global_templates.sql` (idempotente, ASCII 0 bytes>127, aditiva, 0 tablas existentes alteradas): tabla `public.config_global` (fila única `id='default'`, `templates jsonb`, `updated_at`, `updated_by`), helper `public.is_master_org_user()` (SECURITY DEFINER, espejo de `_IS_SYSTEM_ADMIN()`), RLS (SELECT authenticated; escritura solo master), GRANTs, `NOTIFY pgrst 'reload schema'`, rollback comentado, y la RPC nueva `public.fn_config_global_merge(p_templates jsonb)` SECURITY DEFINER que valida la cuenta master (RAISE EXCEPTION ERRCODE 42501), hace merge JSONB server-side (`COALESCE(templates,'{}') || p_templates`), sella `updated_at=now()`/`updated_by=auth.uid()` y devuelve la fila. Contrato JSONB canónico: `{ "version": 2, "hidden": [slugs], "order": [slugs] }` (ADR-040 documentaba v1 sin `order`; la Dirección pidió reordenar → enmienda registrada). `admin.html` implementa: CSS `.ld-theme-oculto`/`.ld-theme-legacy-forzado`, `_cargarConfigGlobal()` (read-through cache `htz_tpl_visibilidad`, fail-open, `console.warn` si `res.error`), `_guardarConfigGlobal(hidden, order)` vía la RPC (SIN comparación de timestamps), `_aplicarTemplatesOcultos()` (classList.toggle, JAMÁS `card.remove()`, + reorden `appendChild`), `_primerThemeVisible(tipo)`, guard `data-cat-empty`, sub-tab "Plantillas" en `pg-globaladmin` (extiende `gaTab()`) con MOSTRAR/OCULTAR/REORDENAR, bloqueo duro de catálogo en cero y aviso suave por categoría, revelación legacy no persistente al editar un evento con template oculto, y fallback de `toggleLandingConfig()` con `_primerThemeVisible('cinematografia')`. Caso borde: la página pública NO consulta `hidden` — ocultar es curación de catálogo/UX, NO seguridad, NO allowlist. **QA Escudo GOLD PASS:** `node --check` 6/6, ASCII 0 en 358 líneas nuevas, balance `<div>` 854/854, Cero Borrado intacto. Defectos A-H corregidos antes del cierre (detalle en `ERRORES_HISTORICOS.md` §13): A) bloqueo optimista roto por formato `timestamptz` (Z vs +00:00); B) éxito silencioso si RLS negaba; C) `updated_by` nunca se escribía; D) faltaba merge JSONB; E) lectura sin observabilidad; F) colisión de namespace `data-cat` (panel Plantillas vs Wizard → `data-tplcat`); G) mensaje de categoría vacía no filtrado; H) revelación legacy no limpiada al cancelar/crear.
    *   **(3) Hub normativo de templates + skill puente (ADR-046):** nuevo `Sistema QR desarrollo/AMPLIACION/TEMPLATES.md` (v1.3.0, 438 líneas): hub normativo con 10 capítulos + Brief obligatorio (9-bis) + Metodología de creación en 8 pasos (9-ter) + Anexo A (versiones), Anexo B (mapa de referencia) y Anexo C (inventario del DOM real ~81 IDs). Nueva skill `.opencode/skills/templates/SKILL.md` (skill puente que remite al hub). Incluye la advertencia Contrato v112 (21 átomos) vs DOM real (~81 IDs): solo 8 de 21 existen — no reportar los 12 restantes como bug.
*   **⚠️ Pendiente inmediato (TSK-035, Dirección/Lead Developer):** ejecutar manualmente `migrations/adr040_config_global_templates.sql` en Supabase (SQL Editor) y correr la **Prueba de Carga Dual** post-migración (ocultar/reordenar desde la cuenta master y confirmar persistencia en otra org; editar un evento con theme oculto y confirmar que conserva su `template_id`). Sin la tabla el front cae en fail-open (todo visible) — no bloquea. Las entregas de este ciclo están en el working tree **sin commit** — Dirección debe commitearlas.
*   **⚠️ ADR-040 ya NO está en "solo diseño":** la mención histórica del hito -7 queda superseded — ver hito -10 y `DECISIONS.md` ADR-045 / Enmienda I de ADR-040.

#### -9. Hito Más Reciente — Fix Mobile del Hero del Silo F11 "CYBERPUNK FOSFORESCENTE" (TSK-033 / ADR-043)
*   **Qué se entregó (working tree modificado, SIN commit):** corrección del bug visual del hero en mobile del silo **f11** (`css/templates/fiesta/f11.css` → v3.2.0, UNICO archivo tocado, 1544 → 1554 líneas). Bug reportado por el usuario: en mobile el hero mostraba arriba los elementos (countdown, meta, botón) y abajo el título. (1) **T1 — orden explícito en la media query 767px** con doble notación `.tpl-f11` + `.Tpl-F11` (aislamiento atómico): `#mod-hero`=1, `#unified-frame`=2, `#mod-hero-meta`=3, `#mod-hero-ctas`=4, resto de bloques re-numerado 5-15 — resultado visual aprobado: título → countdown → meta → botón. (2) **T2 — hero en UNA sola pantalla (proporción 60/40 aprobada):** `#mod-hero` con `min-height: 60svh` (fallback `60vh`), título `clamp(2.6rem, 11vw, 4.2rem)` en 767px y `clamp(2.2rem, 10vw, 3.4rem)` en 480px, `background-position: center center` en mobile (conservando `background-size: cover`), paddings/gaps compactados (countdown + meta + botón comprimidos en el ~40% restante del viewport, sin scroll vertical en el hero).
*   **QA ejecutado (Escudo GOLD — PASS):** balance de llaves 213/213 (diff 0), ASCII-safety (bytes >127 solo en comentarios históricos, no en reglas activas), desktop `min-height: 85vh !important` intacto, breakpoint 992-1279px intacto, sin media queries nuevas, Cero Borrado (sin IDs del Contrato v112 eliminados). `f11gemini.css` NO fue tocado (decisión explícita del usuario). Ver `ERRORES_HISTORICOS.md` §12 y `DECISIONS.md` ADR-043.
*   **⚠️ Pendiente inmediato:** la entrega está en el working tree **sin commit** — Dirección debe commitearla. Verificación visual final (Prueba de Carga Dual) del hero 60/40 en dispositivos reales (mobile y desktop), ya que la proporción es nueva y no hay referencia fotográfica en este ciclo.

#### -8. Cierre de Silo F10 "Rico" v2.0.0 (TSK-032 / ADR-042)
*   **Que se entrego (working tree modificado, SIN commit):** reescritura TOTAL solo-CSS del silo **f10** en UN SOLO archivo - `css/templates/fiesta/f10.css` (v2.0.0, 1801 lineas, 0 bytes > 127). Theme "Rico" (Cyberpunk Fosforescente), evento `rico-5mw5` en produccion (template f10, categoria fiesta - dato de Supabase, fuera del repo). NINGUN otro archivo del repo fue tocado. (1) **Lineup sobre selectores kernel reales:** `.artist-card*` (incl. `.headliner`), `.artist-photo`, `.artist-overlay`, `.artist-name`, `.artist-hora`, `.artist-resena` (line-clamp 2), `.artist-social`, `.artist-social-icon`, `.artist-social a` - el kernel JAMAS genera los `.f10-artist*`/`.f10-lc-*` de v1.0.0. (2) **Cero selectores muertos** (Cero Borrado verificado). (3) **Galeria `#db-gallery-grid`:** maqueta de espacios con grid de N abierto `repeat(auto-fill, minmax(...))` y placeholders glow (celda figure con marco fosforescente + gradiente neon, ADR-008). (4) **ASCII-safety estricto.** (5) **IoC conservada** + `prefers-reduced-motion` + tipografia Geist 900. (6) **Breakpoints reales** 640/767/768/992/1279.
*   **QA ejecutado (Escudo GOLD - PASS):** ASCII-safety 0 bytes > 127 (escaneo de bytes), balance de llaves, cero selectores muertos, Cero Borrado, Aislamiento Atomico `.tpl-f10`/`.Tpl-F10`. Cierre documental: ADR-042, plan `.opencode/plans/template-f10-rico.md`, log de versiones del pie actualizado (fila v2.0.0 canonica), PROJECT.md Seccion 6, TASKS.md TSK-032.
*   **Pendiente inmediato:** la entrega esta en el working tree **sin commit** - Direccion debe commitearla. Ademas, el theme **`rico` NO esta registrado en `admin.html`** (`_THEME_TYPE`/`_THEME_TPL` llegan hasta `mistico`->`f9`): hoy el silo aplica solo por `template_id='f10'` directo en Supabase; si Direccion quiere crearlo/editarlo por el Wizard, registrar `rico` -> `f10` (patron de ADR-041). Cargar fotos de artistas en `config_landing.content.dj_lineup` (pendiente heredado de F9).

#### -7. Hito Más Reciente — Entrega F9 "Místico" (TSK-029 / ADR-041)
*   **Qué se entregó (working tree modificado, SIN commit):** segunda iteración del silo **f9 "Místico / Rastro MC"** en 3 archivos — `css/templates/fiesta/f9.css` (log v1.1.0), `evento.html` y `admin.html`. (1) **Átomo nuevo `#mod-descripcion`** (HTML + render) que nace oculto por el kernel IoC y **solo lo enciende F9**; su fuente prioritaria es `effContent.descripcion || ev.descripcion`, con texto editorial de ejemplo del prelanzamiento del disco Místico de octubre como último fallback (fix H-1). (2) **Silo reorganizado:** se apagan `#mod-historia`, `#mod-objetivo`, `#mod-impacto` y `#mod-info-tecnica` (IDs intactos en el HTML, Cero Borrado) y se enciende `#mod-descripcion`; hero meta a 2 líneas (`.meta-line`); lineup desktop = headliner a la izquierda + 6 artistas en 2 filas de 3 (1 columna en mobile); grid desktop con `cartel|playlist` pareados; footer minimal (base f8) en dorado con sello `- rastro mc`; `.form-tabs` centrado; breakpoints portados de f8 (1279/991/767/640). (3) **Hero meta en 2 líneas solo para `tpl-f9`** vía `__metaSet`, con fallback a 1 línea en los demás silos (fix H-2). (4) **Boletería F9:** solo taquilla con precio real; preventa solo con precio real (oculta si vacía); módulo oculto si no hay ninguna tarifa — nunca pinta "POR DEFINIR". (5) **FAQ fallback** de 5 preguntas editoriales, exclusivo de `tpl-f9`. (6) **WhatsApp sin número en el texto** (cambio GLOBAL aprobado por el usuario; el `href` `wa.me` no cambia). (7) **Registro del theme `mistico` → `f9`** en `admin.html`: card `data-theme="mistico"` (P13) en Fiesta, `_THEME_TYPE`, `_THEME_TPL`, label en `names` e inputs de `content.meta` (fecha/hora/lugar `l1`/`l2`) con persistencia en `_buildConfigLanding()`, precarga y limpieza.
*   **Contrato de datos nuevo (opcional y aditivo):** `config_landing.content.meta = { fecha:{l1,l2}, hora:{l1,l2}, lugar:{l1,l2} }`; vacío o sin `l1` = línea única histórica.
*   **QA ejecutado (Escudo GOLD — PASS):** sintaxis JS (`node --check`), ASCII-safety de código, balance de divs/llaves/comentarios, Cero Borrado y grid. **H-1/H-2 corregidos**; **H-3** (FAQ de ejemplo hardcodeado para cualquier evento f9) y **H-4** (`formatWaPhone()` sin uso = código muerto) quedan **aceptados como deuda técnica (TSK-031)**. Ver `DECISIONS.md` ADR-041 y `.opencode/plans/template-f9-mistico.md` (v2).
*   **⚠️ Pendiente inmediato:** la entrega está en el working tree **sin commit** — Dirección debe commitearla. El **ADR-040 ya está IMPLEMENTADO** (hito -10 / ADR-045): la migración `adr040_config_global_templates.sql` existe y el front ya está escrito — queda únicamente **ejecutar la migración en Supabase** (TSK-035, Dirección) y la Prueba de Carga Dual; hasta entonces el front degrada a fail-open (todo visible). El theme `mistico` es forward-compatible (nace visible por defecto bajo la curacion de ADR-040/045).
*   **⚠️ Pendiente de datos (heredado de la iteración v1):** cargar fotos de cada artista en `config_landing.content.dj_lineup` (hoy solo `nombre`; sin foto se pierde la predominancia fotográfica del lineup de F9).

#### -6. Hito Más Reciente — Exportación de "Lista de Invitados" en Excel/PDF (TSK-027 / ADR-030)
*   **Qué se entregó:** dentro del tab **Asistentes** de `admin.html` se agregó la exportación de la **Lista de Invitados** en `.xlsx` (SheetJS) y `.pdf` (ventana auxiliar + `window.print()`), a partir de `prompt lista.txt`. El requerimiento original asumía columnas inexistentes (`tipo_asistente`, `invitador_codigo`); Dirección cerró el alcance a las columnas realmente necesarias: **#**, **Nombre** y **Cédula**.
*   **Alcance respetado:** sin consultas nuevas a Supabase — reutiliza la cache `_pnAllList` (cargada por `renderTabla()` según la vista evento/serie/general y el período activo) y aplica encima los filtros visibles del panel (búsqueda/tipo/estado/categoría) mediante `_filtrarInscritosPanel()`. Se añadió el filtro adicional "Solo asistencia" (`used=true`).
*   **Anti-duplicidad (Regla de No-Duplicidad):** se extrajeron `_leerFiltrosPanel()` y `_filtrarInscritosPanel()` (compartidos por la tabla y las exportaciones), se refactorizó `_renderTablaPagina()` para consumirlos, y `_xlsxInscritos()` reutiliza `_applyXlsxRowStyle()`. Nuevas funciones: `_etiquetaContextoPanel()`, `_buildInvitadosRows()`, `_generarXLSXInvitados()`, `_generarPDFInvitados()` y `exportarListaInvitados()`. UI: 4 ítems en `#export-menu` y controles (select de alcance + botones Excel/PDF) en el toolbar del tab Asistentes.
*   **Correcciones post-auditoría (qa-auditor):** marca correcta "Hostal Terraza" en el PDF; fallback de etiqueta de contexto para el rol `admin_evento` (M-1); tilde unificada "Cédula" (M-2); TRACE del Escudo GOLD en el PDF (R-1); `.filter(Boolean)` null-safety (R-2).
*   **QA ejecutado (Escudo GOLD — LIMPIA):** `node --check` exit 0 en ambos `<script>` inline; balance de `<div>` HTML puro 831/831 (baseline 830/830); funciones sin duplicados; smoke 25/25 con entradas `NULL` (ADR-008/ADR-024/ADR-029); scope global y multi-tenant correctos; Cero Borrado verificado contra `git show HEAD:admin.html` (ningún ID/función eliminado). Ver `DECISIONS.md` ADR-030.
*   **⚠️ Riesgo preexistente detectado (fuera de alcance, NO corregido):** `generarInforme()` (~L7900 de `admin.html`) consulta `SB.from('inscritos').select('*')` **sin** `.eq('org_id', ORG_ID)` (sus consultas vecinas de `clientes`/`logs` sí filtran por organización) → posible fuga cross-tenant del "Informe gráfico (PDF)". Queda como pendiente en TSK-028 y en Riesgos Activos (§4); relacionado con TSK-026.

#### -5. Hito Más Reciente — Auditoría Null Pointer Completa de admin.html (TSK-023 / ADR-029)
*   **Qué se hizo:** se extendió el barrido de ADR-024 (que se limitó a `_renderTablaPagina`/`renderPanel`) a **todo** `admin.html` buscando métodos de string sin blindar sobre campos de Supabase (`.replace`, `.split`, `.toUpperCase`, `.length`, `.includes`, `.slice`). Resultado: **11 puntos de crash reales corregidos** con el patrón Silent Fallback `(campo||'')` — ninguno había sido reportado todavía por un usuario, pero todos truenan con `TypeError` si el campo llega `null` (caso "Barrio R10" con inscritos incompletos).
*   **Dónde:** `renderEventos()` (2), `renderInvitadoresPerfil()` (1) + `abrirPerfilInvitador()` (2), `renderDirectorio()` (1), duplicados (2), `showVerify()` de Admin (1), `descargarInvitacion()` (1), export de eventos (1), `filtrarGlobalOrgs()`/gráficas globales (1), búsqueda global (1). Se blindaron además interpolaciones que renderizaban el texto literal `"null"` en las dos funciones del mandato (historial de eventos y perfil de invitador).
*   **QA ejecutado:** `node --check` sobre el JS inline (OK), prueba funcional aislada con casos `null` para los 13 patrones parcheados (13/13 OK) y balance de `<div>` 0 (760/760). Cero Borrado intacto — no se eliminó ningún ID/elemento, solo blindaje de interpolaciones.
*   **⚠️ Alcance restante señalado (no cubierto, fuera de mandato):** los archivos `index.html`, `qr.html`, `evento.html`, `evento3.html`, `registro.html`, `serie.html` no fueron auditados con este patrón. `scanner.html` ya quedó blindado parcialmente en TSK-005 (feed de últimos ingresos y `cnt-last`). Si Dirección prioriza, replicar el mismo barrido en `evento3.html` (motor público "El Cerebro", ~6K líneas) sería el siguiente candidato.

#### -4. Hito Anterior — Dashboard contextual portero (TSK-005)
*   **Qué se entregó:** se cerró el gap pendiente de "últimos 5 ingresos" en `scanner.html`. El contador vs aforo (número de ingresos, `de X inscritos · aforo Y`, barra de ocupación con estados verde/ámbar/rojo y badges ★/◎) ya existía en `updateCounter()`; la sección "Últimas entradas" solo reflejaba eventos de la sesión en curso vía `addLog()` y se resetaba en cada recarga. Se agregaron dos funciones: `seedUltimosIngresos()` (consulta `inscritos` con `used=true`, `order('hora_ingreso', desc)`, `limit 5`; fallback offline derivando desde `qrCache` filtrando `used=true`) y `renderUltimosIngresos()` (puebla `#sc-log` con los 5 ingresos más recientes reutilizando el markup `log-row` existente). La semilla se ejecuta una sola vez en `init()` tras `cargarCache()` para no pisar los logs de sesión (denegados/duplicados) en cada escaneo.
*   **Blindaje null (patrón ADR-024):** tanto `renderUltimosIngresos()` como `cnt-last` (que mostraba `null` literal si `nombre`/`hora_ingreso` estaban en `NULL`) usan Silent Fallback `(campo||'—')`. Relevante para el caso "Barrio R10" de inscritos incompletos.
*   **QA ejecutado:** `node --check` sobre el JS inline (sintaxis OK), prueba funcional aislada con casos null/vacío/top-5 de 7/orden offline (4/4 OK) y balance de `<div>` 0 (43 opens / 43 closes). No se modificó HTML estructural ni esquema SQL (Cero Borrado y Contrato v112 intactos).
*   **Nota de alcance (spec BLUEPRINT):** el "botón directo al scanner" descrito en la especificación no aplica en `scanner.html` — este archivo *es* el scanner; el portero llega vía link-token, no vía un login previo. Si en el futuro hay pantalla de login de porteros, esa parte de la spec se evaluaría aparte.

#### -3. Hito Anterior — Separación Master Admin vs. Barrio R10 (ADR-025)
*   **Qué se corrigió:** Barrio R10 (slug `hostal-terraza`) operaba a la vez como cliente regular y como identidad de System Admin global del SaaS, porque `_IS_SYSTEM_ADMIN()` y `applyRoleNav()` comparaban el slug directamente. Se reemplazó por una columna `organizaciones.is_master_org boolean`, se creó una org maestra nueva ("SaaS Master Admin", cáscara sin eventos propios) y Barrio R10 pasa a ser cliente regular (mismo `org_id`, solo cambia `slug` → `barrio-r10`). Corte directo, sin fallback temporal (mandato de Dirección). Ver `DECISIONS.md` ADR-025.
*   **Auditoría previa (antes de tocar código o SQL):** grep completo de `admin.html`, `index.html`, `qr.html`, `evento.html`, `evento3.html`, `registro.html`, `scanner.html`, `serie.html` — solo 3 referencias reales al slug hardcodeado, las 3 en `admin.html`. Se validó contra la exportación real de `pg_policies` (no contra suposición) que solo `insert_organizaciones`/`delete_organizaciones` dependían del slug a nivel de Postgres — `eventos`/`inscritos`/`perfiles` se aíslan por `org_id`, no por slug.
*   **⚠️ Hallazgo colateral, NO corregido en esta sesión (TSK-026):** la misma auditoría de `pg_policies` reveló políticas legacy permisivas en `eventos`, `clientes` y `perfiles` que conceden acceso cross-tenant a *cualquier* cliente autenticado (no solo a Barrio R10) porque conviven sin excluirse con las políticas correctas `org_id = get_org_id()`. Es un riesgo de seguridad más amplio que el problema original de Barrio R10, y requiere su propio Context Package — no se tocó porque estaba fuera del mandato explícito de esta sesión. Ver `DECISIONS.md` ADR-025, diagnóstico punto 3.
*   **Pendiente de ejecución (no ejecutable por la IA):** correr `migrations/adr025_master_admin_separation.sql` contra Supabase real (incluye pasos manuales de Dashboard para crear el nuevo login operativo de Barrio R10). Hasta que esto se ejecute, `admin.html` en producción seguiría funcionando igual que antes solo si la columna `is_master_org` no existe todavía — **desplegar el código de `admin.html` y ejecutar el SQL deben ir juntos**, no por separado, o la org maestra quedará sin acceso al Panel de Sistema.

#### -2. Hito Anterior — Fix "Null Pointer Regression" (ADR-024)
*   **Qué se corrigió:** el panel se bloqueaba por completo para organizaciones con registros de `inscritos` incompletos (caso "Barrio R10": `nombre`/`evento_nombre` en `NULL`), porque `_renderTablaPagina()` usaba `.replace()` sin blindar sobre esos campos. **Confirmado que no es regresión de ADR-023** — bug preexistente que esa organización disparó primero. Se corrigió con el patrón Silent Fallback (`(campo||'')`) y, preventivamente, se blindó también `evStats` en `renderPanel()` (mismo riesgo, aún no disparado). Log TRACE agregado. Ver `DECISIONS.md` ADR-024.
*   **Entregado además:** `migrations/adr024_audit_nulls_inscritos.sql` (solo lectura) para ubicar qué otros registros/organizaciones tienen campos incompletos — incluye nota de que algunos `NULL` en `inscritos` son legítimos por diseño (campos de Fase 2 de `b5`), no corregir a ciegas con `UPDATE` masivo.
*   **⚠️ Alcance no cubierto, pendiente de priorización (TSK-023):** el mismo patrón de riesgo podría existir en `renderEventos()`/`renderInvitadoresPerfil()` (Admin) y en el resto del archivo no auditado — el mandato de esta sesión se limitó explícitamente a `_renderTablaPagina` y subfunciones de `renderPanel`.

#### -1. Hito Anterior — Reorganización de Navegación de Panel (ADR-023)
*   **Qué se entregó:** Igual que ADR-022 pero para "Panel" — 4 pestañas internas (**Resumen** por defecto, Tendencias, Rankings, Asistentes) agrupando las 14 secciones de analítica que antes eran un único scroll. Controles globales (vista/período/exportar) siguen siempre visibles fuera de las pestañas. Ver `DECISIONS.md` ADR-023.
*   **Bug real detectado y corregido en la misma sesión:** la regla CSS que oculta pestañas por defecto solo cubría `[data-adm-tab]`; al reutilizar el patrón con el nuevo atributo `data-pnl-tab` (namespace separado a propósito para no interferir con Admin), las 4 pestañas de Panel se habrían mostrado todas a la vez. Corregido antes de entregar. Ver `ERRORES_HISTORICOS.md` §10 — lección: al reutilizar un patrón de "atributo + clase de estado compartida" para un segundo módulo, verificar que el selector CSS cubra el nuevo namespace de atributo, no solo el nombre de la clase.
*   **Nota para la siguiente sesión:** igual que con Admin, el orden visual de las 4 pestañas de Panel no corresponde al orden físico de los bloques en el archivo — ubicar por `id` o `data-pnl-tab`, no por posición aproximada.

#### 0. Hito Anterior — Reorganización de Navegación de Admin (ADR-022)
*   **Qué se entregó:** El módulo "Admin" (antes un único scroll continuo) ahora tiene 3 pestañas internas — **Historial** (vista por defecto), **+ Nuevo evento** (el Wizard de ADR-021), **Invitadores** — con contadores en cada pestaña. El Historial ganó título de sección (antes no tenía), buscador por nombre, filtro por categoría (Cine/Fiesta/Campaña) y separación Próximos/Pasados/Todos. "Nuevo asistente" (rol `admin_evento`) se reubicó dentro de Historial. Ver `DECISIONS.md` ADR-022.
*   **Regresión real detectada y corregida en la misma sesión:** `aplicarRestriccionesAdminEvento()` ocultaba la card de creación con un selector posicional (`#pg-admin > .wrap > .card`) que la reestructuración en pestañas rompió silenciosamente — corregido antes de entregar. Ver `ERRORES_HISTORICOS.md` §9. Lección para próximas sesiones: antes de envolver/reordenar HTML existente, grepear el archivo por selectores posicionales (`>`, `:nth-child`, `:first-child`) que apunten a esa región.
*   **Confirmación de Dirección:** el desarrollo activo del motor público se está haciendo desde `evento3.html` — dato relevante para TSK-017 (ver abajo), aunque no cierra formalmente la reconciliación con el link `evento.html` que sigue generando `admin.html`.

#### 0b. Hito Anterior — Wizard Inteligente de Creación (ADR-021)
*   **Qué se entregó:** `admin.html` reestructurado en un Wizard de 4 pasos (Cimiento, ADN Visual, Contenido, Blindaje) con Orquestador de Intención Cine/Fiesta/Campaña, modo "Captura Pura" (sin QR), armonías cromáticas vía TinyColor2 y fix de persistencia de `template_id`/`categoria_slug`. Ver `DECISIONS.md` ADR-021 y `ERRORES_HISTORICOS.md` §8 para el detalle completo (incluye dos hallazgos reales: columnas documentadas-pero-no-escritas y un bug de anidación `<div>` en el campo Aforo, ambos corregidos).
*   **⚠️ Nota para la siguiente sesión sobre `admin.html`:** el Wizard se implementó envolviendo el HTML existente **in situ** con `data-wiz-step="N"` en vez de reordenar físicamente el archivo — el orden visual de los 4 pasos en pantalla **no** corresponde al orden físico de los bloques en el archivo. Antes de editar cualquier campo del formulario de creación de eventos, ubicarlo por su `id` o por su `data-wiz-step`, no por su posición aproximada en el archivo.
*   **⚠️ No cerrado todavía:** TSK-016 (`evento.html` debe consumir `captura_pura`), TSK-017 (reconciliar `evento.html` vs. `evento3.html` como "El Cerebro" — ver Riesgos §4 abajo) y TSK-018 (ejecutar la migración SQL) siguen pendientes y son prioridad inmediata.

#### 1. Resumen del Punto de Control (Evolución v110 y Silo b5)
*   **Hito Sellado:** Integración exitosa del silo **b5 ("Emergencia Crítica")** y evolución formal al **Contrato de Datos v110**, expandiendo la capacidad a 20 átomos soberanos interactivos.
*   **Estado del Sistema:** **v1.6.1-GOLD / Contrato v112**. Certificación de la lógica **Dual-Phase** en `eventovenezuela.html` (Registro de Fase 1 + Perfilamiento in-place de Fase 2) como estándar de alta conversión.
*   **Arquitectura:** Validación operativa del modelo de silos atómicos y blindaje de datos mediante el control estricto de esquemas en Supabase.

#### 2. Instrucciones para la Siguiente IA / Sesión
Para mantener el control del proyecto y la trazabilidad, se debe seguir esta secuencia inquebrantable:
1.  **Carga de Contexto:** Leer obligatoriamente en este orden: `PROJECT.md` -> `BLUEPRINT.md` -> `TASKS.md` -> `DECISIONS.md` -> `NEXT.md`.
2.  **Soberanía de Silos:** Al operar sobre plantillas de campaña o emergencia, asegurar la correcta separación de estilos bajo el selector atómico correspondiente.
3.  **Invariabilidad del Cerebro:** Queda prohibido modificar el HTML base de orquestación. Toda reordenación visual debe gestionarse vía `grid-template-areas` en el CSS del silo.
4.  **Asunción de Roles:** Asumir estrictamente el rol asignado según la **Matriz de Capacidades de AI-DOS v1.2**.

#### 3. Próximos Objetivos Operativos (Sincronía con TASKS.md)
*   **Prioridad -1 (TSK-025, ⚠️ bloqueante — desplegar junto con el código, no por separado):** Ejecutar `migrations/adr025_master_admin_separation.sql` en Supabase. El código de `admin.html` ya asume que `is_master_org` existe — si se despliega el HTML sin correr el SQL primero, la org maestra se queda sin acceso al Panel de Sistema.
*   **Prioridad -1b (TSK-026, hallazgo de auditoría de ADR-025):** Evaluar y retirar las políticas RLS legacy permisivas en `eventos`/`clientes`/`perfiles` que permiten acceso cross-tenant a cualquier cliente autenticado — ver `DECISIONS.md` ADR-025, diagnóstico punto 3.
*   **Prioridad -1c (TSK-028, hallazgo de TSK-027):** Corregir `generarInforme()` (~L7900 de `admin.html`) para filtrar `inscritos` por `org_id` — mismo frente multi-tenant que TSK-026, detectado como riesgo preexistente durante el módulo de exportación y NO corregido. Ver `DECISIONS.md` ADR-030.
*   **Prioridad -1d (TSK-035 — ADR-040 IMPLEMENTADO, ejecución pendiente):** ejecutar `migrations/adr040_config_global_templates.sql` en Supabase + Prueba de Carga Dual. El código de `admin.html` y la migración **ya existen** (ADR-045, QA APTO): ocultar sin borrar (`.ld-theme-oculto`), reordenar (`order`), sub-tab Plantillas en `pg-globaladmin` con bloqueo duro en cero catálogo, RPC `fn_config_global_merge` de merge JSONB server-side. Sin la tabla, el front cae en fail-open (todo visible) — desplegar HTML y ejecutar SQL es seguro en cualquier orden, pero la feature solo se ve tras ejecutar la migración. Los themes `mistico`/`f9` y `fosforescente`/`f11` ya quedaron registrados (TSK-029/ADR-044) y nacen visibles por defecto. Ver `DECISIONS.md` ADR-040 (Enmienda I) y ADR-045; `ERRORES_HISTORICOS.md` §13.
*   **Prioridad -1e (TSK-031, deuda técnica de F9):** (H-3) acotar o configurar el FAQ de ejemplo hardcodeado para cualquier evento `tpl-f9`; (H-4) retirar o reutilizar `formatWaPhone()`, que quedó sin uso tras el cambio global de WhatsApp. Ver `DECISIONS.md` ADR-041.
*   **Prioridad -1f (TSK-039, deuda MENOR derivada de ADR-047):** (1) `#ld-form-redirect` usa `type="url"` sin normalización de esquema — normalizar (prefijar `https://` si falta) o validar antes de persistir; (2) decisión de Dirección sobre si `registroaforo.html` (ruta solo-formulario) debe consumir `form_redirect` (hoy NO lo hace). Ver `DECISIONS.md` ADR-047.
*   **Prioridad 0 (TSK-017, ⚠️ bloqueante para dar por cerrado ADR-021):** Reconciliar si `evento.html` (al que apunta el link público de `admin.html`) es el mismo motor que `evento3.html` ("El Cerebro" según `BLUEPRINT.md` §1) — de esto depende si `template_id`/`categoria_slug` tienen efecto visual real.
*   **Prioridad 0b (TSK-016, TSK-018):** Consumir `captura_pura` en `evento.html` y ejecutar `migrations/adr021_eventos_columns.sql` en Supabase.
*   **Prioridad 1 (TSK-010):** Verificación y despliegue del esquema de base de datos en Supabase para la tabla inscritos (incorporando las columnas críticas: `whatsapp`, `tipo_ayuda`, `autorizacion` y el contenedor `respuestas_custom` en JSONB).
*   **Prioridad 2 (TSK-011):** Despliegue físico del archivo `b5.css` en la ruta estandarizada `css/templates/campana/`.
*   **Prioridad 3:** Retomar las tareas estratégicas de monetización e infraestructura (`TSK-001` Integración Wompi y `TSK-002` Edge Function de webhook).

#### 4. Riesgos Activos y Restricciones a Preservar (STRICT)
*   **Identidad de System Admin (ADR-025):** desde este ADR, `organizaciones.is_master_org = true` es la única fuente de verdad para permisos de System Admin global — no un slug. No reintroducir comparaciones de slug hardcodeado en filtros RLS ni guardas de navegación. Barrio R10 (`slug: barrio-r10`) es cliente regular; la org maestra es `slug: master-admin`.
*   **RLS Legacy Cross-Tenant (TSK-026, nuevo):** hasta que se resuelva, cualquier cliente autenticado (no solo la org maestra) puede leer/escribir `eventos`/`clientes`/`perfiles` de otras organizaciones vía políticas permisivas legacy que no fueron retiradas al añadir las políticas `org_id = get_org_id()`. No asumir que el aislamiento multi-tenant está garantizado solo porque existen las políticas `_org` correctas — coexisten con las legacy.
*   **`generarInforme()` sin filtro `org_id` (TSK-028, nuevo):** riesgo preexistente detectado durante TSK-027 (fuera de alcance, NO corregido). El "Informe gráfico (PDF)" de `admin.html` (~L7900) consulta `SB.from('inscritos').select('*')` sin `.eq('org_id', ORG_ID)`, a diferencia de las consultas vecinas de `clientes`/`logs` que sí filtran por organización. Hasta sanearlo, el informe podría incluir inscritos de otras organizaciones. Mismo frente multi-tenant que TSK-026, pero a nivel de código (no de RLS). Ver `DECISIONS.md` ADR-030 (Pendientes).
*   **Contratos de configuración Wizard/Landing (ADR-047, nuevo):** `config_landing.content.form_redirect` es un contrato **aditivo y opcional** — sin él, `evento.html` cae a `comunidad_whatsapp` y si tampoco existe no redirige (Silent Fallback). No confundirlo con `content.whatsapp` (contacto/ventas). Dos límites a preservar mientras no se decida lo contrario: (a) `registroaforo.html` NO consume `form_redirect`; (b) `evento.html` NO lee `config_landing.modules` — el preset `_THEME_MODULES` es UX del Wizard y **no** gobierna la página pública. No prometer lo contrario ni "arreglarlo" sin ADR propio (ADR-006).
*   **Cumplimiento CSP de Vercel:** Prohibido introducir librerías que ejecuten `eval()` o `new Function()`. El estándar de referencia para QRs es `qr-creator`.
*   **Estabilidad de Supabase:** Mantener los websockets de **Realtime deshabilitados** para prevenir bloqueos en la interfaz; usar polling o reconsultas manuales.
*   **Asunciones de Base de Datos:** El núcleo de `eventovenezuela.html` asume la existencia de las nuevas columnas de la v110 en la tabla `inscritos`; cualquier desajuste interrumpirá la Fase 2 del flujo de conversión.
*   **Regla de Bloque (ADR-007):** Los módulos multimedia (#mod-video, #mod-lineup) deben conservar `display: block !important` para evitar el colapso visual tras la inyección JS[cite: 2].
*   **Tratamiento Geist 900:** Obligatoriedad de tipografía **Geist 900 de 3.2rem** con efectos Afterglow en todas las tarjetas de datos técnicos para el estándar de $10,000[cite: 6].
*   **Motor Público Ambiguo (ADR-021 / TSK-017):** `BLUEPRINT.md` declara `evento3.html` como "El Cerebro", pero `admin.html` enlaza a `evento.html` tras crear un evento. No modificar la lógica de generación de `pubLink` en `admin.html` hasta confirmar cuál de los dos es el motor vigente — un cambio a ciegas podría romper enlaces ya distribuidos.
*   **Dashboard portero (TSK-005):** la semilla de "últimos 5 ingresos" se ejecuta una sola vez en `init()`. No debe re-ejecutarse en cada `updateCounter()` (borraría los logs de sesión: denegados/duplicados). Los nuevos ingresos se reflejan arriba vía `addLog()`. Si se desea un feed auto-refrescante, debe evaluarse contra la Regla "Realtime deshabilitados" (usar polling puntual, no websockets).

---
*Este documento se actualiza bajo la Regla v127, garantizando la preservación total de la información histórica.*