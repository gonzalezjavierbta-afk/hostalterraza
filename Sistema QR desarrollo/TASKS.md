📂 TASKS.md — Tablero Operativo (v1.19-FIX)
### TASKS.md — Tablero Operativo de Tareas
#### Estado General
*   **Proyecto:** Sistema QR Hostal Terraza
*   **Checkpoint Actual:** v1.6.5-FIX / Contrato v112 (ADR-021 Wizard + ADR-022 Nav. Admin + ADR-023 Nav. Panel + ADR-024 Null Fix + ADR-025 Separación Master Admin + ADR-029 Auditoría Null Completa + ADR-030 Exportación Lista de Invitados + ADR-043 Fix Mobile Hero F11 v3.2.0 + ADR-044 Catálogo Sin Numeración + Registro Silo F11 'Fosforescente' + ADR-045 ADR-040 IMPLEMENTADO — Catálogo Curado por la Cuenta Master (migración `adr040_config_global_templates.sql` pendiente de ejecución manual por Dirección) + ADR-046 Hub Normativo TEMPLATES.md + Skill `templates` + ADR-047 Contratos Wizard/Landing: `form_redirect` + `#ld-i18n-config` solo paso 4 + Preset ADN Visual `_THEME_MODULES` + **ADR-048 Silo F12 'Kande' entregado (Tropical Noir Brutalista, `f12.css` v1.0.0) + theme `kande` registrado en el Wizard (6 puntos, `_THEME_MODULES` 19 themes) + Reconciliación estructural `evento.html` → `evento-app.html` + **ADR-049 Silo F12 'Kande' ciclo visual CSS-only v1.3.0 -> v1.9.0 (contenido temporalmente fijado en CSS + plan de migracion a datos) desplegado en `main`/Vercel** + **ADR-050 Analítica del landing AMPLIADA (CHECK event_type 6→8 con `section_view`/`lightbox` + índice `idx_page_events_org_slug_created` + 4 vistas con `security_invoker=true` — `v_landing_analytics` 15 cols, `v_landing_clicks_rank`, `v_landing_serie_diaria`, `v_landing_funnel` — + sub-tab "Landing" 5.º en `admin.html` + port de telemetría a `eventovenezuela.html`; migraciones adr032 + adr050 APLICADAS a producción; front implementado en working tree, QA en curso — TSK-058/059)**)
*   **Sprint Activo:** Sprint 1 — Monetización SaaS, Automatización y Conversión de Emergencia
*   **Última Actualización:** 22 de septiembre de 2026

--------------------------------------------------------------------------------

#### 🔴 ALTA PRIORIDAD / EN COLA INMEDIATA

**Fase PROXIMAMENTE del landing (ADR-051) — Pendientes de cierre (2026-09-22)**
- [ ] **TSK-060 | Direccion / Lead Developer (PENDIENTE — no ejecutable por la IA; ADR-051):** Ejecutar `migrations/adr051_fase_landing.sql` en Supabase SQL Editor (pendiente de Direccion) — la columna `eventos.fase_landing` es requisito para activar la fase PROXIMAMENTE; el front degrada fail-open hasta aplicarla. Migracion aditiva e idempotente: `fase_landing text NOT NULL DEFAULT 'completa'` con `CHECK (fase_landing IN ('completa','proximamente'))`. Sin la columna, el kernel `evento-app.html` trata todos los eventos como `'completa'` (comportamiento actual, 0 regresion). Ver `DECISIONS.md` ADR-051.
- [ ] **TSK-061 | QA / @qa-auditor (verificacion end-to-end; ADR-051):** Verificacion end-to-end de la fase PROXIMAMENTE: activar `fase_landing='proximamente'` en un evento con fecha real futura, confirmar que solo se ven hero/titulo/subtitulo/contador/meta + boton WhatsApp (rotulo 'MAS INFO'), sin huecos de grid; pasar a 'completa' y confirmar la landing ampliada; validar OG con 'Proximamente'. Ver `DECISIONS.md` ADR-051.

**Silo F12 "Kande" (Tropical Noir Brutalista) + Registro del theme en el Wizard + Reconciliacion evento-app.html (ADR-048, 2026-09-22)**
- [x] **TSK-043 | Documentation Specialist / @docs-keeper-free (cierre documental; el silo y el registro fueron implementados por el equipo frontend `*-free`):** Cierre documental (Mandato 12 / Reglas de Oro #12) de la entrega del silo NUEVO `css/templates/fiesta/f12.css` v1.0.0 (theme "Kande", template_id `f12`, categoria `fiesta`) y del registro del theme `kande` en `admin.html`. Alcance REAL verificado contra los archivos (ADR-006): `f12.css` = 1563 lineas / 50294 bytes / 0 bytes > 127 / llaves 193/193 / 0 `@import` reales / 0 selectores fuera de scope (100% bajo `.tpl-f12`/`.Tpl-F12`) / 28 tokens `--f12-*` + puente `--tpl-accent: var(--master-accent, #ff2f8e)` / media queries 767 (L1247), 480 (L1439), 992-1279 (L1478), min-1280 (L1518), reduced-motion (L1534) / **14 modulos ENCENDIDOS** (`#mod-hero`, `#unified-frame`, `#mod-hero-meta`, `#mod-hero-ctas`, `#mod-historia`, `#mod-cartel`, `#mod-video`, `#mod-playlist`, `#mod-boletos`, `#mod-form`, `#mod-faq`, `#mod-ubicacion`, `#mod-whatsapp`, `#mod-footer`) y **9 APAGADOS con IDs intactos** (`#mod-lineup`, `#mod-experiencias`, `#mod-objetivo`, `#mod-impacto`, `#mod-info-tecnica`, `#mod-sponsors`, `#mod-equipo`, `#mod-cta-final`, `#mod-descripcion`) / hero opcion A (countdown como tira compacta DENTRO del hero; orden `badge -> titulo -> cinta -> meta -> countdown -> CTA`; NUNCA 100vh/100svh: 72svh base, 68svh <=767px, 72svh 992-1279px, 78svh >=1280px) / datos sin duplicar (solo `#meta-lugar` visible; `#meta-fecha`/`#meta-hora` ocultos por CSS con `:has()`, IDs intactos) / ancla `.hero-cta-btn--lineup` oculta por CSS (apunta a `#mod-lineup` apagado) / **100% CSS** (CERO cambios a JS, CERO subconmutaciones, CERO atomos nuevos). Registro en `admin.html` (10077 lineas) en **6 puntos**: tarjeta `data-theme="kande"` + `onclick="abrirFichaTheme('kande')"` L606 · `_THEME_TYPE` L2830 · `_THEME_TPL` L2844 (`'kande':'f12'`; derivado `_TPL_THEME` del reduce no se edita) · `_THEME_MODULES` L2875 · `_THEME_FICHA` L3103-3104 · `names` L3235; **19 tarjetas `.ld-theme-card`** (18 previas intactas = Cero Borrado). **Correccion de defecto MAYOR (CONTRATO ADN Visual):** `requeridos` de `kande` = `['ld-mod-boletos','ld-mod-playlist','ld-mod-ubicacion']`; `ld-mod-historia` eliminado por completo de la entrada (checkbox solo existe en `#mods-campana` L684, NO en `#mods-fiesta` L651-665; familia ERRORES §13; `hideForeignModulesByType` L5154, loop L5189-5196). **Hallazgo estructural:** `evento.html` NO existe en el repo — el motor publico real es `evento-app.html` (1724 lineas, "Master Orchestrator v214"; `vercel.json` reescribe `/evento.html` → `/api/evento-og`; `#db-gallery-grid` L148 dentro de `#mod-video` L147; `#mod-hero-ctas` nace `display:none !important` L33; sin override `?tpl=`/`?cat=`; `MODULE_IDS` ya no existe en v214). Reconciliado en `TEMPLATES.md` v1.4.0, `PROJECT.md` Seccion 6 y ADR-048. Estado: **Completado ✅** (QA APTO CON OBSERVACIONES — Escudo GOLD del silo PASS; observacion no bloqueante: el texto de `_THEME_FICHA['kande']` L3104 dice "Requeridos: historia..." mientras el mapa funcional ya no incluye historia). Entregado en working tree SIN commit. Ver `DECISIONS.md` ADR-048.
- [ ] **TSK-044 | QA / @qa-auditor (post-commit + D1, T6 smoke):** smoke de produccion sobre el evento `kande-musica-tradicional-del-caribe-colombiano-kk4c` con silo f12: link `css/templates/fiesta/f12.css` responde 200 en produccion; `body.className` contiene la doble clase `tpl-f12`/`Tpl-F12`; consola limpia; verificacion en 3 viewports (mobile/tablet/desktop). Ver `DECISIONS.md` ADR-048 (Pendientes 4).
- [ ] **TSK-045 | Direccion / Lead Developer (D1 + D2, Supabase):** (D1) editar el evento `kande-musica-tradicional-del-caribe-colombiano-kk4c` y asignarle la plantilla "Kande" (`template_id='f12'`, `categoria_slug='fiesta'`) — sin esto el silo NO se inyecta (el kernel real `evento-app.html` no tiene override de plantilla); (D2) cargar la tarifa real del "COVER 20K" — sin tarifa real el kernel auto-oculta `#mod-boletos` (`__hideEmptyModule`). Ver `DECISIONS.md` ADR-048 (Pendientes 2/3).
- [ ] **TSK-046 | Direccion / Creative Director (assets v1.1.0):** construir y subir a Supabase Storage `assets/templates/f12/` los 4 assets del contrato: `f12-hero-bg.v1.webp` (2560x1440, <=350KB), `f12-hero-bg-mobile.v1.webp` (1080x1620, <=250KB), `f12-papel-rasgado.v1.png` (1600x180 con alfa, <=80KB), `f12-flora-esquina.v1.png` (900x900 con alfa, <=150KB). El CSS ya los referencia (L45-48) con capa de fallback visible (ADR-008); la v1.0.0 es funcional sin ellos. Ver `DECISIONS.md` ADR-048 (Pendientes 5).
- [ ] **TSK-047 | Chief Architect / ADR futuro:** barra de navegacion superior del diseno de referencia de Kande — NO implementada porque exigiria un atomo nuevo en `evento-app.html` (cambio de kernel). Requiere plan + ADR propio (decision de Direccion). Ver `DECISIONS.md` ADR-048 (Pendientes 6).
- [ ] **TSK-048 | Tooling / @free-build:** `@media-reader-free` roto — su frontmatter declara `opencode/mimo-v2.5-free`, que YA NO EXISTE en el catalogo (la invocacion devuelve "Model not found"; el catalogo ofrece `mimo-v2.6-flash-free`, `ling-3.0-flash-fin-free`, `muse-spark-1.2-contributor-free`). Actualizar `.opencode/agent/media-reader-free.md` Y la tabla de modelos de `AGENTS.md` (que sigue citando `mimo-v2.5-free` como excepcion intencional). Ver `DECISIONS.md` ADR-048 (Pendientes 7).
- [ ] **TSK-049 | Documentation Specialist (residual del hub v1.4.0):** `TEMPLATES.md` Paso 8 describia 3 puntos de registro y `onclick="seleccionarTheme(...)"`; el real son 6 puntos y usa `abrirFichaTheme(...)` (+ `_THEME_MODULES` de ADR-047 y `_THEME_FICHA`); `_THEME_MODULES` paso de 18 a 19 themes (NEXT.md hito -11 dice 18). Corregido en `TEMPLATES.md` v1.4.0 (2026-09-22) en este cierre; pendiente verificar que no queden citas de `evento.html` como unico motor en otros docs del AI-DOS Core (BLUEPRINT.md, AGENTS.md, hitos de NEXT.md). Ver `DECISIONS.md` ADR-048 (Pendientes 8).

**Silo F12 "Kande" — Ciclo visual CSS-only v1.3.0 -> v1.9.0 (ADR-049, 2026-09-22)**
- [x] **TSK-050 | Frontend (@frontend-tpl-free, implementacion) + Documentation Specialist (cierre documental `@docs-keeper-free`):** Ciclo visual del silo f12 "Kande" (template_id `f12`, categoria `fiesta`, theme `kande`) del evento `kande-musica-tradicional-del-caribe-colombiano-kk4c` (org "Klandestino bar", plan `pro_mensual`; fecha 2026-09-25; lugar "Kr 4 # 19-56 cuarto piso"; boletos: taquilla `$20.000`, preventa `null`). **100% CSS en un solo archivo** `css/templates/fiesta/f12.css` (v1.3.0 -> **v1.9.0**), sin tocar kernel `evento-app.html`, SQL ni `admin.html`. Baseline commit `5d3676c`; commits del ciclo en `main` (ya en produccion via Vercel): `6960cc6` (v1.4.0+v1.5.0: hero titulo/subtitulo + cinta "Klandestino Bar", boleteria solo taquilla, meta 2 lineas), `17f8cb4` (v1.6.0: cinta con subtitulo, quita banda "BOLETERIA", fondo amarillo WhatsApp), `de790a0` (v1.7.0: cinta mas cerca del titulo, hero mobile compacto, badge cyan, CTA mobile menor, boleto-trust discreto), `f6f13ed` (v1.8.0: mobile con menos hueco, CTA "COMPRAR ENTRADAS" 100% CSS, cinta mostaza), `e528464` (v1.9.0: mobile +35% de subida, boleto-trust sin resalte, footer "Klandestino Bar" via CSS, ceja con transparencia). Entre medio, commit de Direccion `a45174f` ("Update f12.css": cambio del asset `--f12-asset-flora`). `HEAD == origin/main == e528464`. **Verificacion real (ADR-006, HEAD):** 2423 lineas / 86600 bytes / 0 bytes > 127 (ASCII-safe) / llaves 257/257 (diff 0) / **22 IDs `#mod-*` unicos intactos** (Cero Borrado, Regla de Oro #3) / los 6 commits del ciclo tocan UNICAMENTE `f12.css`. **Deuda registrada:** contenido temporalmente fijado en CSS (titulo "KANDE", subtitulo de la cinta, meta 2 lineas, CTA "COMPRAR ENTRADAS", copyright) con plan de migracion a datos - ver ADR-049 y tareas D1-D6 (TSK-051..TSK-056). Escudo GOLD: llaves balanceadas + ASCII-safety + Cero Borrado PASS. Ver `DECISIONS.md` ADR-049.
- [ ] **TSK-051 | D1 - Meta data-driven (kernel + Supabase):** extender el kernel `evento-app.html` (~L817, gate `__esF9Meta`) para habilitar `.meta-line` tambien en `.tpl-f12`, y cargar `config_landing.content.meta = {fecha:{l1,l2},hora:{l1,l2},lugar:{l1,l2}}` en el evento Kande (Supabase); luego retirar el contenido fijo de la meta del CSS de f12. Evidencia: el silo f9 (evento `prelanzamiento-mistico-9t39`) ya usa ese contrato (kernel L810-831). Ver `DECISIONS.md` ADR-049 (Pendientes 1).
- [ ] **TSK-052 | D2 - Copyright real (i18n/dato):** el footer de f12 muestra "Klandestino Bar" solo VISUALMENTE por CSS (`#footer-copy::before`); el texto real del DOM y los lectores de pantalla siguen leyendo "© 2026 Chongotronik..." (`js/i18n.js` L250 `landing.copy_chongo`; tambien L620/L990). Fix real por i18n/dato, no por CSS. Ver `DECISIONS.md` ADR-049 (Pendientes 2).
- [ ] **TSK-053 | D3 - Textos fijos del hero/CTA a datos:** "KANDE", subtitulo de la cinta, contenido de la meta y "COMPRAR ENTRADAS" estan hardcodeados en el CSS de f12 (pseudoelementos `content:`); migrarlos a datos del evento para que el silo sea reutilizable. Ver `DECISIONS.md` ADR-049 (Pendientes 3).
- [ ] **TSK-054 | D4 - Assets del silo f12:** `--f12-asset-papel` = `url("")` (vacio) y revision del contrato `assets/templates/f12/` (ADR-048/TSK-046): hoy los assets son URLs externas (`i.ibb.co`, `gstatic.com`), no Supabase Storage. Ver `DECISIONS.md` ADR-049 (Pendientes 4).
- [ ] **TSK-055 | D5 - Deudas heredadas del silo f12:** namespacing `ld-mod-historia-fiesta` (el Wizard de Fiesta no puede expresar `#mod-historia`; ver ADR-048 / ERRORES §13); `@media-reader-free` (ya cubierto por **TSK-048** - no duplicar); barrido de citas `evento.html` en el Core (ya cubierto por **TSK-049** - no duplicar). Net-new de esta tarea: el namespacing por categoria. Ver `DECISIONS.md` ADR-049 (Pendientes 5).
- [ ] **TSK-056 | D6 - Verificacion visual pendiente (mobile):** el overlay mobile de f12 usa margenes negativos (`-50svh` + `calc` con guard `max(0px,...)`) - fragil; validar en 360/390/414px y landscape; contrastes (`.boleto-trust` opacity 0.55, `#org-name-badge` 0.68) y soporte `:has()`/`:not(:has())`. Ver `DECISIONS.md` ADR-049 (Pendientes 6).

**ADR-032 — Analítica del landing `page_events` — MIGRACIÓN APLICADA A PRODUCCIÓN (2026-09-22, operación crítica ejecutada)**
- [x] **TSK-057 | Operación crítica de base de datos / @data-migration (ejecución) + verificación con evidencia reproducible:** **Aplicado `migrations/adr032_landing_analytics.sql` en la instancia de producción `ctgyvydzshueemlelkzv.supabase.co`** (ref: `ctgyvydzshueemlelkzv`, proyecto "hostal-terraza", org `mfraznfjnjmvwgphnvlf`, región South America São Paulo; PostgreSQL 17.6). La tabla `page_events` NO existía en la instancia antes (PGRST205 verificado en auditorías ADR-039/T0 y confirmado pre-ejecución: `to_regclass('public.page_events') = null`). **Canal usado:** Supabase CLI v2.84.2 autenticado (PAT personal `sbp_*` almacenado en Windows Credential Manager, target `Supabase CLI:supabase`, recuperado vía `CredRead` con P/Invoke — nunca impreso ni comiteado) → **Management API endpoint** `POST /v1/projects/{ref}/database/query` (equivalente programático al SQL Editor, rol `postgres`). Script ejecutado ÍNTEGRO (117 líneas, idempotente) en una única llamada. **Verificación post-ejecución (output real transcrito):** `SELECT count(*) FROM public.page_events` → `{"page_events_count": 0}`; 15 columnas confirmadas por `information_schema.columns` (id uuid PK, org_id, evento_id, evento_slug, evento_nombre, page_url, page_ref, event_type, event_action, event_label, event_value numeric, session_id, visitor_id, user_meta jsonb, created_at timestamptz — todas con NOT NULL y defaults del script); índices `idx_page_events_slug_created` (btree evento_slug, created_at DESC), `idx_page_events_visitor`, `idx_page_events_type` + `page_events_pkey` confirmados en `pg_indexes`; `SELECT * FROM public.v_landing_analytics` → vacío SIN error (vista creada); `pg_views` confirma definición íntegra (granularidad pageviews/unique_visitors/form_submits/clicks/avg_seconds_active/last_event + FILTER por event_type). **RLS verificado:** `relrowsecurity = true` (habilitado); `pg_policies` = 2 políticas exactas — `page_events_anon_insert` (FOR INSERT TO anon WITH CHECK true) y `page_events_auth_read` (FOR SELECT TO authenticated USING true); cero políticas UPDATE/DELETE (garantiza Cero Borrado de telemetría). **Grants:** `has_table_privilege` confirma anon INSERT=true / authenticated INSERT+SELECT=true; vista `v_landing_analytics` con SELECT para authenticated. **Prueba funcional real del flujo front (webhook anon):** INSERT como rol `anon` (`SET ROLE anon`) en statement único → SUCCESS con fila real creada (id `736c797c-f498-466a-aa1c-0a9e8e7807c7`, evento_slug `smoke-adr032`, event_type `pageview`); fila de smoke eliminada después (DELETE como postgres) y `count(*)` final = **0** (tabla lista para recibir inserts, cero basura de prueba). **Nota técnica:** el runner de la Management API ejecuta cada statement con autocommit separado — los bloques `BEGIN/ROLLBACK` multi-statement no comparten transacción (evidencia: prueba transaccional inicial no falló pero tampoco insertó) — por eso se ejecutó el script como unidad única y el smoke como statement único. **Ningún archivo de la migración fue modificado; repo quedó intacto** (`git status` limpio al cierre). Pendiente Dirección: rotar/saber que el PAT vive en Credential Manager de la máquina (ver reporte). Ver `DECISIONS.md` ADR-039 (que documentaba `page_events` como PGRST205/inexistente — queda supersedido por esta entrega).

**ADR-050 — Analítica del landing AMPLIADA (CHECK 8 tipos + índice + 4 vistas) — MIGRACIÓN APLICADA A PRODUCCIÓN (2026-09-22/23, operación crítica ejecutada)**
- [x] **TSK-058 | Operación crítica de base de datos / @data-migration (ejecución) + verificación con evidencia reproducible:** **Aplicado `migrations/adr050_landing_analytics_ampliado.sql` (274 líneas, 100% idempotente) en la instancia de producción `ctgyvydzshueemlelkzv.supabase.co`** (proyecto "hostal-terraza", org `mfraznfjnjmvwgphnvlf`, región São Paulo; PostgreSQL 17.6 vía Supabase CLI v2.84.2). **PREREQUISITO VERIFICADO PRE-FLIGHT:** adr032 ya estaba aplicado (`page_events` existente, CHECK original de 6 tipos, `v_landing_analytics` v1 con 8 columnas, `page_events_count=0`) — la migración FELLÓ de DROP VIEW en el primer intento del pre-flight? No: el pre-flight fue lectura pura y pasó OK (consta en el canal), por lo que el estado pre era el esperado del script. **Canal usado (idéntico a TSK-057):** Supabase CLI v2.84.2 + PAT personal `sbp_*` del Windows Credential Manager (target `Supabase CLI:supabase`, recuperado vía `CredRead` con P/Invoke, encoding UTF-8 del blob go-keyring, **nunca impreso ni comiteado**) → Management API `POST /v1/projects/ctgyvydzshueemlelkzv/database/query` (rol postgres, autocommit por statement). Script ejecutado ÍNTEGRO en una única llamada (output vacío = DDL sin error). **VERIFICACIÓN POST (output real transcrito):** (1) `pg_constraint` → `page_events_event_type_check` = `CHECK ((event_type = ANY (ARRAY['pageview','form_submit','click','scroll_depth','active_visit','error','section_view','lightbox'])))` — **8 tipos** ✅; (2) `pg_indexes` → `CREATE INDEX idx_page_events_org_slug_created ON public.page_events USING btree (org_id, evento_slug, created_at DESC)` ✅; (3) `pg_views` → las 4 vistas existen con definición íntegra (`v_landing_analytics` 15 columnas con org_id en GROUP BY + form_ok/dup/error + section_views + lightbox_events + scroll_50pct; `v_landing_clicks_rank`; `v_landing_serie_diaria`; `v_landing_funnel`) y **`reloptions = security_invoker=true` en las 4** (pg_class) ✅; columnas por `information_schema.columns` = analytics 15 / clicks_rank 8 / serie_diaria 18 / funnel 8 ✅; (4) **Prueba funcional** `SET ROLE anon; INSERT INTO page_events (...) VALUES (gen_random_uuid(),'smoke-adr050','Smoke','section_view','sec:visible','hero'); RESET ROLE;` → **EXITOSA** (rol anon inserta tipo nuevo sin violar CHECK — fila leída: `id=946771a2...`, `event_type='section_view'`, `event_action='sec:visible'`, `event_label='hero'`); vista viva confirmada (`v_landing_analytics` mostró la fila: `section_views=1, unique_visitors=1`); (5) **Limpieza:** `DELETE WHERE evento_slug='smoke-adr050'` → `page_events_count=0` y smokes residuales=0 ✅; (6) `SELECT * FROM` las 4 vistas → **vacías sin error** (estado final limpio); (7) **Idempotencia probada:** re-ejecución completa del script sin error y constraint/índice intactos ✅. RLS intacto (2 políticas exactas de adr032) y GRANT SELECT authenticated en las 4 vistas. No se modificó ninguna otra tabla/objeto ni el archivo de migración (git status: `adr050_landing_analytics_ampliado.sql` solo `??` untracked preexistente del working tree). Ver `DECISIONS.md` ADR-050 y `ERRORES_HISTORICOS.md` si corresponde.*

- [x] **TSK-059 | QA / @qa-auditor (certificación del front ADR-050 — dictamen APTO CON OBSERVACIONES recibido):** Certificación del ciclo ADR-050 en el front (working tree SIN commit ni deploy): sub-tab "Landing" 5.º en `admin.html` (KPIs/ranking/serie/embudo/tabla + export XLSX con fail-open), telemetría ampliada en `evento-app.html` (CLICK_RULES L415-428, emitLb L561-613, section_view L461-480, btn:tab_* L674) y port a `eventovenezuela.html` (lb:open L643, link:externo L645-646, section_view L695). Verificar contra archivo real (ADR-006): consultas del sub-tab con `.eq('org_id', ORG_ID)`; empty state fail-open; cero doble conteo click+lightbox; ASCII-safe; balance de divs. **Estado previo: implementado (verificado ADR-006 en cierre documental 2026-09-22); QA en curso — sin dictamen aún** (si el dictamen `@qa-auditor` generaba observaciones, se registrarían como correcciones del ciclo). **ACTUALIZADO 2026-09-22: implementado y QA APTO CON OBSERVACIONES** (ambas resueltas): **MEDIO-1** (26 líneas con bytes>127 en `admin.html` — emojis/tildes del sub-tab Landing) **ACEPTADO por decisión del build** (`admin.html` es HTML de cliente con charset utf-8 y la baseline ya tiene 21171 bytes>127; la regla ASCII-safety de cero bytes>127 aplica a `api/*.js`/serverless); **MEDIO-2** (`eventovenezuela.html` emitía la galería como `event_type='click'` en vez de `'lightbox'`) **CORREGIDO y VERIFICADO** — el frontend movió la emisión de `.gallery-thumb` de CLICK_RULES al handler del lightbox nativo (ahora emite `emit('lightbox','lb:open','gallery',1)` y añadió `lb:close` con orígenes x/backdrop/esc; express_check PASS 9/0, divs 120/120, ASCII 0/0, sin doble emisión); deuda BAJO-1 (documental ADR-050) ya cerrada en la sesión previa. **Pendiente de Dirección: commit del working tree y despliegue del front a Vercel.** Ver `DECISIONS.md` ADR-050 (Pendientes 1) y `NEXT.md` hito -17.

**Separación Master Admin / Barrio R10 (ADR-025) — Pendientes de cierre**
- [x] **TSK-024 | Chief Architect (Claude):** Código en `admin.html` — reemplazadas las 3 referencias a `slug === 'hostal-terraza'` por `is_master_org === true` (`_IS_SYSTEM_ADMIN()`, `applyRoleNav()`, comentario de `_orgOriginal`). Corte directo sin fallback (mandato de Dirección). Estado: **Completado ✅** en código, 7973→7973 líneas (sin adiciones/eliminaciones fuera de las 3 líneas tocadas). Ver `DECISIONS.md` ADR-025.
- [ ] **TSK-025 | Dirección / Lead Developer:** Ejecutar `migrations/adr025_master_admin_separation.sql` contra la instancia real de Supabase — columna `is_master_org`, creación de la org "SaaS Master Admin", reasignación del login actual a la org maestra, alta del nuevo login operativo de Barrio R10, y reescritura de las políticas `insert_organizaciones`/`delete_organizaciones`. Incluye pasos manuales de Supabase Dashboard (creación de usuario) que no pueden ejecutarse por SQL puro.
- [ ] **TSK-026 | Chief Architect (⚠️ hallazgo de auditoría, prioridad alta, fuera del mandato de ADR-025):** Revisar y retirar las políticas RLS legacy permisivas detectadas en `eventos` (`"Admins escriben eventos"`, ALL sin chequeo de `org_id`), `clientes` (`"Clientes acceso total autenticado"`, ALL sin ningún chequeo) y `perfiles` (`is_superadmin()` sin acotar por organización + `org_perfiles_update` sin `WITH CHECK` que impida auto-escalación de `rol`/`org_id`) — hoy permiten a cualquier cliente autenticado, no solo a Barrio R10, leer/escribir datos de otras organizaciones. Requiere su propio Context Package. Ver diagnóstico completo en `DECISIONS.md` ADR-025.
- [ ] **TSK-028 | Chief Architect (⚠️ riesgo preexistente detectado en TSK-027, fuera de su alcance, relacionado con TSK-026):** `generarInforme()` (~L7900 de `admin.html`, ítem "Informe gráfico (PDF)" del `#export-menu`) consulta `SB.from('inscritos').select('*')` **sin** `.eq('org_id', ORG_ID)` — sus consultas vecinas de `clientes` y `logs` sí filtran por organización. Corregir para cerrar la posible fuga cross-tenant del informe. **No fue corregido durante TSK-027** (riesgo preexistente y fuera del mandato). Ver `NEXT.md` §4 (Riesgos Activos) y `DECISIONS.md` ADR-030 (Pendientes).

**Cuenta Master / Catálogo de Plantillas (ADR-040) — IMPLEMENTADO (ADR-045), migración pendiente de ejecución**
- [x] **TSK-030 | Lead Developer / @sql-security + @frontend-tpl (implementación `*-free`, cierre documental `@docs-keeper-free`):** Implementar ADR-040 — catálogo de plantillas curado por la cuenta master con fuente de verdad en `config_global` (tabla nueva + RLS + helper `is_master_org_user()`), sub-tab "Plantillas" en `pg-globaladmin`, lectura/aplicación (`_cargarConfigGlobal()` / `_aplicarTemplatesOcultos()`, ocultar sin borrar vía clase `.ld-theme-oculto`) y ajustes de auto-preselección/filtro por categoría. Estado: **Completado ✅** (QA APTO — Escudo GOLD PASS: `node --check` 6/6, ASCII 0 en 358 líneas nuevas, balance `<div>` 854/854, Cero Borrado intacto). Entregado: `migrations/adr040_config_global_templates.sql` (idempotente, aditiva, fila única `config_global`, helper `is_master_org_user()`, RLS SELECT authenticated/escritura master, RPC `fn_config_global_merge` con merge JSONB server-side + `updated_by`, `NOTIFY pgrst`, rollback comentado; contrato vigente `{version:2, hidden, order}`) + `admin.html` (sub-tab Plantillas con MOSTRAR/OCULTAR/REORDENAR, bloqueo duro en cero, aviso suave por categoría, reorden `appendChild`, revelación legacy no persistente, `_primerThemeVisible`, guard `data-cat-empty`, namespace `data-tplcat`, fail-open con `console.warn`). Defectos A-H del QA corregidos antes del cierre (bloqueo optimista por timestamptz Z, éxito silencioso RLS, `updated_by` no escrito, merge JSONB faltante, lectura sin observabilidad, colisión `data-cat`, mensaje de categoría vacía, limpieza legacy). El theme `fosforescente`/`f11` nace visible por defecto (forward-compatible). Ver `DECISIONS.md` ADR-040 (Enmienda I) y ADR-045; `ERRORES_HISTORICOS.md` §13.
- [ ] **TSK-035 | Dirección / Lead Developer (PENDIENTE — no ejecutable por la IA):** Ejecutar manualmente `migrations/adr040_config_global_templates.sql` en Supabase (SQL Editor) + **Prueba de Carga Dual** post-migración: (1) catálogo completo por defecto en otra org/dispositivo; (2) ocultar y reordenar 1-2 cards desde la cuenta master y confirmar persistencia y reflejo en otras orgs; (3) editar un evento con theme oculto y confirmar que conserva su `template_id` al guardar (revelación legacy, sin allowlist). Sin la tabla, el front degrada a fail-open (todo visible) — no bloquea. Incluye `NOTIFY pgrst 'reload schema'` (dentro de la migración). Ver `DECISIONS.md` ADR-045 (Pendientes 1-3).

**Hub Normativo de Templates (ADR-046) — Entregado**
- [x] **TSK-034 | Documentation Specialist / @docs-keeper-free (+ @free-build/@free-plan en el contenido normativo):** Hub normativo `Sistema QR desarrollo/AMPLIACION/TEMPLATES.md` (v1.3.0, 438 líneas — 10 capítulos + Brief 9-bis + Metodología 8 pasos 9-ter + Anexos A/B/C con inventario del DOM real ~81 IDs) + skill puente `.opencode/skills/templates/SKILL.md` que remite al hub. Incluye la advertencia Contrato v112 (21 átomos) vs. DOM real (~81 IDs): solo 8 de 21 existen con id exacto — no reportar los 12 restantes como bug. Estado: **Completado ✅** (QA APTO). Cero cambios de código de aplicación. Ver `DECISIONS.md` ADR-046.

**Wizard Inteligente (ADR-021) — Pendientes de cierre**
- [ ] **TSK-016 | Lead Developer:** Modificar `evento.html` para leer la nueva columna `captura_pura`: si es `true`, omitir la generación/render del QR tras el registro y mostrar un mensaje de agradecimiento personalizado en su lugar. No se pudo implementar en la sesión de ADR-021 porque `evento.html` no formaba parte de los archivos subidos.
- [ ] **TSK-017 | Chief Architect (⚠️ prioridad alta, verificar primero):** Reconciliar la contradicción entre `BLUEPRINT.md` §1 (declara `evento3.html` como "El Cerebro") y el link público que genera `admin.html` tras crear un evento (`/evento.html?slug=...`, ver `_crearEventoUnico()` y TSK-008). **Actualización de Dirección:** confirmado que el desarrollo activo se está haciendo desde `evento3.html`. Sigue pendiente confirmar formalmente si `evento.html` (al que `admin.html` sigue enlazando) es un alias/redirect de `evento3.html`, un remanente pre-migración a actualizar, o un motor distinto — de esto depende si `template_id`/`categoria_slug` (persistidos desde ADR-021) tienen efecto visual real en el link que el admin comparte.
- [ ] **TSK-018 | Lead Developer:** Ejecutar `migrations/adr021_eventos_columns.sql` (idempotente) contra la instancia real de Supabase para confirmar/crear `template_id`, `categoria_slug`, `captura_pura` en `eventos`.

- [ ] **TSK-003 | Lead Developer (Gemini):** Configurar pg_cron para envío automático de recordatorios 24h antes del evento vía send-ticket-email.

**Blindaje Campaña b5 (Emergencia)**
- [ ] **TSK-010 | Lead Developer:** Verificar y crear columnas en tabla `inscritos` (whatsapp, tipo_ayuda, autorizacion) [cite: 810, 735].

- [ ] **TSK-012 | Chief Architect:** Implementar ADR-012 en `DECISIONS.md` para documentar la extensión del Contrato v110 [cite: 811].

--------------------------------------------------------------------------------

#### 🟡 PRIORIDAD MEDIA

**Refinamiento y UX**
- [x] **TSK-029 | Chief Architect (implementación @free-build + subagentes `*-free`):** Entrega F9 "Místico" (Rastro MC) — segunda iteración del silo: átomo nuevo `#mod-descripcion` (solo F9), organización del silo (se apagan `#mod-historia`/`#mod-objetivo`/`#mod-impacto`/`#mod-info-tecnica` con IDs intactos; hero meta 2 líneas; lineup desktop headliner + 6 artistas en 2 filas de 3; footer dorado con sello `- rastro mc`; breakpoints 1279/991/767/640), boletería por tarifa real (nunca "POR DEFINIR"), FAQ fallback exclusivo `tpl-f9`, WhatsApp sin número en el texto (cambio GLOBAL aprobado por el usuario) y registro del theme `mistico` → `f9` en `admin.html` (card P13, `_THEME_TYPE`, `_THEME_TPL`, `content.meta` l1/l2 con persistencia/precarga/limpieza). Estado: **Completado ✅** en working tree (**sin commit**). Escudo GOLD PASS (`node --check`, ASCII-safety de código, balance de divs/llaves/comentarios, Cero Borrado, grid); H-1 (la descripción ignoraba `ev.descripcion`) y H-2 (la meta 2 líneas degradaba otros silos) corregidos. Ver `DECISIONS.md` ADR-041 y `.opencode/plans/template-f9-mistico.md` (iteración v2).
- [ ] **TSK-031 | Chief Architect (deuda técnica aceptada — hallazgos H-3/H-4 de la entrega F9):** (H-3) El FAQ de ejemplo de F9 está hardcodeado para CUALQUIER evento `tpl-f9` sin FAQ configurado, no solo para el prelanzamiento — migrarlo a contenido configurable o acotarlo al evento. (H-4) `formatWaPhone()` queda sin uso tras el cambio global de WhatsApp (la etiqueta visible ya no muestra el número) — retirar o reutilizar el código muerto; al retirar, confirmar con grep que no queden llamadas. Ver `DECISIONS.md` ADR-041 (Correcciones post-auditoría) y `NEXT.md`.
- [x] **TSK-032 | Frontend (implementacion) + Documentation Specialist (cierre documental):** Entrega F10 "Rico" (Cyberpunk Fosforescente) v2.0.0 - reescritura TOTAL solo-CSS de `css/templates/fiesta/f10.css`: lineup estilado sobre selectores kernel reales (`.artist-card*` incl. `.headliner`, `.artist-photo`, `.artist-overlay`, `.artist-name`, `.artist-hora`, `.artist-resena` line-clamp 2, `.artist-social`, `.artist-social-icon`, `.artist-social a`), cero selectores muertos (retirados `.f10-artist*`/`.f10-lc-*`), galeria `#db-gallery-grid` como maqueta de espacios con grid de N abierto `repeat(auto-fill, minmax(...))` y placeholders glow (ADR-008), ASCII-safety 0 bytes > 127, IoC conservada, tipografia Geist 900, breakpoints reales 640/767/768/992/1279. Evento `rico-5mw5` en produccion (template f10, categoria fiesta). Estado: **Completado** en working tree (**sin commit**) - UNICO archivo tocado `f10.css`; NINGUN otro archivo del repo modificado. Escudo GOLD PASS (ASCII-safety, cero selectores muertos, balance, Cero Borrado, Aislamiento Atomico). Ver `DECISIONS.md` ADR-042 y `.opencode/plans/template-f10-rico.md`.
- [x] **TSK-033 | Frontend (implementacion T1+T2, plan aprobado por el usuario) + Documentation Specialist (cierre documental):** Fix del bug visual del hero en mobile del silo **f11** "CYBERPUNK FOSFORESCENTE" (`css/templates/fiesta/f11.css` → v3.2.0). Bug reportado por el usuario: en mobile, dentro del hero f11, arriba se veían los elementos (countdown, meta, botón) y abajo el título. Causa raíz: en la media query `@media (max-width: 767px)` solo `#mod-hero` recibía `order: 1`; los 3 hermanos `#unified-frame` (countdown), `#mod-hero-meta` y `#mod-hero-ctas` quedaban con `order: 0` (default flexbox) → el navegador los colocaba antes que el título. Solución: (1) orden explícito en la media query 767px con doble notación `.tpl-f11` + `.Tpl-F11` (aislamiento atómico): `#mod-hero`=1, `#unified-frame`=2, `#mod-hero-meta`=3, `#mod-hero-ctas`=4, resto re-numerado 5-15 — resultado: título → countdown → meta → botón; (2) hero completo en UNA sola pantalla (proporción 60/40 aprobada): `#mod-hero` con `min-height: 60svh` (fallback `60vh`), título `clamp(2.6rem, 11vw, 4.2rem)` en 767px y versión escalada `clamp(2.2rem, 10vw, 3.4rem)` en 480px, `background-position: center center` en mobile (conservando `background-size: cover`), paddings/gaps compactados. `f11gemini.css` NO se tocó (decisión explícita del usuario). Estado: **Completado ✅** en working tree (**sin commit**) — único archivo tocado `f11.css` (1544 → 1554 líneas). Escudo GOLD PASS (balance llaves 213/213 diff 0, ASCII-safety, desktop `min-height: 85vh !important` intacto, breakpoint 992-1279px intacto, sin media queries nuevas, Cero Borrado). Ver `ERRORES_HISTORICOS.md` §12 y `DECISIONS.md` ADR-043.
- [x] **TSK-005 | Lead Developer:** Dashboard contextual para porteros en scanner.html. Estado: **Completado ✅** — el contador vs aforo (número de ingresos, "de X inscritos · aforo Y", barra de ocupación con estados verde/ámbar/rojo y badges frecuente/invitado) ya existía en `updateCounter()`; se cerró el gap de "últimos 5 ingresos": `seedUltimosIngresos()` consulta `inscritos` (`used=true`, `order hora_ingreso desc`, `limit 5`) y `renderUltimosIngresos()` puebla la sección "Últimas entradas" al iniciar, con fallback offline desde `qrCache` y blindaje null (patrón ADR-024, `(nombre||'—')`). Verificado con `node --check` (sintaxis OK), prueba funcional con casos null/vacío/top-5/orden offline y balance de `<div>` 0. Ver hito en `NEXT.md`.
- [ ] **TSK-006 | Creative Director:** Configurar manifest.json y Service Worker para hacer el scanner instalable (PWA) [cite: 700, 713].
- [ ] **TSK-013 | Creative AI:** Desarrollar el módulo aditivo "08 Actualizaciones" y el desglose de tarjetas para la sección Historia de b5 [cite: 809].
- [ ] **TSK-014 | Documentation:** Sincronizar permanentemente PROJECT.md y BLUEPRINT.md bajo el estándar v127-MASTER.
- [x] **TSK-015 | Chief Architect (Claude):** Expansión Nuclear f1 — fix de desborde 400px, edge-to-edge en video/playlist y recuperación proporcional del póster con centrado en escritorio. Estado: **Completado ✅ (v2.5.0-GOLD)**, verificado visualmente por el Director en mobile y desktop. Auditoría reveló que los parches iniciales (v2.1.0–v2.3.0) apuntaban a selectores fantasma (`#modulo-lineup`, `#modulo-gallery`, `.media-grid`, `.poster-main-img`, `.spotify-container`) inexistentes en el DOM real de `evento3.html`; corregido con los IDs reales (`#mod-video`, `#mod-lineup`, `#mod-playlist`, `#db-poster-img`, `#db-spotify-embed`), stack móvil de `.lineup-grid` y liberación de wrappers `max-width:1100px` en video/playlist. Ver `ERRORES_HISTORICOS.md` Sección 5 y `DECISIONS.md` ADR-013.
- [x] **TSK-019 | Chief Architect (Claude):** Wizard Inteligente de Creación de Eventos (4 pasos) en `admin.html` — Orquestador de Intención Cine/Fiesta/Campaña, Captura Pura (toggle + persistencia), armonías cromáticas vía TinyColor2, fix de persistencia `template_id`/`categoria_slug` y fix del bug de anidación `<div>` en Aforo. Estado: **Completado ✅** en código (HTML/CSS/JS de `admin.html`, verificado con parser HTML5 — 0 errores de anidación en ~7800 líneas), pendiente de verificación visual real por Dirección (Prueba de Carga Dual) y de cerrar TSK-016/017/018. Ver `DECISIONS.md` ADR-021 y `ERRORES_HISTORICOS.md` §8.
- [x] **TSK-020 | Chief Architect (Claude):** Reorganización de navegación del módulo Admin — pestañas internas Historial/Crear/Invitadores (Historial por defecto), título + buscador + filtro de categoría + Próximos/Pasados en el Historial, contadores en pestañas, y reubicación de "Nuevo asistente" (admin_evento). Estado: **Completado ✅** en código, verificado con parser HTML5 (0 errores) y `node --check` sobre el JS. Detectó y corrigió en la misma sesión una regresión real: `aplicarRestriccionesAdminEvento()` ocultaba la card de creación con un selector posicional que la reestructuración en pestañas habría roto — corregido a selector por atributo. Ver `DECISIONS.md` ADR-022 y `ERRORES_HISTORICOS.md` §9.
- [x] **TSK-021 | Chief Architect (Claude):** Misma reorganización aplicada a Panel — 4 pestañas internas Resumen/Tendencias/Rankings/Asistentes (Resumen por defecto) agrupando las 14 secciones de analítica que antes eran un único scroll, controles globales (vista/período/exportar) siempre visibles fuera de las pestañas, badge de conteo en Asistentes. No se agregó funcionalidad nueva a la tabla de asistentes (ya tenía buscador/filtros/acciones masivas/paginación). Estado: **Completado ✅** en código, verificado con parser HTML5 (0 errores) y `node --check`. Detectó y corrigió en la misma sesión un bug real antes de entregar: la regla CSS de ocultamiento por defecto solo cubría el atributo `data-adm-tab`, no el nuevo `data-pnl-tab` — las 4 pestañas de Panel habrían quedado todas visibles a la vez. Ver `DECISIONS.md` ADR-023 y `ERRORES_HISTORICOS.md` §10.
- [x] **TSK-022 | Chief Architect (Claude):** Fix "Null Pointer Regression" — Silent Fallback en `_renderTablaPagina()` (bloqueaba TODO el panel para orgs con `inscritos.nombre`/`evento_nombre` nulos, caso "Barrio R10") y en `evStats` de `renderPanel()` (mismo riesgo, no disparado aún). Confirmado que NO es regresión de ADR-023. Log TRACE del Escudo GOLD agregado. Verificado con parser HTML5, `node --check` y una prueba funcional aislada con casos `null`. Entregada además `migrations/adr024_audit_nulls_inscritos.sql` (solo lectura) para ubicar otros registros/orgs con campos incompletos — corregida tras reporte de fallo real en Postgres (alias combinados en `ORDER BY`, resuelto con CTE). Ver `DECISIONS.md` ADR-024 y `ERRORES_HISTORICOS.md` §11.
- [x] **TSK-023 | Chief Architect (Claude):** Auditoría completa del patrón "métodos de string sin blindar" (ADR-024) sobre `admin.html` — el mandato de ADR-024 se limitó a `_renderTablaPagina`/`renderPanel`; esta sesión extendió el barrido a TODO el archivo. Resultado: 11 puntos de crash reales corregidos con Silent Fallback `(campo||'')` — `renderEventos()` (L3516 y L3539: `s.nombre`/`e.nombre`.replace en los `onclick` de `openMod`), `renderInvitadoresPerfil()` (L4281 `g.nombre.split`) y `abrirPerfilInvitador()` (L4309 `grupo.nombre.split`, L4349 `l.link_generado.split('slug=')`), más `renderDirectorio()` (L5012 `p.nombre.split`), duplicados (L4931/32 `p.a/b.nombre.split`), `showVerify()` de Admin (L5497 `ins.nombre.split`), `descargarInvitacion()` (L5359 `ev.nombre.replace`), export de eventos (L6467 `ev.nombre.replace` en nombre de hoja), `filtrarGlobalOrgs()`/gráficas globales (L2912 `o.nombre.length`) y búsqueda global (L3205 `inv.codigo.includes`). Se blindaron además interpolaciones que mostraban el texto literal `"null"` al usuario en las dos funciones del mandato (eventos y perfiles de invitador). Verificado con `node --check`, prueba funcional aislada con casos `null` (13/13 OK) y balance de `<div>` 0 (760/760). Ver `DECISIONS.md` ADR-029.
- [x] **TSK-027 | Chief Architect (Claude):** Exportación de "Lista de Invitados" en Excel (.xlsx) y PDF (.pdf) dentro del tab Asistentes de `admin.html`, a partir de `prompt lista.txt` (requerimiento que asumía columnas inexistentes: `tipo_asistente`, `invitador_codigo`). Reporte con SOLO `#`, `Nombre` y `Cédula` (decisión de Dirección), reutilizando la cache `_pnAllList` y los filtros visibles del panel (vista evento/serie/general + período + búsqueda/tipo/estado/categoría) **sin consultas nuevas a Supabase**; filtro adicional "Solo asistencia" (`used=true`) y PDF vía el patrón existente `window.open()` + `window.print()` (sin jsPDF). Anti-duplicidad (Regla de No-Duplicidad): se extrajeron `_leerFiltrosPanel()`/`_filtrarInscritosPanel()`, se refactorizó `_renderTablaPagina()` y `_xlsxInscritos()` reutiliza `_applyXlsxRowStyle()`. Correcciones post-auditoría: marca "Hostal Terraza" en PDF, fallback de etiqueta para rol `admin_evento` (M-1), tilde unificada "Cédula" (M-2), TRACE del Escudo GOLD en PDF (R-1) y `.filter(Boolean)` null-safety (R-2). Estado: **Completado ✅** — Escudo GOLD (qa-auditor) LIMPIA: `node --check` exit 0 en ambos `<script>` inline, balance `<div>` HTML puro 831/831 (baseline 830/830), funciones sin duplicados, smoke 25/25 con `NULL` (ADR-008/ADR-024/ADR-029), scope global y multi-tenant correctos, y Cero Borrado verificado contra `git show HEAD:admin.html` (ningún ID/función eliminado). Ver `DECISIONS.md` ADR-030 y `NEXT.md`.

**Contratos de Configuracion Wizard/Landing (ADR-047) — Entregado (2026-09-18, QA APTO)**
- [x] **TSK-036 | @admin-dev-free (implementacion) + @frontend-tpl-free + Documentation Specialist (cierre documental `@docs-keeper-free`):** Redirect dinamico del formulario del landing (`config_landing.content.form_redirect`). Estado: **Completado ✅** (QA APTO — `@qa-auditor` sobre `admin.html` y `evento.html`). Entregado: `admin.html` — input `#ld-form-redirect` (`type="url"`, L776) junto a `#ld-whatsapp-tel`; persistido en `_buildConfigLanding()` solo si es truthy (`delete` si vacio para no pisar valor previo, L3903-3907; la rama "solo formulario" NO lo persiste, L3767-3776); precarga en `_poblarContenidoWizard()` (L5083); limpieza en `_limpiarWizardCompleto()` (L4979). `evento.html` — fallback `effContent.form_redirect || effContent.comunidad_whatsapp || ''` (L1626) y redireccion en el MISMO tab (`window.location.href`, L1685) solo si el asistente marco unirse a la comunidad; si esta vacio mantiene el mensaje de exito (Silent Fallback, ADR-008). Contrato DISTINTO de `content.whatsapp` (contacto/ventas) y de `content.comunidad_whatsapp`. Limitacion conocida: `registroaforo.html` NO consume `form_redirect` (decision de Direccion; solo aplica al landing de evento). Deuda MENOR derivada en TSK-039. Ver `DECISIONS.md` ADR-047.
- [x] **TSK-037 | @admin-dev-free (implementacion) + Documentation Specialist (cierre documental):** Seccion de idioma (`#ld-i18n-config`, ADR-035) movida a un unico contenedor `.wiz-step[data-wiz-step="4"]` (Blindaje), antes de `#btn-crear-ev`. Estado: **Completado ✅** (QA APTO). Dejo de mostrarse en los pasos 2 y 3; sigue gateada por `toggleLandingConfig()` (landing activa). Regresion evitada: un solo contenedor de paso 4 (verificado por conteo: `data-wiz-step="4"` = 1, `id="ld-i18n-config"` = 1). Ver `DECISIONS.md` ADR-047.
- [x] **TSK-038 | @admin-dev-free (implementacion) + Documentation Specialist (cierre documental):** ADN Visual — preseleccion de modulos por template (`_THEME_MODULES`). Estado: **Completado ✅** (QA APTO). Mapa nuevo en `admin.html` (L2844-2862) con **18 themes** (`requeridos`/`recomendados`/`selectivos`/`metaDosLineas`). Al seleccionar template se SOBRESCRIBE: requeridos marcados+BLOQUEADOS (`disabled`), recomendados marcados, selectivos desmarcados; reset cross-categoria de TODOS los `.mods-tipo [id^="ld-mod-"]`; `metaDosLineas` gobierna `#ld-meta-hero-wrap` (solo `mistico`/f9 `true`). Modo edicion con `{preserveModules:true}`: no pisa `cfg.modules`, re-bloquea requeridos y recalcula el meta wrap. Limitacion conocida (ADR-006): `evento.html` NO lee `config_landing.modules` — el preset es UX del Wizard, no gobierna el render publico. Ver `DECISIONS.md` ADR-047.
- [ ] **TSK-039 | Direccion / Lead Developer (deuda MENOR + decision futura derivada de ADR-047):** (1) `#ld-form-redirect` usa `type="url"` sin normalizacion de esquema — normalizar (prefijar `https://` si falta) o validar antes de persistir; (2) decidir si `registroaforo.html` (ruta solo-formulario) debe consumir `form_redirect` (hoy NO lo hace por decision explicita de Direccion). Ver `DECISIONS.md` ADR-047 (Limitaciones + Pendientes).

**Modo Express (xpress) — Implementación (2026-09-21)**
- [x] **TSK-040 | @free-build (implementación) + Documentation Specialist (cierre documental `@docs-keeper-free`, Reglas de Oro #12):** Implementación del **Modo Express (xpress)** como skill operativa transversal — se activa con "express"/"xpress"/"rápido"; incluye gate de escalado con los archivos críticos reales del repo, ruteo de delegación FREE/PRO, plantilla de brief express, checklist de 6 puntos y cierre documental. Alcance real ejecutado: `.opencode/skills/express-mode/SKILL.md` (**NUEVO**, 116 líneas, ASCII-safe, 0 bytes > 127); `scripts/express_check.js` (**NUEVO**, 362 líneas, Node CommonJS sin dependencias, READ-ONLY, ASCII-safety 0/0/0, `node --check` PASS — escanea `api/*.js` + `js/*.js` + `scripts/*.js` + `*.js` de raíz + el set HTML clave `index.html`/`admin.html`/`scanner.html`/`eventovenezuela.html`; paso 0 de existencia con faltante = SKIP (nunca FAIL) y `BASELINE_DIV_DIFF = { 'index.html': 1, 'admin.html': 3 }` para no reportar como regresión el desbalance preexistente); `DOCUMENTOS/SKILL_MODO_EXPRESS.md` (**REESCRITO**, 120 líneas — copia de registro sincronizada con la skill, sin referencias a ExploraCO); `AGENTS.md` (169 → 182 líneas, +13 inserciones, 0 borrados — sección `## Modo Express (xpress)`, ítem 7 en Reglas transversales y línea de ruteo en el flujo de delegación); `Sistema QR desarrollo/DECISIONS.md` (764 → 785 líneas, append al final: "Nota de práctica operativa (**NO es un ADR**) sin número", fecha 2026-09-21, estado VIGENTE). Verificación ejecutada sobre los archivos reales (ADR-006): `node --check scripts/express_check.js` → PASS; `node scripts/express_check.js` → `Resumen: PASS 9, FAIL 0 (omitidos 0)` con exit code 0 y baseline reconocido; ASCII-safety del script 0/0/0; 0 residuos "ExploraCO" en la skill; `git diff --stat` → `AGENTS.md +13`, `DECISIONS.md +23/-1` (los -1 son el ajuste de fin de línea, no pérdida de contenido). Estado: **Completado ✅ (working tree, SIN commit)**. Deuda asociada registrada como `[DEUDA-EXPRESS]` (6 ítems) en `NEXT.md` (hito -12). Ver `DECISIONS.md` (Nota de práctica operativa 2026-09-21 — sin número ADR).

**Analisis AI-DOS v1.1 + Orquestacion Hybrid por Proyecto (2026-09-21, sesion xpress)**
- [x] **TSK-041 | Documentation Specialist (cierre documental, sesion xpress):** Documento nuevo `DOCUMENTOS/Analisis_AI-DOS_HostalTerraza.md` (319 lineas, ASCII-safe 0 bytes > 127): diagnostico del AI-DOS v1.1 (brechas G1-G12) y de las Reglas de Oro QR v127 frente a la operacion real del repo, con **Seccion 5 central de orquestacion de agentes por proyecto** - sistema HYBRID (esquema C tripartito): matriz agente -> modelo -> ruta PRO/FREE sobre el roster real de 35 agentes, regla de ruteo por RIESGO (PRO obligatorio si toca runtime de produccion, datos/RLS, ADR o certificacion QA; FREE por defecto en exploracion, docs del Core, smokes/seed no criticos y JS rutinario no compartido; ante duda subir a PRO; gate de cuota con etiqueta `[DEUDA-EXPRESS]`), primarios nuevos `hybrid-build`/`hybrid-plan` y plantilla de parametrizacion por proyecto. Numeros clave del analisis (evidencia E3 de `logs/uso/`, mes 2026-09-07): 151 sesiones costo $3.2078; `deepseek-v4-pro` = 72.8% del gasto con solo 3 sesiones; ruta free $0.0000; task 92 = 0.76% de 12.081 calls; ratio de delegacion 35.8% (target >= 50%). Estado: **Completado** (working tree, SIN commit). Deuda derivada `[DEUDA-EXPRESS]` registrada en `NEXT.md` (hito -13): implementar la matriz hybrid en `AGENTS.md`, crear `hybrid-build`/`hybrid-plan`, crear `qa-auditor-free`, indices de `DECISIONS.md`/`ERRORES_HISTORICOS.md`, smokes versionados y gate de commit para TSK-029..040. Ver `DOCUMENTOS/Analisis_AI-DOS_HostalTerraza.md`.

**Esquema TRIPARTITO Free/Hybrid/Pro + Fix Gatekeeper Tier Gratuito (2026-09-21, sesion xpress)**
- [x] **TSK-042 | @free-build (implementacion) + Documentation Specialist (cierre documental `@docs-keeper-free`):** Adoptar el **esquema TRIPARTITO de rutas (Free / Hybrid / Pro)** en HostalTerraza (esquema propuesto por primera vez en TSK-041 / hito -13) y corregir el **403 del gatekeeper del tier gratuito de OpenCode Zen**. (1) **Fix gatekeeper:** causa raiz del 403 `"OpenCode's free tier can only be used from within OpenCode"` = el gatekeeper de OpenCode Zen (aprox. 16-19 sep 2026) exige simultaneamente `stream=true` + declarar las tools `bash`/`shell` y `read` en el body del request + cabeceras de cliente OpenCode; un agente con modelo gratis (`opencode/*`) y `bash: deny` NO declara la tool `bash` -> 403. Corregido en `.opencode/agent/free-plan.md`: `edit: deny`/`bash: deny` -> `edit: ask`/`bash: ask` (verificado en archivo real; la regla queda: modelos gratis NUNCA con `bash: deny`). (2) **Esquema TRIPARTITO:** 4 agentes nuevos en `.opencode/agent/` - `hybrid-plan.md` (primario, `opencode-go/deepseek-v4.1-flash`, edit/bash deny, matriz de ruteo PRO/FREE por riesgo con ruta PRO para backend/admin/renderer/UI compleja/SQL critico/arquitectura/auditoria y ruta FREE para rutinario), `hybrid-build.md` (primario, edit/bash allow), `exp-pickle-free.md` (subagente gratuito de soporte mecanico, temperature 0.3) y `qa-auditor-free.md` (auditor gratuito, Escudo GOLD, solo reporta). **`AGENTS.md` reescrito** al esquema tripartito (199 lineas reales): roster 39 agentes (15 pares pro/free + 9 singulares), secciones 1.1 Ruta GRATUITA / 1.2 Ruta HYBRID / 1.3 Ruta de PAGO + seccion nueva "Modelos gratis: gatekeeper del tier gratuito". **`opencode.json` NO se toco** (`default_agent: free-plan` verificado). Revision `@architect-review-free`: APTO CON OBSERVACIONES; 2 corregidas (free-plan.md tabla -> `@qa-auditor-free`; exp-pickle-free.md -> `architect-review-free`), ambas verificadas en los archivos reales. Estado: **Completado** (working tree, SIN commit). Evidencia real (ADR-006): `opencode --version` = 1.18.32; `node scripts/express_check.js` -> PASS 9 / FAIL 0; roster verificado = 39 archivos. Deuda `[DEUDA-EXPRESS]` registrada en `NEXT.md` (hito -14): `DOCUMENTOS/MODO_EXPRESS_ANALISIS.md` citado por `AGENTS.md` NO existe + `DOCUMENTOS/` real solo tiene 3 archivos (los citados en hitos -12/-13 no estan en el disco); 16 referencias con emoji `🛡️` a `Reglas de Oro QR.md` vs. nombre real sin emoji; decidir ADR del esquema TRIPARTITO en `DECISIONS.md`.

--------------------------------------------------------------------------------

#### 🟢 BACKLOG / LARGO PLAZO


**Infraestructura SaaS (Bloqueante)**
- [ ] **TSK-001 | Lead Developer (Gemini):** Implementar widget de pago de Wompi en registro.html y en la sección de renovación/suscripción.
- [ ] **TSK-002 | Lead Developer (Gemini):** Crear la Edge Function wompi-webhook en Supabase para validar la firma HMAC y activar organizaciones.activa = true.

- [ ] **TSK-004 | Lead Developer:** Construir la lógica de lista de espera (lista_espera table) para eventos con aforo completo.
- [ ] **TSK-007 | Chief Architect:** Refactorizar admin.html para carga perezosa (lazy load) de módulos pesados (~214KB).
- [ ] **TSK-008 | Lead Developer:** Integración de cobro Wompi directo en evento.html para entradas de pago.
- [ ] **TSK-009 | QA / Chief Architect:** Generación e integración de tickets digitales para Apple Wallet (.pkpass) y Google Wallet.

--------------------------------------------------------------------------------
*TRACE: Sincronía técnica [v1.18-FIX] sellada. Cierre documental del ciclo Entrega 1+2+3 (2026-09-16, QA APTO - Escudo GOLD PASS): **TSK-030 COMPLETADA** — ADR-040 IMPLEMENTADO via ADR-045: migracion `migrations/adr040_config_global_templates.sql` (idempotente, aditiva, fila unica `config_global`, helper `is_master_org_user()`, RLS SELECT authenticated/escritura master, RPC `fn_config_global_merge` con merge JSONB server-side, contrato `{version:2, hidden, order}`, NOTIFY pgrst, rollback comentado) + `admin.html` (sub-tab Plantillas MOSTRAR/OCULTAR/REORDENAR en `pg-globaladmin`, bloqueo duro en cero catalogo, aviso suave por categoria, reorden `appendChild`, revelacion legacy no persistente, `_primerThemeVisible`, guard `data-cat-empty`, namespace `data-tplcat`, fail-open con `console.warn`); defectos A-H corregidos antes del cierre (ver `ERRORES_HISTORICOS.md` §13). **ADR-044** (Entrega 1): catalogo sin numeracion `P#` (18 cards `.ld-theme-card` + mapa `names` solo NOMBRE+DESCRIPCION; P9-P13 estaba duplicado entre Fiesta y Campana) + registro del silo f11 como theme `fosforescente` (tarjeta + `_THEME_TYPE['fosforescente']='fiesta'` + `_THEME_TPL['fosforescente']='f11'` + derivado `_TPL_THEME['f11']='fosforescente'` + `names`); `f11.css` real reconciliado a **v3.3.3 / 1595 lineas** (los docs lo daban como v3.2.0/1554); `rico`/`f10` sigue pendiente opcional; NO se tocaron `evento.html`/`f7.html`/`f11.css`/`f11gemini.css`. **ADR-046** (Entrega 3): hub normativo `Sistema QR desarrollo/AMPLIACION/TEMPLATES.md` v1.3.0 (438 lineas, 10 capitulos + Brief + Metodologia 8 pasos + Anexos A/B/C ~81 IDs) + skill puente `.opencode/skills/templates/SKILL.md` (remite al hub; advertencia Contrato v112 vs DOM real: no reportar los 12 atomos renombrados como bug). **TSK-034 COMPLETADA** (hub+skill); **TSK-035 PENDIENTE** (Direccion: ejecutar `adr040_config_global_templates.sql` en Supabase + Prueba de Carga Dual). Historial previo intacto: Entrega F9 "Místico" (TSK-029 — ADR-041: átomo `#mod-descripcion`, silo reorganizado, meta 2 líneas, boletería por tarifa real, registro theme `mistico`→`f9`, WhatsApp sin número; Escudo GOLD PASS, H-1/H-2 corregidos) completada en working tree sin commit; deuda técnica H-3/H-4 registrada como TSK-031; Cierre documental del silo f10 v2.0.0 (TSK-032 - ADR-042): reescritura total solo-CSS de `css/templates/fiesta/f10.css` para el theme "Rico" (Cyberpunk Fosforescente), evento `rico-5mw5` en produccion, log de versiones del pie actualizado a la descripcion canonica, plan en `.opencode/plans/template-f10-rico.md`, fila del silo agregada a `PROJECT.md` Seccion 6 y hito -8 en `NEXT.md`; unico archivo de codigo tocado: `f10.css`; theme `rico` sin registro en `admin.html` (pendiente opcional). Cierre documental del fix mobile del hero f11 (TSK-033 - ADR-043, v3.2.0): bug reportado por el usuario (en mobile los elementos countdown/meta/boton quedaban encima del titulo) corregido con orden explicito de los 4 hermanos del hero en la media query 767px (`.tpl-f11`+`.Tpl-F11`: hero=1, unified-frame=2, hero-meta=3, hero-ctas=4, resto re-numerado 5-15) + hero en UNA sola pantalla (proporcion 60/40 aprobada, `min-height: 60svh`/`60vh`, titulo `clamp(2.6rem, 11vw, 4.2rem)` y version 480px, `background-position: center center`); unico archivo tocado `f11.css` (1544 -> 1554 lineas); `f11gemini.css` NO tocado (decision explicita del usuario); Escudo GOLD PASS (llaves 213/213 diff 0, desktop 85vh intacto, breakpoint 992-1279px intacto, sin media queries nuevas, Cero Borrado); registrado en `ERRORES_HISTORICOS.md` Seccion 12 y `PROJECT.md` Seccion 6 (silo f11 agregado al catalogo).*
*TRACE: Cierre documental del ciclo Contratos Wizard/Landing (2026-09-18, QA APTO - `@qa-auditor`): **ADR-047** registra TRES contratos ya implementados y certificados APTO en `admin.html` y `evento.html` (working tree sin commit). (1) **`form_redirect`** (TSK-036): `config_landing.content.form_redirect` como destino generico del formulario del landing, distinto de `content.whatsapp` y `content.comunidad_whatsapp`; `evento.html` consume `form_redirect || comunidad_whatsapp || ''` y redirige en el mismo tab solo si el asistente marco unirse (Silent Fallback sin link); `admin.html` persiste solo si es truthy (`delete` si vacio), precarga, limpia, y la rama "solo formulario" NO lo persiste; `registroaforo.html` NO lo consume (decision de Direccion). (2) **`#ld-i18n-config` solo en paso 4** (TSK-037): movido al unico `.wiz-step[data-wiz-step="4"]` (Blindaje), antes de `#btn-crear-ev`, ya no aparece en pasos 2/3; sigue gateado por `toggleLandingConfig()`; regresion de contenedor duplicado evitada (`data-wiz-step="4"`=1, nodo i18n=1). (3) **`_THEME_MODULES`** (TSK-038): mapa de ADN Visual con 18 themes (`requeridos`/`recomendados`/`selectivos`/`metaDosLineas`); al seleccionar template sobrescribe requeridos marcados+bloqueados, recomendados marcados y selectivos desmarcados con reset cross-categoria; `metaDosLineas` gobierna `#ld-meta-hero-wrap` (solo `mistico`/f9); en edicion `{preserveModules:true}` no pisa `cfg.modules`. **Limitacion verificada (ADR-006):** `evento.html` NO lee `config_landing.modules` (0 ocurrencias) — el preset es UX del Wizard, no gobierna el render publico. **Deuda MENOR TSK-039:** `type="url"` sin normalizacion de esquema + decision futura sobre `registroaforo.html`. Verificacion documental contra archivo real: `admin.html` 9940 lineas, `evento.html` 1724 lineas, `registroaforo.html` 0 referencias a `form_redirect`. Historial previo intacto (Cero Borrado documental).*
*TRACE: Cierre documental del ciclo Modo Express (xpress) (2026-09-21, verificacion PASS 9/FAIL 0): implementacion de la skill transversal `.opencode/skills/express-mode/SKILL.md` (NUEVO, 116 lineas, ASCII-safe 0 bytes > 127; se activa con "express"/"xpress"/"rapido"; gate de escalado con archivos criticos reales del repo, ruteo de delegacion FREE/PRO, plantilla de brief express, checklist de 6 puntos y cierre documental) + comando unico de verificacion minima `scripts/express_check.js` (NUEVO, 362 lineas, Node CommonJS sin dependencias, READ-ONLY, `node --check` PASS; escanea `api/*.js` + `js/*.js` + `scripts/*.js` + `*.js` de raiz + set HTML clave `index.html`/`admin.html`/`scanner.html`/`eventovenezuela.html`; paso 0 de existencia faltante = SKIP nunca FAIL; `BASELINE_DIV_DIFF` = `index.html` +1 / `admin.html` +3) + `DOCUMENTOS/SKILL_MODO_EXPRESS.md` reescrito (120 lineas, copia de registro sincronizada con la skill, sin referencias a ExploraCO) + `AGENTS.md` (169 → 182 lineas, +13 inserciones, 0 borrados: seccion `## Modo Express (xpress)` L104, item 7 en Reglas transversales L123, ruteo en flujo de delegacion L148) + `DECISIONS.md` (764 → 785 lineas, append al final "Nota de practica operativa" que NO es un ADR, 2026-09-21, estado VIGENTE). Verificacion contra archivo real (ADR-006): `node --check scripts/express_check.js` → PASS; `node scripts/express_check.js` → `Resumen: PASS 9, FAIL 0 (omitidos 0)` con exit 0 y baseline reconocido; ASCII-safety del script 0/0/0; 0 residuos "ExploraCO" en la skill; `git diff --stat` → `AGENTS.md +13`, `DECISIONS.md +23/-1` (los -1 son ajuste de fin de linea, sin perdida de contenido). Deuda `[DEUDA-EXPRESS]` (6 items) registrada en `NEXT.md` hito -12. Historial previo intacto (Cero Borrado documental).*
*TRACE: Cierre documental del ciclo Analisis AI-DOS v1.1 + orquestacion hybrid por proyecto (2026-09-21, sesion xpress): **TSK-041 COMPLETADA** - documento nuevo `DOCUMENTOS/Analisis_AI-DOS_HostalTerraza.md` (319 lineas, ASCII-safe 0 bytes > 127), espejo del analisis de la etapa ExploraCO aplicado a HostalTerraza: diagnostico del AI-DOS v1.1 (brechas G1-G12), diagnostico de las Reglas de Oro QR v127 y Seccion 5 central de orquestacion de agentes por proyecto con sistema HYBRID (matriz agente -> modelo -> ruta PRO/FREE sobre el roster real de 35 agentes, regla de ruteo por riesgo, primarios nuevos `hybrid-build`/`hybrid-plan`, plantilla de parametrizacion por proyecto). Numeros clave con evidencia real (ADR-006): costo mes $3.2078 en 151 sesiones, `deepseek-v4-pro` = 72.8% del gasto con 3 sesiones, ruta free $0.0000, task 0.76%, ratio de delegacion 35.8% vs target >= 50%. Deuda `[DEUDA-EXPRESS]` registrada en `NEXT.md` hito -13 (implementar la matriz hybrid en `AGENTS.md`, crear `hybrid-build`/`hybrid-plan` y `qa-auditor-free`, indices de `DECISIONS.md`/`ERRORES_HISTORICOS.md`, smokes versionados, gate de commit para TSK-029..040). Historial previo intacto (Cero Borrado documental).*
*TRACE: Cierre documental del ciclo Esquema TRIPARTITO + Fix Gatekeeper (2026-09-21, sesion xpress, verificacion PASS 9/FAIL 0): **TSK-042 COMPLETADA** - se adopta el esquema TRIPARTITO (Free/Hybrid/Pro) y se corrige el 403 del gatekeeper del tier gratuito de OpenCode Zen. Fix: `.opencode/agent/free-plan.md` `edit: deny`/`bash: deny` -> `edit: ask`/`bash: ask` (causa raiz: el gatekeeper de OpenCode Zen exige stream=true + tools bash/shell y read declaradas en el request + cabeceras de cliente; `bash: deny` omite la tool y devuelve 403). Esquema TRIPARTITO: 4 agentes nuevos (`hybrid-plan.md`, `hybrid-build.md`, `exp-pickle-free.md`, `qa-auditor-free.md`) + `AGENTS.md` reescrito a 199 lineas (roster real 39 agentes: 15 pares pro/free + 9 singulares; secciones 1.1 Free / 1.2 Hybrid / 1.3 Pro + seccion "Modelos gratis: gatekeeper"). `opencode.json` intacto (`default_agent: free-plan`). Revision `@architect-review-free` APTO CON OBSERVACIONES; 2 corregidas verificadas. Evidencia (ADR-006): `opencode --version` = 1.18.32; `node scripts/express_check.js` -> PASS 9 FAIL 0; roster verificado 39 archivos; `DOCUMENTOS/MODO_EXPRESS_ANALISIS.md` = NO existe (deuda); 16 referencias `🛡️` a Reglas de Oro vs. nombre real sin emoji (deuda); ADR TRIPARTITO pendiente de decision de Direccion (deuda). Historial previo intacto (Cero Borrado documental).*
*TRACE: Cierre documental del ciclo Silo F12 "Kande" + Registro del theme + Reconciliacion evento-app.html (2026-09-22, QA APTO CON OBSERVACIONES): **TSK-043 COMPLETADA** - silo NUEVO `css/templates/fiesta/f12.css` v1.0.0 (1563 lineas, 50294 bytes, ASCII 0 bytes > 127, llaves 193/193, 0 `@import` reales, 0 selectores fuera de scope bajo `.tpl-f12`/`.Tpl-F12`, 28 tokens `--f12-*` + puente `--tpl-accent: var(--master-accent, #ff2f8e)`, 14 modulos encendidos / 9 apagados con IDs intactos, hero opcion A sin 100vh, datos sin duplicar, ancla `.hero-cta-btn--lineup` oculta, 100% CSS sin cambios de kernel) + theme `kande` registrado en `admin.html` en 6 puntos reales (L606 `data-theme="kande"` + `abrirFichaTheme`, L2830 `_THEME_TYPE`, L2844 `_THEME_TPL`, L2875 `_THEME_MODULES`, L3103-3104 `_THEME_FICHA`, L3235 `names`; 19 tarjetas `.ld-theme-card`, 18 previas intactas) + correccion de defecto MAYOR del preset ADN Visual (`requeridos` de `kande` = boletos/playlist/ubicacion; `ld-mod-historia` eliminado por no existir en `#mods-fiesta` L651-665, solo en `#mods-campana` L684) + **hallazgo estructural** `evento.html` NO existe en el repo: motor real `evento-app.html` (1724 lineas, v214; `vercel.json` → `/api/evento-og`; `injectAtomicCSS` L598-638; doble clase L691-693; `#db-gallery-grid` L148 dentro de `#mod-video` L147; `#mod-hero-ctas` nace `display:none` L33; sin override `?tpl=`/`?cat=`; `MODULE_IDS` ausente en v214) - reconciliado en TEMPLATES.md v1.4.0, PROJECT.md Seccion 6 y ADR-048 sin borrar texto original. **Deudas TSK-044..TSK-049** (T6 smoke, D1/D2 Direccion, assets v1.1.0, nav superior = ADR futuro, `@media-reader-free` roto por modelo retirado, hub Paso 8 a 6 puntos). NOTA DE NUMERACION: los IDs TSK-040/041/042 ya existian (Modo Express / Analisis AI-DOS / Esquema Tripartito del 2026-09-21); por Cero Borrado y unicidad de ID este ciclo se numera desde **TSK-043** (mapeo pedido: 040->043, 041->044, 042->045, 043->046, 044->047, 045->048, 046->049). Historial previo intacto (Cero Borrado documental).*
*TRACE (2026-09-22, complemento de verificacion del ciclo F12/ADR-048, patron ADR-006): **entregable de `ERRORES_HISTORICOS.md` materializado esta sesion** — subseccion 13.3 agregada (colision de namespace Fiesta/Campana del preset `_THEME_MODULES['kande']`; problema/causa raiz/correccion/blindaje/verificacion + TRACE al pie). Reconciliaciones verificadas contra los archivos reales (prevalece el archivo): (1) `_THEME_FICHA['kande']` real en `admin.html` **L3104-3105** (docs previos citaban L3103-3104 — desfase de 1 linea por el par `{n,c}`/`{t}`; el cuerpo `n:'Kande',c:[...]` abre en L3104 y la descripcion `t:[...]` en L3105); (2) `_aplicarPresetTheme` real **L3247-3293** (rango completo; `document.getElementById(id)` global en L3263/3268/3276/3284); (3) `_buildConfigLanding` real **L3894** (persistencia `historia: chk('ld-mod-historia')` en **L3961**); (4) `hideForeignModulesByType` **L5154** (mapa `ld-mod-historia:'campana'` L5167; loop `querySelectorAll('[id^="ld-mod-"]')` L5189); (5) `#mods-fiesta` contenedor **L650-678** con grid de checkboxes L652-665 (docs citaban L651-665 — equivalente); (6) conteo `.ld-theme-card` = **19** confirmado por regex sobre `admin.html`; (7) `f12.css` re-verificado: 1563 lineas / 50294 bytes / ASCII 0 / llaves 193/193 / 28 tokens unicos `--f12-*` / `.tpl-f12` 215 + `.Tpl-F12` 171 / 5 media queries (767 L1247, 480 L1439, 992-1279 L1478, min-1280 L1518, reduced-motion L1534) / alturas hero 72-68-72-78 svh. Los 6 entregables pedidos para el ciclo quedan CERRADOS (ADR-048 + TSK-043..049 + hito -15 + fila PROJECT + hub v1.4.0 + subseccion ERRORES 13.3). Historial previo intacto (Cero Borrado documental).*
*TRACE: Cierre documental del ciclo visual Silo F12 "Kande" v1.3.0 -> v1.9.0 (2026-09-22, ADR-049): **TSK-050 COMPLETADA** - ciclo 100% CSS-only en UN solo archivo `css/templates/fiesta/f12.css` (v1.3.0 -> v1.9.0), sin tocar kernel `evento-app.html`, SQL ni `admin.html`. Baseline `5d3676c`; commits del ciclo en `main` (desplegados via Vercel): `6960cc6` (v1.4.0+v1.5.0), `17f8cb4` (v1.6.0), `de790a0` (v1.7.0), `f6f13ed` (v1.8.0), `e528464` (v1.9.0 = HEAD); commit de Direccion `a45174f` (asset `--f12-asset-flora`) entre medio. `HEAD == origin/main == e528464`; working tree limpio/sincronizado. Verificacion real (ADR-006, HEAD): 2423 lineas / 86600 bytes / ASCII 0 bytes > 127 / llaves 257/257 / 22 IDs `#mod-*` intactos (Cero Borrado) / 6 commits del ciclo tocan solo `f12.css`. Patron del ciclo documentado en **ADR-049** (contenido temporalmente fijado en CSS + plan de migracion a datos); deuda D1-D6 registrada como **TSK-051..TSK-056** (D5 referencia TSK-048/TSK-049 sin duplicarlas). Relevo en `NEXT.md` hito -16. Historial previo intacto (Cero Borrado documental).*
*TRACE (2026-09-22, EJECUCION OPERATIVA CRITICA de base de datos, Evidencia Escudo GOLD de datos): **TSK-057 COMPLETADA** - `migrations/adr032_landing_analytics.sql` APLICADO y VERIFICADO en produccion `ctgyvydzshueemlelkzv.supabase.co` (instancia confirmada por el usuario; canal Supabase CLI v2.84.2 + PAT de Windows Credential Manager -> Management API `/v1/projects/{ref}/database/query`, rol postgres, PostgreSQL 17.6). Estado pre: `page_events` INEXISTENTE (null en to_regclass, ya documentado como PGRST205 en ADR-039) y `v_landing_analytics` inexistente. Estado post (output real): `count(*)` = 0; 15 columnas conforme al script; 3 indices + pkey; vista `v_landing_analytics` creada (definicion integral verificada); RLS `relrowsecurity=true` con exactamente 2 politicas (`page_events_anon_insert` INSERT anon WITH CHECK true / `page_events_auth_read` SELECT authenticated USING true) y cero UPDATE/DELETE (Cero Borrado de telemetria); grants anon INSERT / authenticated INSERT+SELECT; smoke funcional INSERT como rol `anon` exitoso (fila real creada y luego limpiada; count final 0). Limitacion del canal documentada: el runner de Management API usa autocommit por statement (los bloques BEGIN/ROLLBACK no comparten transaccion en una misma llamada multi-statement). No se modifico el archivo de migracion; `git status` limpio al cierre; unico cambio en repo = esta entrada documental. Historial previo intacto (Cero Borrado documental).*
*TRACE (2026-09-22, cierre documental del ciclo ADR-050, verificacion ADR-006 contra working tree): **TSK-058 COMPLETADA** - `migrations/adr050_landing_analytics_ampliado.sql` (274 lineas, 100% idempotente) APLICADO y VERIFICADO en produccion `ctgyvydzshueemlelkzv.supabase.co` (mismo canal de TSK-057: Supabase CLI v2.84.2 + PAT CredMan -> Management API rol postgres). Estado pre verificado: adr032 aplicado, `page_events` con CHECK 6 tipos, `page_events_count=0`. Estado post (output real transcrito): CHECK 8 tipos (`section_view`, `lightbox` agregados) via `pg_constraint`; indice `idx_page_events_org_slug_created (org_id, evento_slug, created_at DESC)` via `pg_indexes`; 4 vistas (`v_landing_analytics` 15 columnas con org_id en GROUP BY + form_ok/dup/error + section_views + lightbox_events + scroll_50pct, `v_landing_clicks_rank`, `v_landing_serie_diaria`, `v_landing_funnel`) con **`security_invoker=true` en pg_class**; GRANT SELECT TO authenticated. **TSK-059 (implementado, QA en curso):** front del ciclo verificado contra archivos reales — `admin.html` sub-tab "Landing" 5.º pill (`data-pnl-pill="landing"` L1439, `data-pnl-tab="landing"` L1659-1698, 6 KPIs, `renderLanding()` L8151 con las 4 vistas filtradas por ORG_ID/slug/periodo, `exportarLandingXLSX()` L8283 con empty state fail-open L8161, SIN boton PDF en el sub-tab); `evento-app.html` (CLICK_RULES L415-428 con `btn:spotify` + `link:externo`, comentario lightbox nativo L409-414, `emitLb` L561-613 `lb:*`, `section_view` L461-480, `btn:tab_*` L674); `eventovenezuela.html` port b5 (`.gallery-thumb`→`lb:open` L643, `link:externo` L645-646, `section_view` L695; formulario campana con `comunidad_ok`/`form_error` sin `comunidad_dup`). **ADR-050 registrado en `DECISIONS.md`** (tras ADR-049, sin bumpear version v1.16-GOLD) + **hito -17 en `NEXT.md`** + **riesgo activo tenancy `page_events_auth_read USING true` en `NEXT.md` §4** (ajuste por org_id diferido al barrido TSK-026; impacto en 4 vistas via security_invoker). **PENDIENTE DIRECCION:** commitear working tree (`admin.html` M, `evento-app.html` M, `eventovenezuela.html` M, `migrations/adr050_landing_analytics_ampliado.sql` untracked, `DECISIONS.md` M, `NEXT.md` M, `TASKS.md` M) y desplegar el front a Vercel (las migraciones adr032+adr050 YA estan en la instancia; datos de telemetria acumulan desde 2026-09-22; sub-tab Landing NO visible en produccion hasta desplegar). Historial previo intacto (Cero Borrado documental).*